**Root Causes Analysis**

1. **Probe Script Swallowing Errors**
   - **File:** lib/nasim/probe.sh
     - **Line Pattern:** `probe_and_show() { ... 2>/dev/null ... }`
   - **Recommended Fix:** Remove the error redirection to surface issues.
     ```bash
     probe_and_show() {
         # ...
         echo "Status: $status"
         echo "Models: $models"
         # Remove 2>/dev/null to show errors
         # 2>/dev/null
     }
     ```

2. **Missing Models Command**
   - **File:** bin/nasim (or lib CLI)
     - **Line Pattern:** No `nasim models` command exists.
   - **Recommended Fix:** Add a new command to list available models.
     ```bash
     function cmd_models() {
         echo "Available models:"
         for model in "${MODELS[@]}"; do
             echo "$model"
         done
     }
     ```

3. **Stale Doctor Reports**
   - **File:** lib/nasim/doctor.sh
     - **Line Pattern:** Incorrect status reporting despite issues.
   - **Recommended Fix:** Enhance checks to include model availability.
     ```bash
     function doctor_check() {
         # ...
         if [ "$model_status" != "OK" ]; then
             echo "Model status: $model_status"
         fi
     }
     ``
