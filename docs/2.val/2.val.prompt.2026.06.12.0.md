"I need a comprehensive engineering blueprint to build Nasim, a terminal-native AI coding assistant that is a feature-complete local 'copy' of Claude Code. The goal is Zero Distance—it must function exactly as Claude Code does, but using local LLM infrastructure. Provide a detailed plan covering the following:
1. Core Engine: The 'Zero-Distance' Fork
Source Parity: Describe the process of mirroring the latest logic from the anthropics/claude-code repository.
API Redirection: Detail the implementation of a hard-coded or env-driven API Shim. The app must natively intercept all calls intended for the Anthropic API and route them to a local black server using ANTHROPIC_BASE_URL redirection
.
Namespace Transformation: Provide a script or methodology to globally rename all .claude references to .nasim and update the binary name to nasim, ensuring it creates project-specific .nasim/ directories and a global ~/.nasim/ base repository
.
2. The Triad Architecture (Client-Server-Bridge)
Client Implementation: Use a Node.js + Ink stack for the TUI to ensure identical UI/UX, streaming updates, and syntax highlighting
.
Server Implementation: Setup for Ollama or LM Studio on a remote server (black) configured for network access (0.0.0.0)
.
The Bridge (Server-Side): Implement a translation proxy on the server that handles the Agentic Tool Loop. It must translate Anthropic tool schemas into local model formats while handling complex multi-turn tasks (read, write, edit, bash)
.
3. Identical Knowledge Sync Engine (Import/Export)
The Bidirectional Layer: Detail a synchronization service that makes importing from and exporting to Claude Code an identical, easy process.
Import Logic: Develop a parser to read Claude's state.db and JSON sessions. It must transform this data into Nasim's high-performance JSONL history and SQLite FTS5 search index
.
Export Logic: Create a reverse-migration path to push Nasim’s local insights back into the official ~/.claude/ directory, updating Claude's internal database so the tools remain perfect peers
.
Global Sync: Ensure that whenever either nasim or claude is run in a directory, their knowledge is automatically cross-imported/exported to keep history and CLAUDE.md perfectly synced across all projects
.
4. Tooling & Security
Tool Parity: Implement all Claude Code tools (Read, Write, Edit, Bash, Glob, Grep, Git) with a 'Human-in-the-loop' confirmation gate for destructive operations
.
Plan Mode: Implement a planning system prompt that allows the local LLM to propose changes before execution
.
Please provide this as a structured roadmap with specific phases: Foundation (Redirection), Sync Engine (Import/Export), Agentic Parity (Tool Use), and Production (Performance Optimization)."