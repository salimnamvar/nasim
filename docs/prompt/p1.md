# Final Audit of Use Case Diagrams

## Overview

All previous **CAR‑UC** tasks have been successfully applied. The use case model is now **almost fully aligned** with the C4 architecture.  
The following fixes have been confirmed:

| Issue | Status |
|-------|--------|
| Actors corrected for internal groups (Provider, Router, RepoIntelligence, Evaluation, EditStrategy, Context, TL‑12) | ✅ |
| Overview diagram (`uc_overview.puml`) now uses correct UC IDs (PRV‑01, SRV‑01, HK‑01) | ✅ |
| Duplicate `AGT‑05` removed; `AGT‑02` now includes `SAF‑01` | ✅ |
| `PRV‑04` renamed to `SELECT Provider Backend` | ✅ |
| `OBS‑06 EXPORT OTLP` added | ✅ |
| `AGT‑11` renamed to `DELEGATE to Persona` | ✅ |

The use case set is **comprehensive, consistent, and traceable** to the component model.

---

## Remaining Issues (Minor)

### 1. Component Owner Mismatches in UC Inventory

The inventory (`README.md`) lists component owners that **do not exist** in the C4 component diagrams:

| UC ID | Owner (current) | Should Be |
|-------|-----------------|-----------|
| PRV‑01, PRV‑04 | `ProviderFactory` | `LiteLLMProxy` (or `Provider`) |
| SAF‑01, SAF‑02, SAF‑03 | `PermissionGate` | `SafetyCoordinator` |

**Rationale:**  
- `ProviderFactory` is not a component in the C4 model; `LiteLLMProxy` is the actual proxy that handles registration and backend selection.  
- `PermissionGate` is an internal implementation detail of the `SafetyCoordinator`; the component `SafetyCoordinator` is the documented owner in the C4 diagrams.

### 2. Overview Diagram Description Mismatch

In `uc_overview.puml`, the use case `SRV‑01 LIST Sessions` has a description that reads *“RESTful API + SSE streaming”*. This description does not match the UC name. It should either:
- Rename the UC to `SRV‑00 SERVE HTTP API` (and add it to the inventory), or
- Update the description to `List all sessions with pagination` (matching the detailed diagram).

Given that `SRV‑01` is already defined as `LIST Sessions` in the detailed diagram and inventory, the description in the overview should be corrected to align.

---

## CAR Prompt for Autonomous Fix

The following **Challenge‑Action‑Result** tasks will resolve these final inconsistencies.

---

### CAR‑UC‑7: Correct Component Owners in UC Inventory

**Challenge (Measure):**  
The UC inventory (`README.md`) lists `ProviderFactory` and `PermissionGate` as component owners for several use cases. These components are **not present** in the C4 model. The correct owners are `LiteLLMProxy` (for provider registration/selection) and `SafetyCoordinator` (for permission checks).

**Action (Fix):**  
1. **Edit** `docs/UC/README.md`.
2. In the table, locate rows where `Component Owner` is `ProviderFactory` (PRV‑01 and PRV‑04).  
   - Replace `ProviderFactory` with `LiteLLMProxy`.
3. Locate rows where `Component Owner` is `PermissionGate` (SAF‑01, SAF‑02, SAF‑03).  
   - Replace `PermissionGate` with `SafetyCoordinator`.

**Result (Verify):**  
- Run `grep -E "ProviderFactory|PermissionGate" docs/UC/README.md`. No matches should appear.
- All owners now correspond to actual components in the C4 diagrams (e.g., `LiteLLMProxy` and `SafetyCoordinator`).

---

### CAR‑UC‑8: Align Overview Description for SRV‑01

**Challenge (Measure):**  
In `uc_overview.puml`, the description for `SRV‑01 LIST Sessions` says *“RESTful API + SSE streaming”*. This is inconsistent with the detailed diagram where `SRV‑01` is specifically `LIST Sessions`. The description should match the UC name.

**Action (Fix):**  
- **Edit** `docs/UC/uc_overview.puml`.
- Locate the line:
  ```
  usecase "**SRV-01 LIST Sessions**\n--\nRESTful API + SSE streaming" as SRV01
  ```
- Replace the description with:
  ```
  usecase "**SRV-01 LIST Sessions**\n--\nList all sessions with pagination" as SRV01
  ```
- Alternatively, if you prefer a high‑level summary, you can change the UC to `SRV-00 SERVE HTTP API` and add it to the inventory, but the simpler fix is to align the description.

**Result (Verify):**  
- Check the overview diagram: `SRV‑01` now has the same description as in the detailed `uc_server.puml`.
- PlantUML renders without errors.

---

## Final Validation

After applying CAR‑UC‑7 and CAR‑UC‑8:
- All UC owners match components from the C4 model.
- The overview diagram is consistent with the detailed use cases.
- The use case model is **100% aligned** with the C4 architecture.

**Overall Status:** The use case diagrams are now **complete and consistent**, ready to serve as the behavioural specification for implementation.