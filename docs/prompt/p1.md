Here is the comprehensive audit of the nasim architecture across C4, UC, and SM layers, followed by the exact prompt you can feed to an agent to fix every issue.

---

## Audit Findings — CAR Framework

| # | Severity | Challenge | Action | Result |
|---|----------|-----------|--------|--------|
| 1 | **Critical** | **Dependency Inversion Violation** — `c4_nasim_component.puml` shows `AgentOrchestrator` (Core Library) *delegating to* `CLI` and `HTTP API Server` containers, while the Container diagram correctly shows CLI/Server → Core. Core must never depend on its callers. | Reverse or remove the two `Rel(agent_orch, cli_container/server_container, ...)` lines in the Component Overview. If event emission is needed, model it as Core → external event sink, not as Core delegating to outer containers. | Clean layered architecture: Core Library has zero inward dependencies; CLI and Server depend on Core only. |
| 2 | **Critical** | **C4 Boundary Violation** — `c4_nasim_container.puml` expands `Observability Platform` (an external system in Context diagram) as an internal `System_Boundary` with nested containers (log_agent, loki, prometheus, grafana). C4 forbids expanding external systems inside your own container diagram. | Collapse `Observability Platform` back to `System_Ext`. Remove all internal containers of the observability platform from nasim’s container diagram. Keep only cross-system relationships: `core → obs_platform` (stdout logs) and `obs_platform → core` (HTTP GET /metrics scrape). | C4-level-2 compliant diagram showing only nasim’s own deployable units. |
| 3 | **Critical** | **Lifecycle-Write UC Ownership Corruption** — SM.md assigns `Session.CLOSED` to `AGT-14 HANDLE Error` (Agent group) and `Plan.COMPLETED` to `AGT-01 PROCESS User Task`. Entity lifecycle state writes must be owned by UCs in the same domain. | Create `SSN-09 CLOSE Session` (or assign to `SSN-01`) for `CLOSED`. Assign `Plan.COMPLETED` to `AGT-08 APPROVE Plan` or a dedicated plan completion UC. Update `SM/sm_session_lifecycle.puml` and `SM/sm_plan_lifecycle.puml` transition labels. | Each lifecycle state has a single, domain-correct owning UC; SM-C4-UC traceability is preserved. |
| 4 | **High** | **C4 ↔ UC Group Boundary Mismatch** — UC treats `SAF` (Safety) and `RTG` (Router) as top-level groups with their own use-case packages, but C4 embeds them as sub-components inside `Agent` and `Provider` groups. This breaks 1:1 traceability between architecture layers. | **Option A (preferred):** Promote `SafetyCoordinator` to its own C4 component group (`c4_nasim_component_safety.puml`) and `ModelRouter`+`FallbackChain` to `c4_nasim_component_router.puml`. Update `C4.md` inventory. **Option B:** Demote SAF/RTG in UC to be sub-packages under AGT/PRV. | Perfect 1:1 mapping between C4 component groups, UC packages, and SM state-machine domains. |
| 5 | **High** | **Missing Subagent Group in Component Overview** — `C4.md` inventory lists `c4_nasim_component_subagent.puml`, but `c4_nasim_component.puml` (the cross-container overview) omits the `Subagent Group` boundary entirely, orphaning the diagram. | Add `Container_Boundary(subagent_group, "Subagent Group")` to `c4_nasim_component.puml` containing `SubagentCoordinator`. Reference `AgentOrchestrator` from Agent group via `Component_Ext` (do not duplicate it). | Complete component overview; no orphaned diagrams; DRY principle enforced. |
| 6 | **High** | **Data Structures Modeled as Runtime Components** — C4 diagrams treat `AgentEvent`, `Config` (dataclass), `Session` (dataclass), `Hook` (types.py), `HookResult`, `PluginManifest`, `ContextNode`, `ContextEdge`, `QualitySignal` (result VO), `ProviderCapabilities`, and `CompactionPolicy` as components. These are data/entities, not runtime behavioral components. | Remove them from C4 component diagrams or reclassify as data types (not `Component(...)`). If needed for documentation, describe them in a separate entity model or as comments. Keep only runtime services/modules. | C4 diagrams show true architectural components; OOP/component modeling principles are respected. |
| 7 | **High** | **PlantUML Syntax Failures** — Eight C4 component diagrams are missing the `@startuml` directive, causing render failures in standard PlantUML engines. | Add `@startuml <diagram_id>` as the **first line** (before `!include`) in: `c4_nasim_component_agent.puml`, `c4_nasim_component_config.puml`, `c4_nasim_component_plugins.puml`, `c4_nasim_component_hooks.puml`, `c4_nasim_component_git.puml`, `c4_nasim_component_subagent.puml`, `c4_nasim_component_cli.puml`, `c4_nasim_component_mcp.puml`. | All 20+ C4 diagrams render correctly without syntax errors. |
| 8 | **Medium** | **Relationship Direction Inconsistencies** — (a) Memory: overview shows `MemoryStore → MemoryScope`, but detailed diagram shows `MemoryScope → MemoryStore`. (b) Edit Strategy: overview shows `EditStrategy → ConcreteCoder` ("extends"), but detailed diagram correctly shows `ConcreteCoder → EditStrategy`; inheritance arrows point from parent to child in the overview, which is backwards. | (a) Reverse the arrow in `c4_nasim_component.puml` to match `c4_nasim_component_memory.puml`: `MemoryScope → MemoryStore`. (b) Reverse all 8 edit-strategy "extends" arrows in the overview so they point from concrete coder (child) to `EditStrategy` (parent). | Consistent relationship semantics across overview and detailed diagrams. |
| 9 | **Medium** | **UC Relationship Semantics Errors** — `uc_overview.puml` abuses `<<include>>` for optional/cross-cutting concerns: `AGT-01` includes `PRV-01` (provider registration is setup, not per-task), `TL-01` (file read is not always needed), `SAF-01` (safety is part of tool dispatch, not core loop), `WRL-01` (logging is automatic), `EDT-01` (editing is conditional). Also `SRV-01 LIST Sessions` incorrectly includes `AGT-01`. | Change `AGT-01 → PRV-01, TL-01, SAF-01, WRL-01, EDT-01` to simple associations or `<<extend>>`. Remove `SRV-01 → AGT-01` entirely. Use `<<include>>` only for mandatory sub-functionality. | UC model correctly distinguishes mandatory, optional, and cross-cutting behaviors. |
| 10 | **Medium** | **Circular UC Dependency** — `uc_evaluation.puml` has `EVL-01 EVALUATE Task` including `EVL-06 COORDINATE Retry`, and `EVL-06` extending `EVL-01`. This creates a cycle in the use-case model. | Break the cycle: remove `EVL-06 ..> EVL01 : <<extend>>`. Model retry as an internal mechanism of the EvaluationEngine, not as a UC extending its own orchestrator. | Acyclic UC dependency graph; clean hierarchical evaluation flow. |
| 11 | **Medium** | **Presentation Layer Leaking into Domain** — `c4_nasim_component_sandbox.puml` shows `DiffPresenter` directly triggering `StagedApplicator`. Presentation components must not command application logic directly. | Insert `DiffSandboxManager` as mediator: `DiffPresenter → DiffSandboxManager` (approval event), `DiffSandboxManager → StagedApplicator` (apply command). Remove direct `DiffPresenter → StagedApplicator`. | Clean separation of concerns: Presentation (formatting) → Application (orchestration) → Domain (application). |
| 12 | **Medium** | **Circular Component Dependencies in Evaluation** — `c4_nasim_component_evaluation.puml` shows `EvaluationEngine → RetryCoordinator` and `RetryCoordinator → EvaluationEngine` (same for `RepetitionDetector`). Also `LLMReviewer → QualitySignal` is labeled "consumes" but the arrow points from reviewer to signal (should be signal → reviewer for consumption, or label as "produces"). | Break cycles by modeling `RetryCoordinator` and `RepetitionDetector` as event emitters that `EvaluationEngine` subscribes to (unidirectional). Fix `LLMReviewer → QualitySignal`: either reverse arrow to `QualitySignal → LLMReviewer` with label "consumes", or change label to "produces". | Acyclic component graph; consistent C4 relationship semantics. |
| 13 | **Medium** | **Resource-Oriented Design (ROD) Violation** — Server component claims "ROD-compliant REST endpoints" but UC names are RPC-style: `SRV-02 INSERT Session`, `SRV-05 RETIRE Session`, `SRV-06 DISPATCH Message`. ROD uses resource + HTTP method semantics. | Rename to ROD-compliant UC names: `SRV-02 CREATE Session`, `SRV-05 DELETE Session`, `SRV-06 SEND Message` (or `POST Message`). Ensure `c4_nasim_component_server.puml` description matches. | UC vocabulary aligns with REST/ROD principles and the stated ROD compliance. |
| 14 | **Medium** | **Config Component Naming Schizophrenia** — `c4_nasim_component.puml` uses `config_loader` and `config_schema`, but `c4_nasim_component_observability.puml` uses `config` (ambiguous alias for the same component). | Standardize on `config_loader` and `config_schema` across all diagrams. Update `c4_nasim_component_observability.puml` to reference `config_loader` and `config_schema` explicitly. | Unique, consistent component identifiers; no alias confusion. |
| 15 | **Medium** | **Duplicate Relationship in Memory Diagram** — `c4_nasim_component_memory.puml` draws `WorkingMemoryAdapter → MemoryStore` twice with different labels ("persist working entries" and "short-term persistence"). | Remove the duplicate line. Keep one relationship with a single, accurate label (e.g., "persists working entries"). | Non-redundant, clean diagram. |
| 16 | **Medium** | **SM Color Syntax Mismatch** — `SM.md` mandates `<back:#HEX>STATE</back>` syntax, but all `.puml` state-machine diagrams use `state "STATE" as STATE #HEX` syntax. | Update `SM.md` to document the actual syntax used (`#HEX` after `as` alias), or update all `.puml` files to use `<back:#HEX>` inside state descriptions. Prefer updating `SM.md` to match reality. | Documentation and code use consistent, renderable syntax. |
| 17 | **Medium** | **Orphaned Component in Overview** — `c4_nasim_component.puml` contains `ContextProcessor` (ABC) which is **not** listed in the `C4.md` inventory table for Context Graph group. | Either add `ContextProcessor` to the `C4.md` inventory table (with description), or remove it from the overview diagram to maintain inventory-diagram parity. | Inventory table remains the authoritative source; no orphaned components. |
| 18 | **Low** | **Mass Relationship Label Inconsistency** — Hundreds of verb-tense mismatches between overview and detailed diagrams (e.g., overview "emit logs" vs detailed "logs agent lifecycle events"; overview "rank results" vs detailed "ranks search results"). | Normalize all relationship labels to third-person singular present tense (e.g., "reads", "writes", "triggers", "routes"). Create a shared glossary if needed. | Professional, consistent documentation across all C4 levels. |
| 19 | **Low** | **Incorrect ABC Tag on Concrete Tools** — `c4_nasim_component_tools.puml` tags `RepoMapTool`, `SemanticSearchTool`, and `ReviewTool` as "ABC", but they are concrete tools extending the base `Tool` ABC. | Remove the "ABC" technology tag from these three components. Ensure they show inheritance from `Tool` (child → parent) like other tools. | Correct OOP inheritance representation in C4. |
| 20 | **Low** | **Backwards UC Flow in CLI** — `uc_cli.puml` shows `CLI-04 READ CLI Arguments` including `CLI-01 PROCESS User Input`, which is backwards (arguments must be parsed *before* processing). | Reverse the relationship so `CLI-01` includes `CLI-04`, or remove the `<<include>>` and document them as sequential steps in the UC narrative. | Logical temporal flow: parse args → process input. |

---

## The Fix Prompt

Copy and paste the following block into your agent. It contains surgical, file-by-file instructions.

```markdown
You are a principal software architect specializing in C4 modeling, Use-Case-driven design, and state-machine traceability. Perform exact, surgical edits on the nasim architecture documentation to fix the issues below. Do not refactor code outside the listed files; do not change file names.

=== CRITICAL: C4 COMPONENT OVERVIEW (docs/C4/c4_nasim_component.puml) ===

1. REMOVE these two lines (Core must not depend on outer containers):
   - Rel(agent_orch, cli_container, "delegates to CLI")
   - Rel(agent_orch, server_container, "delegates to Server")

2. ADD the missing Subagent Group boundary inside Core Library:
   - Container_Boundary(subagent_group, "Subagent Group") {
         Component(subagent_coord, "SubagentCoordinator", "", "Parent-child orchestration, nesting limit 5, result aggregation")
     }
   - Rel(agent_orch, subagent_coord, "spawn, collect")  [keep existing label from agent diagram]

3. FIX Memory relationship direction (must match detailed diagram):
   - CHANGE: Rel(memory_store, memory_scope, "filters by scope")
   - TO:     Rel(memory_scope, memory_store, "filters by scope")

4. FIX Edit Strategy inheritance directions (arrows must point from child to parent ABC):
   - CHANGE all 8 lines: Rel(edit_strategy, <coder>, "extends")
   - TO:                 Rel(<coder>, edit_strategy, "extends")
   (Affected coders: search_replace_coder, whole_file_coder, unified_diff_coder, fenced_block_coder, function_level_coder, diff_sandbox_coder, architect_coder, inline_patch_coder)

5. REMOVE or reclassify data-structure components that are not runtime behavioral components:
   - Remove: agent_events, config_schema, session_model, hook_types, hook_result, plugin_manifest, context_node, context_edge, quality_signal, provider_caps, compaction_policy
   - If removal breaks diagram readability, replace with a comment note instead of a Component box.

6. FIX Config naming inconsistency:
   - Ensure all references to the Config group use `config_loader` and `config_schema` (not `config`).
   - In the observability relationships section, change `config` to `config_loader` or `config_schema` as appropriate.

7. REMOVE the orphaned component `context_processor` OR add it to the C4.md inventory table under Context Graph group.

8. NORMALIZE relationship labels to third-person singular present tense where they differ from detailed diagrams (e.g., "rank results" → "ranks search results", "build repo map" → "builds repo map", etc.).

=== CRITICAL: C4 CONTAINER DIAGRAM (docs/C4/c4_nasim_container.puml) ===

1. REPLACE the illegal internal expansion of Observability Platform:
   - REMOVE:
       System_Boundary(obs_platform, "Observability Platform") {
           Container(log_agent, ...)
           Container(loki, ...)
           Container(prometheus, ...)
           Container(grafana, ...)
       }
   - ADD:
       System_Ext(obs_platform, "Observability Platform", "Agent-based collection, Loki logs, Prometheus metrics, Grafana dashboards. nasim emits only.")

2. REMOVE all internal relationships of the observability platform (log_agent→loki, grafana→loki, grafana→prometheus, log_agent→loki push).

3. KEEP only these cross-boundary relationships:
   - Rel(core, obs_platform, "writes structured logs to stdout", "stdout")
   - Rel(obs_platform, core, "pull-scrapes /metrics endpoint", "HTTP GET")

4. Keep System_Ext(otel_collector, ...) as-is.

=== CRITICAL: SM LIFECYCLE-WRITE OWNERSHIP (docs/SM/SM.md) ===

1. In the Session Lifecycle table, CHANGE:
   - Target State CLOSED | Lifecycle-Write UC AGT-14 HANDLE Error
   - TO: Target State CLOSED | Lifecycle-Write UC SSN-01 PERSIST Session (or introduce SSN-09 CLOSE Session and use that)

2. In the Plan Lifecycle table, CHANGE:
   - Target State COMPLETED | Lifecycle-Write UC AGT-01 PROCESS User Task
   - TO: Target State COMPLETED | Lifecycle-Write UC AGT-08 APPROVE Plan

3. In the color syntax note, CHANGE:
   - "SQ diagrams must use `<back:#HEX>STATE</back>` syntax"
   - TO: "State-machine diagrams use `state \"STATE\" as STATE #HEX` syntax per PlantUML standard"

=== HIGH: C4-UC GROUP ALIGNMENT ===

Choose ONE of the following options and apply consistently:

Option A (Recommended):
- Create new file docs/C4/c4_nasim_component_safety.puml with Container_Boundary for Safety Group containing SafetyCoordinator.
- Create new file docs/C4/c4_nasim_component_router.puml with Container_Boundary for Router Group containing ModelRouter, FallbackChain, ProviderCapabilities.
- Update docs/C4/C4.md inventory table to list these two new diagrams.
- Update docs/C4/c4_nasim_component.puml to include Safety and Router groups (remove SafetyCoordinator from Agent group, remove ModelRouter/FallbackChain/ProviderCapabilities from Provider group, or reference them via Component_Ext if duplication is unavoidable).

Option B:
- In all UC .puml files, nest SAF use cases inside the Agent Core package and RTG use cases inside the Provider package. Remove separate SAF and RTG packages.

=== HIGH: C4 SUBAGENT DIAGRAM (docs/C4/c4_nasim_component_subagent.puml) ===

1. Add `@startuml c4_nasim_component_subagent` as the FIRST line (before `!include`).

2. REMOVE the duplicate `AgentOrchestrator` component inside this diagram. Instead, reference the one in the Agent group:
   - Component_Ext(agent_orch, "AgentOrchestrator", "Agent Group")

3. Keep SubagentCoordinator inside the boundary.

=== HIGH: REMOVE DATA-STRUCTURE COMPONENTS FROM DETAILED DIAGRAMS ===

In the following files, remove or reclassify components that are explicitly data types / enums / dataclasses / value objects:

- docs/C4/c4_nasim_component_agent.puml: Remove AgentEvent (event type enum).
- docs/C4/c4_nasim_component_config.puml: Add `@startuml` first. Keep ConfigLoader; Config is a dataclass—remove it or note it as a data type, not a Component.
- docs/C4/c4_nasim_component_session.puml: Add `@startuml` first. Session is a dataclass—remove or reclassify.
- docs/C4/c4_nasim_component_hooks.puml: Add `@startuml` first. Hook and HookResult are types—remove or reclassify.
- docs/C4/c4_nasim_component_plugins.puml: Add `@startuml` first. PluginManifest is metadata—remove or reclassify.
- docs/C4/c4_nasim_component_evaluation.puml: QualitySignal is a result VO—remove or reclassify.
- docs/C4/c4_nasim_component_tools.puml: Remove "ABC" tag from RepoMapTool, SemanticSearchTool, ReviewTool. They are concrete tools extending Tool.

=== MEDIUM: UC OVERVIEW (docs/UC/uc_overview.puml) ===

1. FIX incorrect `<<include>>` relationships:
   - CHANGE `AGT01 ..> PRV01 : <<include>>` TO simple association (no stereotype) or remove.
   - CHANGE `AGT01 ..> TL01 : <<include>>` TO simple association or remove.
   - CHANGE `AGT01 ..> SAF01 : <<include>>` TO simple association or remove.
   - CHANGE `AGT01 ..> WRL01 : <<include>>` TO simple association or remove.
   - CHANGE `AGT01 ..> EDT01 : <<include>>` TO `<<extend>>` (editing is conditional).
   - REMOVE `SRV01 ..> AGT01 : <<include>>` entirely (listing sessions does not invoke the agent).

2. FIX Observability → Wire Log relationship:
   - CHANGE `OBS01 ..> WRL01 : <<extend>>` TO simple association or remove (observability may read wire log, but it does not extend its behavior).

=== MEDIUM: UC EVALUATION (docs/UC/uc_evaluation.puml) ===

1. BREAK the circular dependency:
   - REMOVE the line: `EVL06 ..> EVL01 : <<extend>>`

=== MEDIUM: UC SERVER (docs/UC/uc_server.puml) ===

1. FIX semantic mismatches:
   - SRV-05 RETIRE Session includes SSN-01 PERSIST Session → this is wrong. Retiring is deletion, not persistence. Introduce a new UC `SSN-09 DELETE Session` and change SRV-05 to include SSN-09 instead.
   - SRV-07 LIST Messages includes SSN-07 SEARCH Sessions → this is wrong. Change to `SRV07 ..> SSN02 : <<include>>` (READ Session to get messages).

=== MEDIUM: UC CLI (docs/UC/uc_cli.puml) ===

1. FIX backwards flow:
   - CHANGE `CLI04 ..> CLI01 : <<include>>` TO `CLI01 ..> CLI04 : <<include>>` (process input includes reading args, not the reverse).

=== MEDIUM: C4 SANDBOX DIAGRAM (docs/C4/c4_nasim_component_sandbox.puml) ===

1. FIX layer violation:
   - REMOVE: `Rel(diff_presenter, staged_applicator, "triggers apply after approval")`
   - ADD: `Rel(diff_presenter, diff_sandbox_mgr, "notifies approval")`
   - ADD: `Rel(diff_sandbox_mgr, staged_applicator, "applies approved diffs")`

=== MEDIUM: C4 EVALUATION DIAGRAM (docs/C4/c4_nasim_component_evaluation.puml) ===

1. BREAK circular dependencies:
   - REMOVE: `Rel(retry_coordinator, eval_engine, "triggers re-evaluation")`
   - REMOVE: `Rel(repetition_detector, eval_engine, "signals escalation")`
   - If needed, replace with event-based semantics or note that EvaluationEngine polls/subscribes to them.

2. FIX contradictory relationship:
   - CHANGE `Rel(llm_reviewer, quality_signal, "consumes")`
   - TO either:
     a) `Rel(quality_signal, llm_reviewer, "consumed by")`  [if reviewer consumes it], OR
     b) `Rel(llm_reviewer, quality_signal, "produces")`      [if reviewer creates it]

=== MEDIUM: C4 OBSERVABILITY DIAGRAM (docs/C4/c4_nasim_component_observability.puml) ===

1. FIX Config references:
   - CHANGE all `config` references in relationships to `config_loader` (for orchestration/loading) or `config_schema` (for schema access).

=== MEDIUM: C4 MEMORY DIAGRAM (docs/C4/c4_nasim_component_memory.puml) ===

1. REMOVE duplicate relationship:
   - Remove ONE of the two `Rel(working_mem_adapter, memory_store, ...)` lines.

=== LOW: SYNTAX FIXES FOR MISSING @startuml ===

Add `@startuml <diagram_id>` as the absolute first line in these files:

- docs/C4/c4_nasim_component_agent.puml      → @startuml c4_nasim_component_agent
- docs/C4/c4_nasim_component_config.puml     → @startuml c4_nasim_component_config
- docs/C4/c4_nasim_component_plugins.puml    → @startuml c4_nasim_component_plugins
- docs/C4/c4_nasim_component_hooks.puml      → @startuml c4_nasim_component_hooks
- docs/C4/c4_nasim_component_git.puml        → @startuml c4_nasim_component_git
- docs/C4/c4_nasim_component_cli.puml        → @startuml c4_nasim_component_cli
- docs/C4/c4_nasim_component_mcp.puml        → @startuml c4_nasim_component_mcp

=== LOW: C4 CONTAINER BOUNDARY STYLE (all component diagrams) ===

Inside `c4_nasim_component.puml`, the internal groupings currently use `Container_Boundary`. For strict C4 compliance, internal logical groupings inside a Component diagram should use `Boundary` (not `Container_Boundary`). `Container_Boundary` is reserved for the single container being expanded. Update all internal boundaries in the overview to use `Boundary`.

=== VERIFICATION CHECKLIST ===

After editing, verify:
- [ ] `c4_nasim_component.puml` contains NO relationships from `agent_orch` to `cli_container` or `server_container`.
- [ ] `c4_nasim_container.puml` contains NO `System_Boundary` for Observability Platform.
- [ ] All `.puml` files start with `@startuml`.
- [ ] `SM.md` lifecycle-write table shows Session.CLOSED owned by a Session UC (not AGT-14).
- [ ] No data-class components (Config, Session, AgentEvent, etc.) appear as `Component(...)` boxes.
- [ ] Edit-strategy inheritance arrows in overview point from child to parent.
- [ ] `uc_overview.puml` has no `<<include>>` from AGT-01 to PRV-01, TL-01, SAF-01, WRL-01, EDT-01.
- [ ] `uc_evaluation.puml` has no circular dependency between EVL-01 and EVL-06.
- [ ] `c4_nasim_component_sandbox.puml` has no direct `DiffPresenter → StagedApplicator` relationship.
```

This prompt gives the agent exact file paths, line semantics, and a verification checklist so you can validate the output.