# Autonomous CAR Audit & Fix Prompt for nasim C4 Diagrams

You are an autonomous architecture agent. Your task is to **measure, validate, and fix** the C4 diagrams in `/home/salim/prj/salim/nasim/code/nasim/docs/C4/` to align them with the project's architectural principles (p1.md) and the comprehensive audit findings.

**Objective**: Execute the following CAR (Challenge-Action-Result) tasks sequentially. For each task, measure the current state, apply the fix, and verify the result.

---

## CAR 1: Missing External Systems in Context & Container Diagrams

**Challenge (Measure)**: 
- The component diagrams (`c4_nasim_component_repo_intelligence.puml` and `c4_nasim_component_observability.puml`) reference `Tree-sitter`, `Embedding Model`, and `Vector Store` as external systems.
- However, these are **missing** from the high-level Context (`c4_nasim_context.puml`) and Container (`c4_nasim_container.puml`) diagrams. This breaks the C4 parent-child traceability rule (every external system in Level 3/4 must appear in Level 1/2).

**Action (Fix)**:
1. **Edit** `c4_nasim_context.puml`.
   - Add these 3 `System_Ext` definitions near the other external systems (e.g., after `lsp_server`).
   ```plantuml
   System_Ext(tree_sitter, "Tree-sitter", "Incremental AST parser for source code")
   System_Ext(embedding_model, "Embedding Model", "Code embedding generation (local or API)")
   System_Ext(vector_store, "Vector Store", "Persistent vector storage for similarity search")
   ```
   - Add 3 `Rel` relationships from `nasim` to these systems:
   ```plantuml
   Rel(nasim, tree_sitter, "parses source code AST", "tree-sitter CLI/lib")
   Rel(nasim, embedding_model, "generates code embeddings", "HTTP/local")
   Rel(nasim, vector_store, "stores/retrieves embeddings", "SQLite + vector")
   ```

2. **Edit** `c4_nasim_container.puml`.
   - Add the exact same 3 `System_Ext` definitions.
   - Add 3 `Rel` relationships from `core` (the Core Library container) to these systems:
   ```plantuml
   Rel(core, tree_sitter, "parses AST")
   Rel(core, embedding_model, "generates embeddings")
   Rel(core, vector_store, "vector CRUD")
   ```

**Result (Verify)**:
- Run `grep -E "System_Ext.*tree_sitter|embedding_model|vector_store" c4_nasim_context.puml c4_nasim_container.puml`. Each file must return 3 matches.
- Run `plantuml -tsvg c4_nasim_context.puml` and `plantuml -tsvg c4_nasim_container.puml` to ensure no syntax errors.

---

## CAR 2: Incomplete Edit Strategy Detail Diagram

**Challenge (Measure)**: 
- The overview diagram (`c4_nasim_component.puml`) lists 10 edit strategies: `SearchReplaceCoder`, `WholeFileCoder`, `UnifiedDiffCoder`, `FencedBlockCoder`, `FunctionLevelCoder`, `DiffSandboxCoder`, `ArchitectCoder`, `InlinePatchCoder`, plus the `EditStrategy` ABC and `StrategySelector`.
- However, the detail diagram (`c4_nasim_component_edit_strategy.puml`) **only contains 5** strategies, missing `FencedBlockCoder`, `FunctionLevelCoder`, and `InlinePatchCoder`.

**Action (Fix)**:
- **Edit** `c4_nasim_component_edit_strategy.puml`.
- Inside the `Container_Boundary(edit_strategy_boundary, "Edit Strategy Group")`, add the 3 missing components:
```plantuml
    Component(fenced_block_coder, "FencedBlockCoder", "", "Fenced code block format edit")
    Component(function_level_coder, "FunctionLevelCoder", "", "AST-targeted function replacement")
    Component(inline_patch_coder, "InlinePatchCoder", "", "apply-patch format edit")
```
- Add the corresponding `extends` relationships after the existing ones:
```plantuml
Rel(fenced_block_coder, edit_strategy, "extends")
Rel(function_level_coder, edit_strategy, "extends")
Rel(inline_patch_coder, edit_strategy, "extends")
```
- Add relationships to `host_fs` for file application:
```plantuml
Rel(fenced_block_coder, host_fs, "applies edits")
Rel(function_level_coder, host_fs, "AST-aware edits")
Rel(inline_patch_coder, host_fs, "applies patch")
```

**Result (Verify)**:
- Run `grep -c "Component.*Coder" c4_nasim_component_edit_strategy.puml`. The count must be **8** (SearchReplace, WholeFile, UnifiedDiff, DiffSandbox, Architect, FencedBlock, FunctionLevel, InlinePatch).
- Ensure no duplicate `extends` relations with the ABC.

---

## CAR 3: Missing CLI Component Source File

**Challenge (Measure)**: 
- The README inventory lists `c4_nasim_component_cli.puml` as a valid per-group diagram.
- This file is **absent** from the repository (not provided in the source list). This is a blocker for users navigating the C4 set.

**Action (Fix)**:
- **Create** `c4_nasim_component_cli.puml` in the `docs/C4/` directory.
- Use the exact structure from the other component diagrams, referencing `c4_nasim_component_cli.puml` content from the source block above (it was defined but not in the list? Actually, the user *did* provide the source for it at the bottom of the file content! It's there, I see `--- SOURCE: /home/salim/.../c4_nasim_component_cli.puml ---` at the very bottom. Wait, the user said "source not shown" in the previous audit, but the *provided file content* actually includes it. Let me scroll up. Yes, the final block is `c4_nasim_component_cli.puml`. So the file *does* exist in the repository. The "missing" was an audit observation based on the user's *list* vs *source*, but the source is there. **Correction for Agent**: The file exists. The agent should skip creation, but verify it exists. Instead, I will change this CAR to **verify and validate** the CLI diagram against the `core_library` dependency.
- *Wait*, the audit said `c4_nasim_component_cli.puml` source is not shown. Actually, looking at the provided text, it IS shown. I'll change this to a verification task. **Revise CAR 3 to: Ensure CLI diagram properly delegates to Core Library**.
- **Edit** `c4_nasim_component_cli.puml` (it already has the correct `Rel(repl_session, core_library, "delegates to agent orchestrator")`). No action needed, just verify.

**Result (Verify)**:
- Confirm the file exists and contains the correct relationships to `Container_Ext(core_library, "Core Library")`.

---

## CAR 4: Overloaded Provider Diagram (Reduce Cognitive Load)

**Challenge (Measure)**: 
- `c4_nasim_component_provider.puml` lists **24 individual external system boxes** (OpenAI, Anthropic, Google, etc.). This creates a "spaghetti" of lines that obscures the main architecture (LiteLLM proxy). 
- The principle of C4 is to show *architecturally significant* details, not exhaustive configuration.

**Action (Fix)**:
- **Edit** `c4_nasim_component_provider.puml`.
- **Remove** all the individual `System_Ext(openai_api, ...)` through `System_Ext(litellm_server, ...)` blocks (keep the `anthropic_api`, `openai_api`, `google_api`, `ollama_api` as representative top-tier providers).
- **Replace** the removed 20+ blocks with a single comprehensive `System_Ext`:
```plantuml
System_Ext(all_llm_backends, "100+ LLM Backends (OpenAI, Anthropic, Google, DeepSeek, etc.)", "Unified via litellm. Includes local servers (Ollama, vLLM, LM Studio) and cloud providers (Bedrock, Vertex, Groq, Together, Cerebras, DeepInfra, etc.)")
```
- **Replace** the massive list of `Rel(litellm_proxy, xxx_api, "routes to provider")` with a **single** relationship:
```plantuml
Rel(litellm_proxy, all_llm_backends, "routes via model string prefix", "HTTP/JSON")
```
- Keep the remaining structure (`Provider`, `ModelRouter`, `FallbackChain`, etc.) intact.

**Result (Verify)**:
- The number of `System_Ext` declarations in the file should drop from ~25 to ~4.
- `plantuml -tsvg` must render without errors. The diagram should now visually emphasize the routing logic (Router, Fallback, Capabilities) rather than the long tail of providers.

---

## CAR 5: Evaluation Group Missing `QualitySignal` Component

**Challenge (Measure)**: 
- `c4_nasim_component.puml` (overview) defines `Component(quality_signal, "QualitySignal", "", "accept: bool | float, feedback: str")`.
- `c4_nasim_component_evaluation.puml` (detail) does **not** contain this component, breaking the parent-child traceability rule.

**Action (Fix)**:
- **Edit** `c4_nasim_component_evaluation.puml`.
- Inside the `Container_Boundary(evaluation_boundary, ...)`, add the missing component:
```plantuml
    Component(quality_signal, "QualitySignal", "", "Result: accept (bool | float), feedback (str)")
```
- Add a relationship showing where it flows:
```plantuml
Rel(eval_engine, quality_signal, "produces")
Rel(llm_reviewer, quality_signal, "consumes")
```

**Result (Verify)**:
- `grep -c "QualitySignal" c4_nasim_component_evaluation.puml` must return **at least 1**.

---

## CAR 6: Observability Missing Critical Link to CLI Renderer

**Challenge (Measure)**: 
- `c4_nasim_component_observability.puml` defines `DualOutputAdapter` (responsible for writing JSON to stdout and human-readable output when TTY).
- However, it **does not** show the relationship to the CLI's `Renderer` component (defined in `c4_nasim_component_cli.puml`). This creates an architectural blind spot for the human output path.

**Action (Fix)**:
- **Edit** `c4_nasim_component_observability.puml`.
- Since `Renderer` is in a different container boundary (CLI Process), we must use `Container_Ext` or `Component_Ext`. Add the following after the existing relationships:
```plantuml
Container_Ext(cli_renderer, "CLI Renderer", "Rich terminal output engine")
Rel(dual_output_adapter, cli_renderer, "delegates human-readable output when isatty")
```
- *Alternatively*, if you want to keep the CLI diagram pure, add a note: `Note: DualOutputAdapter delegates to CLI Renderer for human rendering when TTY detected.`

**Result (Verify)**:
- Verify the `Rel` line exists in the file.
- Ensure the diagram still renders the "tenas pattern" (emit-only) correctly.

---

## CAR 7: (Stretch) Merge ProviderCapabilities & ModelCatalog

**Challenge (Measure)**: 
- `ProviderCapabilities` (runtime: streaming, tools, vision) and `ModelCatalog` (static: context limits, pricing) have overlapping concerns (context window, token limits).
- This causes potential duplication of metadata.

**Action (Fix)**:
- **Edit** `c4_nasim_component_provider.puml`.
- **Rename** `ModelCatalog` to `ModelMetadata` or merge it into `ProviderCapabilities` (add `pricing` and `static_limits` to the description).
- For simplicity, remove `ModelCatalog` and add a note to `ProviderCapabilities`: `"Capability declaration + static metadata (context limits, pricing)"`.
- Update the `Rel(model_router, provider_caps, "queries capabilities & metadata")` to replace the old `Rel(model_router, model_catalog, ...)`.

**Result (Verify)**:
- `plantuml -tsvg` renders without unresolved references.

---

## CAR 8: Add `ResourceLimiter` to Sandbox Group (Security Hardening)

**Challenge (Measure)**: 
- The Sandbox diagram mentions `resource limits` in the description of `SandboxMonitor`, but there is no specific component enforcing CPU, memory, or disk quotas.
- This is a security gap in the design documentation.

**Action (Fix)**:
- **Edit** `c4_nasim_component_sandbox.puml`.
- Add:
```plantuml
    Component(resource_limiter, "ResourceLimiter", "", "Enforces CPU, memory, and disk quotas per sandbox instance")
```
- Add relationships:
```plantuml
Rel(sandbox_executor, resource_limiter, "configures limits before exec")
Rel(sandbox_policy, resource_limiter, "defines quota rules")
Rel(resource_limiter, host_fs, "enforces disk quotas")
Rel(resource_limiter, host_shell, "enforces CPU/memory via cgroups/rlimits")
```

**Result (Verify)**:
- The Sandbox group now has a clear component for resource enforcement, completing the security isolation narrative.

---

# Execution Order & Final Validation

Run these tasks in order: **1, 2, 4, 5, 6, 7, 8**. (Skip 3 if the file exists).

**Final Global Validation**:
1. Run `bash scripts/lint.sh` (if available) to check code style for any generated markdown/plantuml.
2. Render all diagrams: `for f in docs/C4/*.puml; do plantuml -tsvg $f; done`. Ensure zero errors.
3. Verify counts in the inventory: 18 groups, no orphan components (all `Component` declarations in detail files must appear in `c4_nasim_component.puml`).

**Measurement Report**:
After completing the tasks, generate a `docs/audit/c4_fix_report.md` containing a checklist of these CAR tasks and the verification results (e.g., "CAR 1: Passed - 3 systems added").

Begin execution.