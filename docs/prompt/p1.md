🔧 MASTER ARCHITECTURE CORRECTION PROMPT (10/10 MODE)

You are acting as a Staff-level Software Architect + Documentation Integrity Engine.
Your task is to perform a full architectural audit, correction, and normalization of the entire system design and all C4 diagrams.

1. OBJECTIVE

You must bring the architecture and documentation quality to 10/10 production-grade standard, meaning:

Strict adherence to C4 model semantics
No conceptual leakage between layers
No missing or inconsistent components across diagrams
No duplicated or drifting definitions
Clear separation of:
Context
Containers
Components
Code-level concerns

You are not allowed to “approximate” or “handwave” architecture.

2. CORE TASKS
A. Global Consistency Audit

Scan all diagrams and identify:

Containers that should not exist (non-deployable concepts)
Components incorrectly modeled as containers (or vice versa)
Missing containers or missing subsystems (e.g., MCP, Plugins, Hooks, Server, Tools)
Broken traceability between:
System Context → Container → Component diagrams
Duplicate responsibilities across layers
Orchestrator “God Object” violations
B. Normalize Architecture (MANDATORY)

Refactor architecture to enforce:

1. Single Responsibility per layer
CLI = interface only
Server = transport only
Agent = orchestration only
Tools = execution only
Provider = inference only
Plugins = extensibility only
Session = persistence only
Config = configuration only
2. No cross-layer logic leakage

If a component performs multiple responsibilities:

split it
or introduce an intermediate service layer
C. MCP Architecture Correction

You MUST explicitly model MCP as a first-class subsystem:

MCP Client Runtime
MCP Server Runtime
MCP Tool Adapter
MCP Discovery Layer

Ensure it is NOT hidden inside Tools or Server.

D. Diagram Synchronization Rule

Every change must satisfy:

Every container MUST have a corresponding component diagram
Every component must belong to exactly one container
No orphan components across diagrams
No duplicated naming with different meanings
E. Remove Architectural Smells

Eliminate:

God objects (especially AgentOrchestrator)
Mixed abstraction diagrams (logical + physical + component in same graph)
Implementation leakage (Python class names unless necessary)
“Future scope” elements inside core architecture diagrams
3. REQUIRED OUTPUT FORMAT

You must output:

1. Problem Report
List all architectural violations
Categorize by severity:
Critical (breaks C4 correctness)
Major (design inconsistency)
Minor (naming/clarity)
2. Corrected Target Architecture

Provide:

Clean System Context
Clean Container Model
Clean Component decomposition per container
3. Refactored Responsibility Map

Explicit mapping:

Capability → Owner Container → Internal Components

Example format:

Tool execution → Tool Layer → ToolExecutor, ToolRegistry
LLM inference → Provider Layer → Provider interface implementations
Session persistence → Session Layer → SessionStore
4. Updated C4 Rules (NEW STANDARD)

Write a formal set of rules that will become the system’s “architecture constitution”, including:

What qualifies as a container
What qualifies as a component
What must never be mixed
Naming conventions
Dependency direction rules
5. Knowledge Base Update Instruction

Generate a clean, canonical knowledge update for:

~/.claude/architecture-knowledge.md

It must include:

Final architecture model
Layer responsibilities
MCP role definition
Tooling boundaries
Diagram consistency rules

This file becomes the single source of truth for future design decisions.

4. STRICT CONSTRAINTS
Do NOT introduce new features unless required to fix inconsistencies
Do NOT merge layers to “simplify”
Do NOT omit MCP modeling
Do NOT leave any container without components
Do NOT allow ambiguous ownership
5. SUCCESS CRITERIA

You achieve 10/10 only if:

All diagrams are consistent and non-contradictory
No mixed abstraction levels exist
Every subsystem has a clear owner
MCP is first-class and fully modeled
No God objects remain
Architecture is fully C4-compliant and traceable