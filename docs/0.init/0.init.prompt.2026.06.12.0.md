I want to develop a terminal-native AI coding assistant that functions as a local 'sibling' to Claude Code. Please provide a technical blueprint for writing this application based on the following requirements:
Architecture: The app must follow a Client-Server model where the 'Client' (the CLI) runs on a local machine and communicates with a 'Server' (running an LLM via Ollama or LM Studio, )
.
API Compatibility: The application must be designed to speak the Anthropic Messages format natively so that it can eventually serve as a drop-in replacement or redirect for existing tools
.
Environment Redirection: Explain how to implement a system that uses environment variables (like ANTHROPIC_BASE_URL and ANTHROPIC_API_KEY) to toggle between the official Anthropic API and a local server IP
.
Local Knowledge Repo: The app needs to utilize a local directory for state, history, and context, similar to the ~/.claude/ knowledge repo. How should I structure this local database/filesystem for maximum performance?
Tool Use & The Bridge: Provide a plan for implementing a translation proxy or 'Bridge' that allows the local LLM to perform advanced tasks like filesystem edits and shell commands, similar to how claude-proxy handles tool use
.
Technical Stack Recommendations: Based on existing tools like Aider, Cline, or OpenCode, what language (e.g., Node.js, Python, Rust) and libraries would you recommend for handling real-time terminal output and secure shell execution
?
Please format the response as a structured blueprint with steps for initial setup, API integration, and local state management
."