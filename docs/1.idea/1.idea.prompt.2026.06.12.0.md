"I am developing Project Nasim, a terminal-native AI coding assistant designed as a privacy-focused, local-first 'sibling' to Claude Code. Please provide an engineering-grade technical blueprint for this application based on the following architectural requirements:
1. High-Level Architecture (The Triad)
Client (salim-hp): A terminal UI (TUI) that manages local state, terminal I/O, and user interactions.
Server (black): A high-performance machine running a local LLM (via Ollama or LM Studio) and a Translation Bridge.
The Bridge: A proxy layer that translates the Anthropic Messages format natively to local LLM schemas, handling complex tool-calling and response normalization
.
2. Client-Side Implementation (~/.nasim/)
Local Knowledge Repo: Structure a local directory mirroring ~/.claude/. Implement a Hybrid Storage Approach: use JSONL for append-only, crash-resistant conversation logs and SQLite with FTS5 for sub-millisecond keyword search across history
.
Context Assembly: Logic to load CLAUDE.md at every session start and inject it into the system prompt to maintain coding persona and project-specific memory
.
Redirection Logic: Implement a drop-in replacement system using environment variables. If ANTHROPIC_BASE_URL is set to the Nasim Bridge IP and ANTHROPIC_API_KEY is a non-empty placeholder (e.g., 'local'), the client must route all traffic locally
.
3. Knowledge Migration Engine (Import/Export)
The Import Layer: Develop a system to parse existing ~/.claude/ data (sessions, state, and CLAUDE.md). Detail the transformation logic to convert Claude's JSON history into Nasim's high-performance JSONL format, including deduplication via content-hashing
.
The Export Layer: Create a reverse-migration path to push Nasim’s local insights back into the official .claude/ repo. This must include updating the official state.db and merging refinements to CLAUDE.md so the two tools remain synchronized peers.
4. Server-Side & Bridge Capabilities
The Agentic Tool Loop: Describe how the Bridge intercepts tool-use requests (filesystem edits, shell commands, git operations)
.
Security & 'Plan Mode': Implement a 'Human-in-the-loop' confirmation gate for destructive operations and a Command Allow-list to prevent restricted shell access (e.g., blocking rm -rf / or access to ~/.ssh/)
.
Native Compatibility: Ensure the server exposes a /v1/messages endpoint that adheres strictly to the Anthropic Messages API schema to allow other tools (like the official Claude Code CLI) to use Nasim as a backend
.
5. Recommended Technical Stack
Evaluate a Node.js/TypeScript + Ink stack (for UI parity and SDK alignment) versus a Rust + Ratatui stack (for zero-dependency, high-throughput performance)
.
Recommend libraries for Secure Shell Execution (e.g., execa or node-pty) and Streaming SSE handling
.
Please format this as a structured roadmap with phases for Foundation, Agentic Tooling, Knowledge Migration, and Production Polish."