@startuml sq_contextgraph06_track_token_budget
' ============================================================
' Title:     CTX-06 — TRACK Token Budget
' Boundary:  nasim code agent
' Purpose:   Track token consumption against allocated budget
' Milestone: v1.0
' Version:   9.1.0
' Source:    docs/UC/README.md, docs/SM/README.md
' Review:    —
' CSR Chain: User -> CLI Process -> ServerRouter -> TokenBudgetTracker
' SM States: (stateless — COMPACTING owned by AGT-06)
' ============================================================

!include ../common/sq_styles.puml

title nasim -- CTX-06 TRACK Token Budget

actor "User" as user

box "Interface" INTERFACE_COLOR
  participant "CLI Process" as iface
end box

box "API" API_COLOR
  participant "ServerRouter" as router
end box

box "Service" SERVICE_COLOR
  participant "TokenBudgetTracker" as tracker
end box

== CTX-06 TRACK Token Budget ==

user -> iface : CTX-06 TRACK Token Budget({used_tokens, total_budget})
activate iface
iface -> router : CTX-06 TRACK Token Budget({used_tokens, total_budget})
activate router
router -> tracker : CTX-06 TRACK Token Budget({used_tokens, total_budget})
activate tracker

alt budget_within_limits
  ref over tracker : CTX-06 TRACK Token Budget({used_tokens, total_budget})
  tracker --> router : 200 OK
  deactivate tracker
  router --> iface : 200 OK
  iface --> user : 200 OK
  deactivate router
  deactivate iface
else budget_exceeded
  tracker --> router : 429 TOO_MANY_REQUESTS\n{error: {code: "CTX-06_BUDGET_EXCEEDED", message: "Token budget exceeded", status: 429}}
  deactivate tracker
  router --> iface : 429 TOO_MANY_REQUESTS
  iface --> user : 429 TOO_MANY_REQUESTS
  deactivate router
  deactivate iface
end

@enduml
