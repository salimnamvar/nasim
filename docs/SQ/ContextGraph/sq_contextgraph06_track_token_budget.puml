@startuml sq_contextgraph06_track_token_budget
' ============================================================
' Title:     CONTEXTGRAPH-06 — TRACK Token Budget
' Boundary:  nasim code agent
' Purpose:   Track token consumption against allocated budget
' Milestone: v1.0
' Version:   9.1.0
' Source:    docs/UC/README.md, docs/SM/README.md
' Review:    —
' CSR Chain: User -> CLIAdapter -> AgentController -> TokenBudgetTracker
' SM States: (stateless — COMPACTING owned by AGENT-06)
' ============================================================

!include ../common/sq_styles.puml

title nasim -- CONTEXTGRAPH-06 TRACK Token Budget

actor "User" as user

box "Interface" INTERFACE_COLOR
  participant "CLIAdapter" as iface
end box

box "API" API_COLOR
  participant "AgentController" as router
end box

box "Service" SERVICE_COLOR
  participant "ContextService" as tracker
end box

== CONTEXTGRAPH-06 TRACK Token Budget ==

user -> iface : CONTEXTGRAPH-06 TRACK Token Budget({used_tokens, total_budget})
activate iface
iface -> router : CONTEXTGRAPH-06 TRACK Token Budget({used_tokens, total_budget})
activate router
router -> tracker : CONTEXTGRAPH-06 TRACK Token Budget({used_tokens, total_budget})
activate tracker

alt budget_within_limits
  ref over tracker : CONTEXTGRAPH-06 TRACK Token Budget({used_tokens, total_budget})
  tracker --> router : 200 OK
  deactivate tracker
  router --> iface : 200 OK
  iface --> user : 200 OK
  deactivate router
  deactivate iface
else budget_exceeded
  tracker --> router : 429 TOO_MANY_REQUESTS\n{error: {code: "CONTEXTGRAPH-06_BUDGET_EXCEEDED", message: "Token budget exceeded", status: 429}}
  deactivate tracker
  router --> iface : 429 TOO_MANY_REQUESTS
  iface --> user : 429 TOO_MANY_REQUESTS
  deactivate router
  deactivate iface
end

@enduml
