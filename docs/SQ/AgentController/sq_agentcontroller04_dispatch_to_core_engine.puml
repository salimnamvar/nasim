@startuml sq_ac04_dispatch_to_core_engine
' ============================================================
' Title:     AGENTCONTROLLER-04 — DISPATCH to Core Engine
' Boundary:  nasim code agent
' Purpose:   Forward validated request to Task Service
' Milestone: v1.0
' Version:   9.1.0
' Source:    docs/UC/README.md, docs/SM/README.md
' Review:    —
' CSR Chain: User -> InterfaceAdapter -> Agent Controller -> Task Service
' SM States: LISTENING (#E8EAF6), THINKING (#D7CCC8), IDLE (#ECEFF1)
' ============================================================

!include ../common/sq_styles.puml

title nasim -- AGENTCONTROLLER-04 DISPATCH to Core Engine

actor "User" as user

box "Interface" INTERFACE_COLOR
  participant "CLI Adapter" as iface
end box

box "API" API_COLOR
  participant "Agent Controller" as router
end box

box "Service" SERVICE_COLOR
  participant "Task Service" as engine
end box

== AGENTCONTROLLER-04 DISPATCH to Core Engine ==

user -> iface : AGENTCONTROLLER-04 DISPATCH to Core Engine({validated_payload})
activate iface

iface -> router : AGENTCONTROLLER-04 DISPATCH to Core Engine({validated_payload})
activate router

router -> engine : AGENTCONTROLLER-04 DISPATCH to Core Engine({validated_payload})
activate engine

alt dispatch successful
  ref over engine
    TASKSERVICE-01: PROCESS User Task
  end ref

  engine --> router : 200 OK — {task_result}
  deactivate engine
  router --> iface : 200 OK — {task_result}
  deactivate router
  iface --> user : 200 OK — {task_result}
  deactivate iface

else engine unavailable
  engine --> router : 503 SERVICE_UNAVAILABLE\n{error: {code: "AGENTCONTROLLER-04_DISPATCH_ERROR", message: "Core engine unavailable", status: 503}}
  deactivate engine
  router --> iface : 503 SERVICE_UNAVAILABLE
  deactivate router
  iface --> user : 503 SERVICE_UNAVAILABLE
  deactivate iface
end

@enduml
