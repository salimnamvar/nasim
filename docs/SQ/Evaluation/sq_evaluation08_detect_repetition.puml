@startuml sq_evaluation08_detect_repetition
' ============================================================
' Title:     EVL-08 — DETECT Repetition
' Boundary:  nasim code agent
' Purpose:   Detect repetitive tool call or response patterns
' Milestone: v1.0
' Version:   9.1.0
' Source:    docs/UC/README.md, docs/SM/README.md
' Review:    —
' CSR Chain: User -> CLI Process -> ServerRouter -> RepetitionDetector
' SM States: CHECKING (#C5CAE9)
' ============================================================

!include ../common/sq_styles.puml

title nasim -- EVL-08 DETECT Repetition

actor "User" as user

box "Interface" INTERFACE_COLOR
  participant "CLI Process" as iface
end box

box "API" API_COLOR
  participant "ServerRouter" as router
end box

box "Service" SERVICE_COLOR
  participant "RepetitionDetector" as detector
end box

== EVL-08 DETECT Repetition ==

user -> iface : GET /evaluation/repetition
activate iface
iface -> router : GET /evaluation/repetition
activate router
router -> detector : EVL-08 DETECT Repetition({task_id, recent_calls})
activate detector

alt no repetition
  ref over detector : TRANSITION State(<back:#C5CAE9>CHECKING</back>)
  detector --> router : 200 OK
  deactivate detector
  router --> iface : 200 OK
  iface --> user : 200 OK
  deactivate router
  deactivate iface
else repetition detected
  detector --> router : 409 Conflict
  deactivate detector
  router --> iface : 409 Conflict
  iface --> user : 409 Conflict
  deactivate router
  deactivate iface
end

@enduml
