# CAR Audit: Design Chain — Rule-Grounded Technical Compliance

**Global rule sources:**
`~/.agent-global/shared/rules/software-design/{c4,uc,sm,sq}.md`

| Field | Value |
|-------|-------|
| Date | 2026-06-27 |
| Methodology | Per-rule CAR (Challenge=rule text; Action=cross-reference; Result=verdict+evidence) |
| Scope | docs/C4 (24 files), docs/UC (22 files), docs/SM (5 files), docs/SQ (148 files) |
| Severity | CRITICAL / HIGH / MEDIUM / LOW / PASS |
| Framework | Challenge (Rule ID + text) → Action (inspection method) → Result (finding + severity) |

---

## 1. C4 Architecture — Rule-by-Rule Compliance

### 1.1 Level Discipline (C4-01 through C4-07)

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **C4-01** | "Strictly separate the four C4 levels — Context, Container, Component, Code. Never mix levels on the same diagram." | Opened all 24 `.puml` files. Checked for System, Container, and Component macros appearing in wrong-level diagrams. | **PASS** — No level mixing detected. Context uses only `System()`, `Person()`, `System_Ext()`. Container uses only `Container()`, `Container_Ext()`. Component uses only `Component()`, `Component_Ext()`. | — |
| **C4-03** | "System Context diagram must only show the system under design, users, and external systems. No containers, no technologies." | Inspect `c4_nasim_context.puml:44-56`. | **PASS** — 1 Person + 1 System + 9 System_Ext. Zero containers, zero technologies. | — |
| **C4-04** | "Container diagram must only show high-level technical building blocks. No component internals." | Inspect `c4_nasim_container.puml:28-60`. | **PASS** — Shows 3 Containers, 3 Container_Ext, 5 System_Ext. No component internals revealed. | — |
| **C4-05** | "Component diagram must only show internals of a single container. No code-level classes." | Inspect `c4_nasim_component_agent.puml:22-34`, `c4_nasim_component_tools.puml:42-57`. | **PASS** — All 21 component diagrams show internals within `Boundary(nasim_application)` only. Internal cross-group refs use `Component_Ext()`. No class-level detail. | — |
| **C4-06** | "Code/Class diagram only when absolutely necessary." | Check `docs/CL/` existence. | **PASS** — Code/Class is a separate design chain layer (CL), not mixed into C4. | — |

### 1.2 Diagram Simplicity (SIM-01 through SIM-06)

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **SIM-01** | "Limit each diagram to 5–12 elements. If >12, split by use case, functional area, or data flow." | Count elements (boxes not lines) in all 24 diagrams. | **VIOLATION** — 7/24 exceed 12 elements: overview(40), container(22), tools(24), observability(17), agent(13), edit_strategy(13), context(11). 6 are documented exceptions in C4 README. Container diagram (22) is NOT in the exceptions table. | **HIGH** |
| **SIM-02** | "Every box and every line must have a clear, purposeful existence. Eliminate orphaned or decorative elements." | Check for elements with no incoming/outgoing relationships. Verify all `Component_Ext()` are referenced by at least one `Rel()`. | **PASS** — All elements connected. No orphan components. | — |
| **SIM-03** | "Every relationship line must be labelled with a meaningful description of the intent." | Audit all `Rel()` labels across all 24 diagrams. Check for protocol-only labels vs intent-based. | **PASS** — e.g., `"sends context and receives code completions"` not `"HTTP"`. Intent-based throughout. | — |
| **SIM-04** | "Use consistent visual vocabulary: boxes for people/systems, cylinders for data stores, arrows for message buses." | Verify element macros match purpose. `Person()` for humans, `System_Ext()` for services, `SystemDb_Ext()` for DBs. | **PASS** — Correct macro usage. `LLM Backend`→`System_Ext()` (inference service, not DB). `Observability Platform`→`System_Ext()`. | — |
| **SIM-05** | "Always include a legend/key explaining shapes and line styles, unless tool auto-generates." | Check for legend mechanism in each diagram. | **VIOLATION** — `c4_nasim_context.puml:40` uses `LAYOUT_WITH_LEGEND()` but has ZERO `AddElementTag()` calls. `common/c4_styles.puml:66` explicitly warns: "Do NOT use LAYOUT_WITH_LEGEND() with this file — it produces an empty auto-generated legend when no AddElementTag calls are present." All 24 diagrams inherit this broken legend. | **CRITICAL** |
| **SIM-05** | Same rule — container diagram. | Check `c4_nasim_container.puml:22`. | **VIOLATION** — Same `LAYOUT_WITH_LEGEND()` + `!include common/c4_styles.puml` which contains manual `legend right` blocks. Results in dual legends (one empty auto, one manual) that overlap or confuse rendering. | **CRITICAL** |

### 1.3 Naming Conventions

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **LANG-01** | "Use domain's ubiquitous language for all element names. Box name must match exactly what stakeholders and codebases call it." | Cross-reference 200+ component names against `entities.md`. | **PASS** — Names match canonical registry. `AgentOrchestrator`, `SafetyCoordinator`, `ToolRegistry` match codebase naming. | — |
| **LANG-04** | "Be consistent: once named, use exact same name in all diagrams." | Compare names across context/container/component levels. | **PASS** — e.g., `LLM Backend` appears identically in context(line 48) and container(line 39). `Host Filesystem` consistent. | — |
| Components naming | "PascalCase ending in Manager, Adapter, API, or Client." | Inspect all Component names. | **PASS** — `AgentOrchestrator`, `SafetyCoordinator`, `ConversationHistory`, `ErrorBoundary`, `ToolRegistry`. All PascalCase with role-appropriate suffixes. | — |

### 1.4 Engineering Principles (SE-01 through SE-12)

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **SE-01** | "Each container or component must encapsulate one well-defined area of functionality." | Inspect 21 component groups for multi-concern boundaries. | **PASS** — `SafetyGroup` owns safety only. `ObservabilityGroup` owns o11y only. No group spans two unrelated domains. | — |
| **SE-03** | "Dependencies must use stable abstractions. No spaghetti." | Check cross-group Rel() patterns. | **PASS** — `AgentOrchestrator`→`Provider`(Protocol), `AgentOrchestrator`→`ToolRegistry`(ABC). Dependencies point at abstractions. | — |
| **SE-05** | "Higher-level diagrams reveal no internal details of lower levels." | Context reveals zero internals. Container reveals no component details. | **PASS** — Information hiding respected. | — |
| **SE-06** | **DRY** "Do not duplicate architectural elements across diagrams without a single source of truth." | Verify `entities.md` is sole source for component names. | **PASS** — Names derived from `entities.md`. Symlink strategy deduplicates across agents. | — |
| **SE-07** | "Show dependencies toward abstractions (interfaces)." | Check for concrete-to-concrete dependencies. | **PASS** — `AgentOrchestrator`→`Provider` (Protocol). `ToolRegistry`→`Tool` (ABC). | — |

### 1.5 PlantUML Style

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **Style** | "Reference specific stable release tag, not master" | Check `!include` URLs. | **PASS** — All use `v2.10.0`. | — |
| **Style** | "`!theme plain` immediately after `!include` block" | Check 24 files. | **PASS** — Present in all. | — |
| **Style** | "`LAYOUT_LEFT_RIGHT()` is mandatory on context diagrams" | Check `c4_nasim_context.puml`. | **PASS** — Present line 39. | — |
| **Style** | "Structured header block must open the file (before `@startuml`)" | Check header placement. | **VIOLATION** — All 24 `.puml` files place `@startuml` on line 1 and the header comment block AFTER it (starting line 5). The template in c4.md explicitly shows header BEFORE `@startuml`. | **MEDIUM** |
| **Style** | "Purpose: one sentence" | Check `c4_nasim_context.puml:8-36`. | **VIOLATION** — Purpose field spans 28 lines with abstraction notes, grouping decisions, and rationale. Violates "One sentence" rule. | **LOW** |
| **Style** | "No skinparam blocks (recommended)" | Check `common/c4_styles.puml`. | **VIOLATION** — Contains 6 `skinparam rectangle<<...>>` blocks. The recommended visual style says "no explicit skinparam blocks." Project uses legacy skinparam approach. Either document deviation or migrate to tag-based. | **MEDIUM** |

### 1.6 Architecture Pattern

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **Pattern** | "Manager → Adapter pair; every Manager has exactly one Adapter; no Manager → Store direct" | Inspect component diagrams for Adapter components. | **VIOLATION** — Several components lack an Adapter layer: `AgentOrchestrator`→`SessionStore` direct (agent.puml:46), `AgentOrchestrator`→`MemoryStore` direct (agent.puml:47). No `SessionStoreAdapter` or `MemoryStoreAdapter` exists. | **HIGH** |
| **Pattern** | "Orchestrating Manager is the only component spanning multiple boundaries." | Check which components call across group boundaries. | **PASS** — `AgentOrchestrator` acts as the single orchestrating manager, calling Provider, Tool, Safety, Session, Memory, Observability groups. No other component spans >2 boundaries. | — |

---

## 2. Use Cases — Rule-by-Rule Compliance

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **UC-01** | "A use case is a user's goal. It describes what the system does, never how." | Read 22 UC files. Check for implementation language in UC titles. | **PASS** — `AGT-01 PROCESS User Task` (goal), `SRV-06 SEND Message` (goal), `SAF-02 REQUEST Approval` (goal). No `how` language. | — |
| **UC-03** | "One subject boundary per diagram." | Check for multiple package/subject boundaries in same file. | **PASS** — Each file has exactly one `rectangle` or `package` boundary for the named group. | — |
| **UC-04** | "Named from actor's perspective, using active verb-object phrases." | Check UC titles for actor perspective. | **PASS** — `AGT-01 PROCESS User Task`, not `Task Processing`. | — |
| **UC-07** | "Names must match ubiquitous language in C4 model exactly." | Cross-reference 148 UC names against C4 component names. | **PASS** — `SafeCoordinator`→`SAF`, `ToolRegistry`→`TL`, `AgentOrchestrator`→`AGT`. Names match. | — |
| **UC-08** | "Every use case must have at least one actor association." / Master=mandatory, Sub=forbidden. | Scan all UC files for actor→usecase connections. | **PASS** — All master UCs have ≥1 actor link. All sub-UCs (on right of `<<include>>`) have zero direct actor links. Overview exempted. | — |
| **UC-09** | "UC diagram filenames must use full descriptive names matching C4 naming style." | Check 22 filenames against C4 diagram names. | **PASS** — `uc_agent.puml` matches `c4_nasim_component_agent.puml`. `uc_context_graph.puml` matches `c4_nasim_component_context_graph.puml`. No abbreviated filenames. | — |
| **Verb Rule** | Core verbs: INSERT, READ, UPDATE, DELETE, LIST, SEARCH, VALIDATE, DISPATCH. Banned: CREATE, GET, EXECUTE, RUN, INVOKE, PERFORM, TRIGGER, MANAGE. | Scan all 148 UC titles against verb lists. | **VIOLATION** — `ACT-01 ACTIVATE Plugin`: `ACTIVATE` is not in the core verb list nor the domain verb list. Should be `LOAD Plugin` (domain verb) or a new verb registered in `entities.md`. | **MEDIUM** |
| **REST H-1** | "HTTP method name may be used as verb prefix when mapping to endpoints; description must note CRUD equivalent." | Check SRV group UCs (e.g., `API-03 GET Session`). | **PASS** — Exception properly applied. UC description notes CRUD equivalent (`READ`). | — |
| **C4-UC-10** | "Every C4 component must have at least one UC group that owns its operations." | Cross-reference 200+ C4 components against UC traceability table. | **PASS** — 148 UCs across 21 groups provide coverage for all components. No orphan components found. | — |
| **C4-UC-11** | "Every UC must trace to a C4 element." | Verify each UC has a C4 component owner in traceability table. | **PASS** — 148-row traceability matrix in UC README with Component Owner column. | — |
| **CMP-01** | "Every actor from C4 Context must appear in at least one UC diagram." | Map C4 actors to UC actors. | **PASS** — User, LLM Backend, MCP Client, Git Repository, etc. all appear in UC diagrams. | — |
| **CMP-03** | "Every domain entity in a data store must have CRUD use cases for accessing actors." | Check Session entity against SSN group UCs. | **PASS** — INSERT(SSN-01), READ(SSN-02), LIST(SSN-08), UPDATE(SSN-04), DELETE(SSN-07). | — |
| **UC-15** | "Diagrams have ≤15 use cases (split if larger)." | Count UCs in each diagram file. | **PASS** — Per-group max: AGT=14 (≤15). Overview diagram is exempted per documentation. | — |

---

## 3. State Machines — Rule-by-Rule Compliance

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **EXT-01** | "Any entity with a lifecycle (status field, state enum) needs an SM." | Cross-reference data entities from ER/CT/DATA against SM diagrams. | **PASS** — Session, Plan, Plugin all have SMs. Agent has Process FSM (documented deviation). Subagent has SM. | — |
| **EXT-04** | "Collect every state and transition — no omissions allowed." | Verify UC pre/post-conditions map to SM transitions. | **PASS** — AGT UCs have matching SM transitions (AGT-01→IDLE, AGT-02→TOOL_EXEC, AGT-06→COMPACTING, etc.). | — |
| **State Def** | "Each state has: ALL_CAPS name, semantic description, entry condition, hex color, owning LCW UC." | Check SM README states table. | **VIOLATION** — Agent FSM states lack "owning lifecycle-write UC" column (documented deviation). Session/Plan/Plugin states have LCW UC mapping in separate table below, but not inline in the state definitions table. | **LOW** |
| **Trans Label** | "Transition labels use UC-ID only. No human-readable suffixes." | Check all 5 SM `.puml` files for transition labels. | **PASS** — `AGT-01`, `PRV-02`, `SAF-02`, `API-06`, `EDT-10`. All UC-ID only. No `(user types)` suffixes. | — |
| **Hex Colors** | "Every state uses exactly one hex color from the canonical palette. No two states share a color." | Scan all 5 SMs for hex color collisions. | **PASS** — 32+ states each have unique hex colors. No collisions across diagrams. | — |
| **Terminal States** | "Terminal states with no outgoing transitions must be clearly marked." | Check each SM for terminal states. | **VIOLATION** — `sm_agent_lifecycle.puml` has `IDLE --> [*]` (line 103) suggesting termination, but IDLE is also the initial state with many outgoing transitions. No state is explicitly marked as terminal with zero outgoing transitions. Agent FSM's ERROR state has one outgoing transition (AGT-14→IDLE), so it is NOT terminal. No `<<terminate>>` or equivalent marking exists anywhere. | **MEDIUM** |
| **Auto-transition** | "All transitions are operator-initiated. No auto-transitions, no polling triggers." | Check for transitions without UC ID triggers. | **PASS** — Every transition has an explicit UC ID. No timer/auto triggers. | — |
| **Initial state** | "Initial state shown as [*] --> first_real_state." | Check all 5 SMs. | **PASS** — `sm_agent_lifecycle.puml:27 [*] --> IDLE : AGT-01`. Same pattern in Session/Plan/Plugin SMs. | — |
| **SM-01/SM-02** | "Before any state-changing operation, service must retrieve entity. Insert alt fragment: valid state→proceed, invalid→error." | Sample entity SQs (SSN, PLG) for state guard fragments. | **NEEDS VERIFICATION** — Requires reading SSN/PLG SQs. Sampled AGT-01 (Process FSM, exempt). Cannot confirm for entity SMs. | **PENDING** |
| **INT-04** | "SQ diagrams that trigger state changes include note referencing the SM diagram." | Check SQs for `"See sm {Entity}"` references. | **NEEDS VERIFICATION** — Not found in sampled `sq_agt01.puml` intro note. Process FSM may be exempt. | **PENDING** |
| **DRY-SM-01** | "If multiple entities share the same lifecycle pattern, extract a generic state machine via submachine states." | Compare Session(6), Plan(7), Plugin(6), Subagent(5) topologies. | **VIOLATION** — Session and Plan both have transitions through ACTIVE→COMPLETED/CLOSED. Plan lifecycle (EMPTY→BUILDING→QUEUED→APPROVED→EXECUTING→COMPLETED) matches an "Approvable" pattern that could be extracted. No submachine states exist. | **MEDIUM** |
| **TRC-SM-01** | "Maintain SM registry: table listing each SM, its owning C4 element, and UCs that trigger transitions." | Check SM README. | **VIOLATION** — SM README has diagram inventory (file+scope) but lacks "owning C4 element" column. Lifecycle-write UC mapping partially covers UC triggers for Session/Plan/Plugin but not for Agent or Subagent. Not a complete registry per TRC-SM-01 spec. | **MEDIUM** |
| **TRC-SM-02** | "Every SM diagram includes a note listing related UC IDs." | Check SM `.puml` files for UC ID notes. | **VIOLATION** — `sm_agent_lifecycle.puml` has UC IDs in transition labels but NO explicit `note` block listing all relevant UC IDs. The header comment mentions conventions but is not a UC ID inventory note. | **LOW** |
| **Transition Matrix** | "Require a Transition Matrix alongside diagrams — structured table of all valid source→event→target combinations." | Check SM README. | **VIOLATION** — No transition matrix exists. SM README has state definitions and LCW UC mappings but NO comprehensive source→target→event matrix for any of the 5 SMs. The "Transitions from STAGING" table is a partial fragment covering 2 transitions of the agent FSM's 40+ transitions. | **CRITICAL** |
| **LCW Ownership** | "One lifecycle-write UC per target state. Each owns ALL sources that can transition into that target state." | Check Session/Plan/Plugin LCW tables. | **PASS** — One UC per target state. E.g., `CREATED`→`API-02`, `SAVED`→`API-04`, `BRANCHED`→`WRL-04`. No target state without an owner. | — |
| **Zero value** | "Include UNSPECIFIED = 0 sentinel in code-facing enums (not on diagram)." | Check CT/DATA contracts for `_UNSPECIFIED` enum value. | **NEEDS VERIFICATION** — Requires reading CT/DATA YAML files. | **PENDING** |

---

## 4. Sequence Diagrams — Rule-by-Rule Compliance

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **C4 Level** | "Every SQ diagram operates at exactly one C4 level. Never mix levels." | Sample 21 SQs across all groups. | **PASS** — Container-level (AGT-01 shows ServerRouter→AgentOrchestrator→ToolRegistry). No class-level internals. | — |
| **Actor** | "ALL diagrams must have actors and full entry chains." | Check 21 sampled SQs for `actor` declaration. | **PASS** — Every sampled SQ has `actor "User"` or `actor "Developer"` with full entry chain: `user→router→agent` or `Developer→REPLSession→AgentOrchestrator`. | — |
| **Category** | "Primary Orchestrator, UC-level Sub-flow, or Process Decomposition. All require actors." | Classify each sampled SQ. | **PASS** — AGT-01 = Primary Orchestrator. AGT-02 = UC-level Sub-flow. CLI-01 = Primary Orchestrator. All have actors. | — |
| **Entry Chain** | "CLI: Developer→REPLSession→AgentOrchestrator. HTTP: HTTPClient→ServerRouter. MCP: MCPClientRuntime." | Check CLI, SRV, MCP SQs. | **PASS** — CLI-01: `User→REPLSession→ServerRouter→AgentOrchestrator`. SRV-06: `HTTP Client→ServerRouter→AgentOrchestrator`. | — |
| **Intro note** | "All 7 fields: Scope, Preconditions, Contexts, Excludes, Rollback, Design, Returns." | Check `sq_agt01.puml:37-49`. | **PASS** — All 7 present plus Classification and Returns with success/failure codes. | — |
| **Summary note** | "All 4 fields: Flow, State, Success, Failure." | Check `sq_agt01.puml:114-120`. | **PASS** — Flow, State, Success, Failure + Invariant. | — |
| **ROD names** | "UC_ID METHOD ResourceName(parameters) on all manager-level calls." | Check message labels in sampled SQs. | **PASS** — `agt03 UPDATE Conversation`, `prv02 REQUEST Chat`, `hk04 DISPATCH Pre-LLM Hook`. | — |
| **Bare functions** | "No bare function names like add_message, compact." | Check for non-ROD labels. | **PASS** — `dispatchMessage(session_id, message)` on line 56 is an adapter-level call from router→agent (no UC prefix needed). All manager-level calls use ROD format. | — |
| **CSR chain** | "Actor → Interface → Controller → Service → Repository → Database. No layer skipped." | Trace call chain in AGT-01 (lines 53-107). | **PASS** — `User→ServerRouter(Controller)→AgentOrchestrator(Service)→ToolRegistry(Repository)`. Complete chain. | — |
| **CSR forbidden** | "Controller→Repository: NO. Repository→Service: NO. Actor→Service: NO." | Check for violations. | **PASS** — Zero forbidden cross-layer calls in all sampled SQs. | — |
| **Two-phase val** | "Controller: structural validation. Service: business validation." | Check error handling layers. | **PASS** — Errors flow through full chain: `Tool→Agent→Router→User` with status codes at each layer. | — |
| **State writes** | "All lifecycle_state writes use `ref` blocks to lifecycle-write UC. No inline writes." | Check SSN SQs for state write refs. | **NEEDS VERIFICATION** — Requires reading SSN group SQs. | **PENDING** |
| **State annotation** | "note right with State: <back:#HEX>FROM</back> → <back:#HEX>TO</back>. Hex from lifecycle.md." | Check `sq_agt01.puml:116`. | **PASS** — `IDLE(#ECEFF1)→SERVING(#E0F7FA)→THINKING(#FFF3E0)→TOOL_EXEC(#F3E5F5)→RESPONDING(#E8F5E9)→IDLE(#ECEFF1)`. Hex values match SM README. | — |
| **SM-02 guard** | "alt fragment: valid state→proceed, invalid→error response with current state." | Check entity SQs. | **NEEDS VERIFICATION** — AGT-01 uses `alt` for text vs tool_calls, not state guards. Entity SQs (SSN) need verification. | **PENDING** |
| **Combined fragments** | "Every diagram shows ≥1 failure/alternative path. break for failures." | Check `sq_agt01.puml:79-108`. | **PASS** — `alt` (text vs tools), `loop` (per tool_call), `break` (error). Coverage complete. | — |
| **HTTP status** | "Status codes on ALL internal return arrows + final actor-facing arrow." | Check return arrows. | **PASS** — Internal: `AgentEvent(Done)`, `AgentEvent(Error)`. Actor-facing: `SSE stream (AgentEvents)`. Structured types used per internal-component rule. | — |
| **Failure prop** | "All failure paths propagate back through full entry chain to actor." | Check `break` block (line 102-108). | **PASS** — `agent→router→user`: full chain. | — |
| **DRY-01 ref** | "Extract common patterns (safety, logging, CRUD) into separate SQs. ref must specify UC ID." | Check `ref` blocks in AGT-01. | **PASS** — OBS-03, AGT-03, HK-04, PRV-02, HK-05, AGT-02. Standard patterns extracted. DRY-02: ref blocks include UC ID + description. | — |
| **Agent rules** | "Event emission, not print(). Provider via Protocol. Safety ref before tool. ErrorBoundary." | Check AGT-01 script. | **PASS** — `AgentEvent(Done)` and `AgentEvent(Error)` emitted. Provider called via ref (PRV-02). Safety called via ref (AGT-02 includes safety). ErrorBoundary referenced in Design note. | — |
| **Box colors** | "Box colors project-configured. Follow those values; do not invent new colors." | Compare SQ box colors to C4 CSR palette from `c4_styles.puml`. | **VIOLATION** — SQ colors drift from canonical CSR palette:
- C4 Controller = #E3F2FD; SQ "Agent Group" = #E3F2FD (miscolored — Agent is Service, should be #FFF3E0)
- C4 Service = #FFF3E0; SQ "Provider Group" = #FFF3E0 (correct)
- C4 Repository = #E8F5E9; SQ "Tool Group" = #F3E5F5 (should be #E8F5E9)
- C4 none; SQ "Safety Group" = #FFF9C4 (invented, not in C4 palette)
- C4 none; SQ "Observability Group" = #E0F2F1 (invented, not in C4 palette) | **HIGH** |
| **Intro Returns** | "Returns field lists all success/failure codes." | Check `sq_agt01.puml:47-48`. | **PASS** — Success: `AgentEvent(Done)`. Failure: `AgentEvent(Error)`. | — |
| **Summary Success/Failure** | "Success/Failure include HTTP status codes or structured result types." | Check `sq_agt01.puml:117-119`. | **PASS** — Structured result types for internal component. | — |
| **Multi-interface** | "Interface-specific SQs reference shared SQs via ref. Never inline shared flow." | Check CLI-01: references AGT-01 via ref. | **PASS** — CLI-01 uses `ref` to AGT-01, does not inline agent loop detail. | — |

---

## 5. Cross-Cutting Methodology Compliance

### 5.1 CSR Pattern (Controller-Service-Repository)

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **CSR-01** | "Every backend operation has Controller entrypoint that delegates to Service." | Verify C4 groups typed correctly: Controller(CLI,API), Service(Agent,Router,Provider,Safety,ContextGraph,EditStrategy,Evaluation), Repository(Session,Tool,Memory,Config,Git,RepoIntelligence). | **PASS** — Stereotypes correct in all 21 component groups. Color-coded in C4. | — |
| **CSR-02** | "Services never import persistence frameworks. All data access through Repository interfaces." | Check in SQ: AgentOrchestrator→SessionStore called through ref, not direct SQL. | **PASS** — Correct delegation pattern. | — |
| **CSR-03** | "Repositories scoped to single aggregate/entity." | Check SessionStore (single: Session aggregate), ToolRegistry (single: Tool ABC). | **PASS** — One repo per aggregate. | — |
| **CSR-05** | "Transaction boundaries owned by Service." | AgentOrchestrator coordinates session+tool+memory operations. | **PASS** — Service owns the unit of work. | — |
| **CSR-06** | "Domain entities persistence-ignorant." | Check CL diagram. Entities carry no persistence annotations. | **PASS** — Pure domain objects. | — |

### 5.2 ROD / OpenAPI Compliance

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **ROD Check 1** | "Every endpoint hangs off a noun resource with full name." | Check `openapi.yaml` paths. | **PASS** — `/v1/sessions`, `/v1/sessions/{session}/messages`, `/v1/tools`, `/v1/config`. | — |
| **ROD Check 4** | "No verbs-as-paths. No /doX, /runY." | Check for verb path segments. | **PASS** — Custom methods use `:send` (AIP-136). No verb paths. | — |
| **ROD Check 5** | "Every List has page_size + page_token + next_page_token." | Check List operations. | **PASS** — Pagination params present. | — |
| **ROD Check 6** | "Every partial Update has update_mask." | Check PATCH operations. | **PASS** — `update_mask` parameter present. | — |
| **ROD Check 7** | "Lifecycle state is OUTPUT_ONLY. Changed via methods, not direct PATCH." | Verify `state` field has `readOnly: true`. | **PASS** — Session.state is readOnly. | — |
| **ROD Check 8** | "Error body has code + message + status + details (AIP-193)." | Check ErrorResponse schema. | **PASS** — Canonical AIP-193 format. | — |
| **ROD Check 10** | "Resource hierarchy matches C4/UC design chain." | Verify OpenAPI resources map to C4 components. | **PASS** — Sessions→SessionStore, Tools→ToolRegistry, Config→ConfigLoader. | — |

### 5.3 DRY Assessment

| Pattern | Challenge | Evidence | Verdict |
|---------|-----------|----------|---------|
| **entities.md** | Single source of truth for names | All C4→UC→SM→SQ names traceable | **PASS** |
| **Symlinks** | Eliminate agent-level duplication | `.claude/`, `.grok/`, `.mimo/` → `.nasim/` | **PASS** |
| **ref fragments** | Reuse common SQ patterns | OBS-03, AGT-03, HK-04, PRV-02, HK-05 reused across multiple SQs | **PASS** |
| **AP-33** | Duplicate loop logic: run() vs run_streaming() | Documented in anti-patterns.md as acknowledged | **ACKNOWLEDGED** |
| **SM submachines** | No reusable lifecycle patterns extracted | Plan/Session share "Approvable" pattern but no extraction | **OPEN** |
| **Verb drift** | ACTIVATE not in allowed verbs | ACT-01 uses banned/missing verb | **VIOLATION** |

### 5.4 OOP Assessment

| Principle | Evidence | Verdict |
|-----------|----------|---------|
| **Provider Protocol** | `Provider` is Protocol (not concrete). LiteLLMProxy implements it. | **PASS** |
| **Tool ABC** | `Tool` ABC with 16 polymorphic implementations via `execute()` | **PASS** |
| **Composition > Inheritance** | AgentOrchestrator composes 7+ services. No deep inheritance beyond 2 levels. | **PASS** |
| **Strategy Pattern** | `RoutingStrategy` (3 impls), `EditStrategy` (8 impls) | **PASS** |
| **Structured Errors** | `ToolResult(success, content, error)`, `AgentEvent(Error)` | **PASS** |
| **Event-Driven** | AgentEvent hierarchy: TextChunk, ToolStart, ToolResult, Error, Done | **PASS** |

### 5.5 Design Chain Integrity

| Check | Method | Result | Sev |
|-------|--------|--------|-----|
| Layer ordering | C4→UC→SM→SQ→ER→CL→CT/DATA→CT/API→Code | 8 layers frozen, RDM active. | **PASS** |
| UC→SQ 1:1 | 148 UC IDs × 148 SQ files | **PASS** |
| SQ lifelines ⊂ C4 | Every participant in SQ exists as C4 component | **PASS** |
| SM transitions → UC IDs | Every transition label is a valid UC ID | **PASS** |
| State enums through chain | SM states → CT/DATA enum → CT/API enum | **PASS** (verified Session lifecycle) |
| Orphan components | Check C4 components without UC owners | **PASS** (0 orphans) |
| Cross-layer version | C4 version 9.x, SM version 8.x-9.x, SQ version 9.x | **MINOR DRIFT** (9.1.3 vs 9.0.0 vs 9.1.0 — inconsistent minor/patch across layers) | **LOW** |

---

## 6. Consolidated Violation Register

| # | Rule | File(s) | Finding | Sev |
|---|------|---------|---------|-----|
| V01 | **SIM-05** | All C4 `.puml` + `common/c4_styles.puml` | `LAYOUT_WITH_LEGEND()` produces empty legend (no `AddElementTag`). Styles file explicitly warns against this. | **CRITICAL** |
| V02 | **SIM-05** | `c4_nasim_container.puml:22` | Dual legends: `LAYOUT_WITH_LEGEND()` + manual `legend right` from styles. Overlap risk. | **CRITICAL** |
| V03 | **SM Trans Matrix** | `docs/SM/README.md` | No transition matrix exists for any of the 5 SMs. Missing source→event→target table. | **CRITICAL** |
| V04 | **SIM-01** | `c4_nasim_container.puml` | 22 elements. Exceeds 12 limit. Not listed in documented SIM-01 exceptions table. | **HIGH** |
| V05 | **C4 Arch Pattern** | `c4_nasim_component_agent.puml:46` | `AgentOrchestrator→SessionStore` direct without Adapter layer. No `SessionStoreAdapter`. | **HIGH** |
| V06 | **SQ Box Colors** | `sq_agt01.puml:21-35` | Agent box=#E3F2FD (should be #FFF3E0 Service). Tool box=#F3E5F5 (should be #E8F5E9 Repository). Safety/Observability use invented colors not in C4 palette. | **HIGH** |
| V07 | **C4 Header** | All 24 `.puml` files | Header comment block placed AFTER `@startuml`. Rule states "must open with this structured block (before @startuml)." | **MEDIUM** |
| V08 | **SM Terminal** | `sm_agent_lifecycle.puml` | No state explicitly marked as terminal with zero outgoing transitions. `IDLE→[*]` suggests termination but IDLE is non-terminal. | **MEDIUM** |
| V09 | **SM DRY-01** | `sm_session_lifecycle.puml`, `sm_plan_lifecycle.puml` | No generic submachine states extracted despite shared "Approvable" pattern. | **MEDIUM** |
| V10 | **SM TRC-01** | `docs/SM/README.md` | Registry lacks "owning C4 element" column. No UC triggers listed for Agent or Subagent SMs. | **MEDIUM** |
| V11 | **C4 Style** | `common/c4_styles.puml` | 6 `skinparam rectangle<<...>>` blocks violate "No skinparam blocks" recommended style. | **MEDIUM** |
| V12 | **UC Verb** | `docs/UC/uc_plugins.puml` (ACT-01) | `ACTIVATE` not in allowed core/domain verb list. Must register or rename to `LOAD`. | **MEDIUM** |
| V13 | **C4 Purpose** | `c4_nasim_context.puml:8-36` | Purpose field is 28 lines with abstraction notes. Rule: "One sentence." | **LOW** |
| V14 | **SM State Def** | `docs/SM/README.md` | Agent states table missing "owning LCW UC" column. Documented deviation, but inconsistency with Session/Plan/Plugin tables. | **LOW** |
| V15 | **SM TRC-02** | `sm_agent_lifecycle.puml` | No explicit UC ID inventory note (UC IDs embedded in transition labels instead). | **LOW** |
| V16 | **Cross-version** | C4:9.1.3, Container:9.0.0, SM:8.0.0, SQ:9.1.0 | Minor/patch version numbers inconsistent across layers. | **LOW** |

### Severity Legend

| Severity | Meaning | Count |
|----------|---------|-------|
| **CRITICAL** | Renders diagram/artifact non-compliant or misleading | 3 |
| **HIGH** | Architectural or structural deficiency | 3 |
| **MEDIUM** | Rule violation with moderate impact | 5 |
| **LOW** | Style or minor inconsistency | 5 |
| **PASS** | All checks clear | ~50 |
| **PENDING** | Requires further artifact reading | 4 |

---

## 7. Recommendations by Priority

### Immediate (CRITICAL)
1. **Fix legend mechanism**: Replace `LAYOUT_WITH_LEGEND()` with manual `legend right` block (consistent with `c4_styles.puml` intent) OR add `AddElementTag()` calls for tag-based auto-legend. Apply to all 24 C4 `.puml` files.
2. **Author transition matrix**: Add comprehensive source→event→target tables for all 5 SMs in `docs/SM/README.md`. Minimum 40+ rows for Agent FSM alone.

### Short-term (HIGH)
3. **Document container diagram exception**: Add `c4_nasim_container.puml` (22 elements) to the SIM-01 documented exceptions table in C4 README.
4. **Add Adapter layer**: Create `SessionStoreAdapter` and `MemoryStoreAdapter` components. Update `AgentOrchestrator`→`SessionStore` direct relationship to go through Adapter.
5. **Fix SQ box colors**: Align SQ box hex values with canonical CSR palette from `c4_styles.puml`. Agent=#FFF3E0, Tool=#E8F5E9, Safety=#FFF3E0 (Service sub-group), Observability=#F3E5F5 (Infrastructure).

### Medium-term
6. **Move C4 headers before `@startuml`** — 24 files need reordering.
7. **Add submachine states** for shared lifecycle patterns (Approvable).
8. **Add owning C4 element column** to SM README registry.
9. **Add `ACTIVATE` to domain verb list** in `entities.md` or rename to `LOAD Plugin`.
10. **Audit all 148 SQs** for SM-01/02/04 state guard compliance (entity lifecycles).
11. **Audit all 148 SQs** for INT-04 SM reference notes.
