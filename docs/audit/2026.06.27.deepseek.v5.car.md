# CAR Audit: Design Chain — Rule-Grounded Technical Compliance, v5

**Global rule sources:**
- `~/.agent-global/shared/rules/software-design/c4.md` — C4 Architecture Authoring Policy
- `~/.agent-global/shared/rules/software-design/uc.md` — Use Case Authoring Policy
- `~/.agent-global/shared/rules/software-design/sm.md` — State Machine Authoring Policy
- `~/.agent-global/shared/rules/software-design/sq.md` — Sequence Diagram Authoring Policy
- DRY, CSR (Controller-Service-Repository), ROD (Resource-Oriented Design), OOP principles

| Field | Value |
|-------|-------|
| Date | 2026-06-27 |
| Scope | docs/C4 (24 files), docs/UC (22 files + UC README with 148 UCs), docs/SM (5 files), docs/SQ (148 files across 21 groups), docs/README.md |
| Methodology | Per-rule CAR (Challenge = rule text; Action = cross-reference method; Result = verdict + evidence + severity) |
| Severity | CRITICAL / HIGH / MEDIUM / LOW / PASS |
| Coverage | All 4 rulebooks cross-referenced against every artifact; 10 discovered violations beyond previous audit |

---

## 1. C4 Architecture — Rule-by-Rule CAR

### 1.1 Level Discipline (C4-01 through C4-07)

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **C4-01** | *"Strictly separate the four C4 levels — Context, Container, Component, Code. Never mix levels on the same diagram."* | Opened all 24 `.puml` files. Checked for System, Container, and Component macros appearing in wrong-level diagrams. | **PASS** — No level mixing. Context uses `System()`, `Person()`, `System_Ext()` only. Container uses `Container()`, `Container_Ext()` only. Component uses `Component()`, `Component_Ext()` only. | — |
| **C4-03** | *"System Context diagram must only show the system under design, users, and external systems. No containers, no technologies."* | Inspect `c4_nasim_context.puml:44-56`. | **PASS** — 1 Person + 1 System + 9 System_Ext. Zero containers, zero technologies. | — |
| **C4-04** | *"Container diagram must only show high-level technical building blocks. No component internals."* | Inspect `c4_nasim_container.puml:28-60`. | **PASS** — Shows 3 Containers, 3 Container_Ext, 5 System_Ext. No component internals revealed. | — |
| **C4-05** | *"Component diagram must only show internals of a single container. No code-level classes."* | Inspect all 21 component diagrams. | **PASS** — All diagrams show internals within `Boundary(nasim_application)` only. Cross-group refs use `Component_Ext()`. No class-level detail. | — |

### 1.2 Diagram Simplicity (SIM-01 through SIM-06)

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **SIM-01** | *"Limit each diagram to 5–12 elements. If >12, split by use case, functional area, or data flow."* | Count elements in all 24 diagrams. | **VIOLATION** — 4 diagrams exceed 12-element limit: overview_core(27), tools(24), observability(17), edit_strategy(13). C4 README lists 6 documented exceptions. overview_core and tools ARE in the exception table; observability(17) and edit_strategy(13) are NOT. Container diagram(22) IS listed as exception. | **MEDIUM** |
| | | | | |
| **SIM-01** | Detail — `c4_nasim_component_observability.puml` | Counted 8 Component + 6 Component_Ext + 3 System_Ext = **17 elements**. | Not in the SIM-01 exception table. README line 281-291 lists 6 exceptions. Observability(17) is missing. | **MEDIUM** |
| **SIM-01** | Detail — `c4_nasim_component_edit_strategy.puml` | Counted 11 Component + 1 Component_Ext + 1 System_Ext = **13 elements**. | Not in the SIM-01 exception table. Marginal overage but not documented. | **LOW** |
| **SIM-02** | *"Every box and every line must have a clear, purposeful existence. Eliminate orphaned or decorative elements."* | Check for elements with no incoming/outgoing relationships. | **PASS** — All elements connected. No orphan components. | — |
| **SIM-03** | *"Every relationship line must be labelled with a meaningful description of the intent."* | Audit all `Rel()` labels across all 24 diagrams. | **PASS** — Intent-based labels throughout (e.g., `"sends context and receives code completions"`, not `"HTTP"`). Protocol optional in technology slot. | — |
| **SIM-04** | *"Use consistent visual vocabulary: boxes for people/systems, cylinders for data stores, arrows for message buses."* | Verify element macros match purpose. | **PASS** — `Person()` for humans, `System_Ext()` for services, `SystemDb_Ext()` only for databases. `LLM Backend` → `System_Ext()` (correct — inference service). | — |
| **SIM-05** | *"Always include a legend/key explaining shapes and line styles."* | Check legend mechanism in each diagram. Context diagram (line 40) uses `LAYOUT_WITH_LEGEND()`. | **VIOLATION** — Context diagram has ZERO `AddElementTag()` calls, producing an empty auto-generated legend. `common/c4_styles.puml:66` explicitly warns: *"Do NOT use LAYOUT_WITH_LEGEND() with this file — it produces an empty auto-generated legend when no AddElementTag calls are present."* All diagrams that include `c4_styles.puml` use manual `legend right` blocks (correct). Context diagram does NOT include `c4_styles.puml` and has a non-functional legend. | **CRITICAL** |
| **SIM-05** | Same rule — container diagram uses both `LAYOUT_WITH_LEGEND()` AND manual `legend right`. | Check `c4_nasim_container.puml`. | **VIOLATION** — Container diagram includes both `LAYOUT_WITH_LEGEND()` and manual `legend right` block. This produces dual legends that may overlap in rendering. | **HIGH** |
| **SIM-05** | All component diagrams include `c4_styles.puml` which provides manual `legend right`. | Verify all 21 component diagrams. | **PASS** — All component diagrams use a single manual legend via the shared include file. | — |

### 1.3 Naming Conventions (LANG-01 through LANG-04)

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **LANG-01** | *"Use domain's ubiquitous language for all element names. Box name must match exactly what stakeholders and codebases call it."* | Cross-reference 200+ component names against C4 README and UC README. | **PASS** — `AgentOrchestrator`, `SafetyCoordinator`, `ToolRegistry` match across C4, UC, SM, SQ, and codebase. | — |
| **LANG-04** | *"Be consistent: once named, use exact same name in all diagrams."* | Compare names across context/container/component levels. | **PASS** — `LLM Backend` appears identically in context and container diagrams. `Host Filesystem` consistent. | — |
| Components naming | *"PascalCase ending in Manager, Adapter, API, or Client."* | Inspect all Component names. | **PASS** — `AgentOrchestrator`, `SafetyCoordinator`, `ConversationHistory`, `ErrorBoundary`, `ToolRegistry`. All PascalCase with role-appropriate suffixes. | — |

### 1.4 Engineering Principles (SE-01 through SE-12)

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **SE-01** | *"Each container or component must encapsulate one well-defined area of functionality."* | Inspect all 21 component groups for multi-concern boundaries. | **PASS** — Each group owns exactly one concern. No group spans two unrelated domains. | — |
| **SE-03** | *"Dependencies must use stable abstractions. No spaghetti."* | Check cross-group `Rel()` patterns. | **PASS** — `AgentOrchestrator`→`Provider` (Protocol), `AgentOrchestrator`→`ToolRegistry` (ABC). Dependencies point at abstractions. | — |
| **SE-05** | *"Higher-level diagrams reveal no internal details of lower levels."* | Context reveals zero internals. Container reveals no component details. | **PASS** — Information hiding respected across levels. | — |
| **SE-06** | **DRY** *"Do not duplicate architectural elements across diagrams without a single source of truth."* | Verify `entities.md` existence as sole source of truth. | **VIOLATION** — `entities.md` is referenced as the canonical source in 7+ C4 diagram headers (e.g., `c4_nasim_context.puml:34` Source field, `c4_nasim_component_agent.puml:15`) but the file **does not exist** anywhere in the project tree. Symlink strategy deduplicates across agent directories, but the entity registry itself is missing. | **CRITICAL** |
| **SE-07** | *"Show dependencies toward abstractions (interfaces)."* | Check for concrete-to-concrete dependencies. | **PASS** — `AgentOrchestrator`→`Provider` (Protocol). `ToolRegistry`→`Tool` (ABC). | — |
| **SE-11** | *"Composition over Inheritance."* | Check for deep inheritance hierarchies. | **PASS** — AgentOrchestrator composes 7+ services. No deep inheritance beyond 2 levels. Tool ABC has 16 flat implementations. | — |
| **SE-12** | *"Fail-Fast / Robustness: if architecturally significant, show error channels."* | Check for error/retry channels in C4 diagrams. | **PASS** — `ErrorBoundary` is a first-class component. Agent component diagram includes error delegate path. | — |

### 1.5 Architecture Pattern (Manager→Adapter→Store)

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **Pattern** | *"Manager → Adapter pair; every Manager has exactly one Adapter; no Manager → Store direct."* | Inspect all component diagrams for Adapter components. | **VIOLATION** — `c4_nasim_component_agent.puml:48-49` shows `AgentOrchestrator`→`SessionStore` and `AgentOrchestrator`→`MemoryStore` direct. No `SessionStoreAdapter` or `MemoryStoreAdapter` exists in any C4 diagram. `AgentOrchestrator`→`ToolRegistry` direct at line 42 also bypasses adapter layer. Agent Group has zero adapters. | **HIGH** |
| | | | | |
| **Pattern** | *Memory Group has Adapters, Agent Group does not use them.* | Compare `c4_nasim_component_memory.puml` (has `EpisodicMemoryAdapter`, `SemanticMemoryAdapter`, `WorkingMemoryAdapter`) vs `c4_nasim_component_agent.puml` (AgentOrchestrator→MemoryStore direct). | **VIOLATION** — Memory Group correctly defines adapters for store access, but Agent Group bypasses them entirely. AgentOrchestrator should call `MemoryRetriever`/`MemoryIndexer` (adapters) not `MemoryStore` directly. | **HIGH** |
| **Pattern** | *"Orchestrating Manager is the only component spanning multiple boundaries."* | Check which components call across group boundaries. | **PASS** — `AgentOrchestrator` acts as the single orchestrating manager, calling Provider, Tool, Safety, Session, Memory, Observability groups. No other component spans >2 boundaries. | — |

### 1.6 PlantUML Style Compliance

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **Style** | *"Reference specific stable release tag, not master."* | Check `!include` URLs. | **PASS** — All use `v2.10.0`. | — |
| **Style** | *"`!theme plain` immediately after `!include` block."* | Check 24 files. | **PASS** — Present in all. | — |
| **Style** | *"`LAYOUT_LEFT_RIGHT()` is mandatory on context diagrams."* | Check `c4_nasim_context.puml`. | **PASS** — Present at line 39. | — |
| **Style** | *"Structured header block must open the file (before `@startuml`)."* | Check header placement. c4.md line 201: *"every C4 `.puml` file must open with this structured block (before `@startuml`)"*. | **VIOLATION** — All 24 `.puml` files place `@startuml` on line 1 and the header comment block AFTER it (starting line 5–6). | **MEDIUM** |
| **Style** | *"Purpose: one sentence."* | Check `c4_nasim_context.puml:8-36`. | **VIOLATION** — Purpose field spans 28 lines with abstraction notes, grouping decisions, and rationale. Violates "One sentence" rule. | **LOW** |
| **Style** | *"Minimal comments rule: only section-divider comments permitted after `@startuml`."* | Check overview diagrams for `'` comment blocks. | **VIOLATION** — `c4_nasim_component_overview_core.puml:105-148` includes section divider comments (`' ── Entry points ──`, `' ── Agent → Service groups ──`). While useful, these are redundant per the minimal comments rule. The model should speak for itself. | **LOW** |
| **Style** | *"No `note` blocks in C4; model speaks."* | Check C4 files for `note` blocks. | **PASS** — Zero `note` blocks in any C4 diagram. | — |
| **Style** | *"No skinparam blocks (recommended style)."* | Check `common/c4_styles.puml`. | **VIOLATION** — Contains 6 `skinparam rectangle<<...>>` blocks. The recommended c4.md visual style says "no explicit skinparam blocks." Project uses legacy skinparam approach. Document as deviation or migrate to tag-based. | **MEDIUM** |

---

## 2. Use Cases — Rule-by-Rule CAR

### 2.1 Core UC Principles (UC-01 through UC-08)

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **UC-01** | *"A use case is a user's goal. It describes what the system does, never how."* | Read 22 UC files. Check for implementation language in UC titles. | **PASS** — `AGT-01 PROCESS User Task` (goal), `SRV-06 SEND Message` (goal), `SAF-02 REQUEST Approval` (goal). No `how` language. | — |
| **UC-03** | *"One subject boundary per diagram."* | Check for multiple package/subject boundaries in same file. | **PASS** — Each file has exactly one `rectangle` or `package` boundary for the named group. | — |
| **UC-04** | *"Named from actor's perspective, using active verb-object phrases."* | Check UC titles for actor perspective. | **PASS** — `AGT-01 PROCESS User Task`, not `Task Processing`. | — |
| **UC-07** | *"Names must match ubiquitous language in C4 model exactly."* | Cross-reference 148 UC names against C4 component names. | **PASS** — `SafetyCoordinator`→`SAF`, `ToolRegistry`→`TL`, `AgentOrchestrator`→`AGT`. Names match. | — |
| **UC-08** | *"Every use case must have at least one actor association."* / Master=mandatory, Sub=forbidden. | Scan 22 UC files for actor→usecase connections. | **PASS** — All master UCs have ≥1 actor link. All sub-UCs have zero direct actor links. Overview exempted. | — |
| **UC-15** | *"Diagrams have ≤15 use cases (split if larger)."* | Count UCs per diagram. | **PASS** — Per-group max: TL=22 but only 14 are in the diagram (sub-UCs for CTX/EDT/EVL kept in parent). Overview exempted. | — |

### 2.2 UC Naming and Verb Compliance

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **Verb Rule** | *Core verbs: INSERT, READ, UPDATE, DELETE, LIST, SEARCH, VALIDATE, DISPATCH. Domain verbs: PROCESS, SPAWN, COLLECT, COORDINATE, INJECT, SCORE, DISTILL, REDACT, EMIT, EXPOSE, DISCOVER, ADAPT. Banned: CREATE, GET, EXECUTE, RUN, INVOKE, PERFORM, TRIGGER, MANAGE.* | Scan all 148 UC titles against verb lists. | **VIOLATION** — Of ~61 distinct verbs used across 148 UC titles, only 18 are in the allowed core+domain verb list. 43 verbs are non-allowed. Examples: `STREAM`(CLI-03, PRV-03, OBS-01), `ENABLE`(CLI-05, PLG-05), `REQUEST`(CLI-06, PRV-02, SAF-02), `SWITCH`(CLI-07, AGT-13, RTG-04), `QUEUE`(AGT-07), `APPROVE`(AGT-08), `DELEGATE`(AGT-11), `LOAD`(AGT-12, CFG-01, PLG-02), `HANDLE`(AGT-14), `REGISTER`(PRV-01, HK-01, PLG-03, PLG-04), `SELECT`(PRV-04, RTG-01, EDT-01), `APPLY`(12 UCs from EDT-02..09 + CFG-03, SAF-03, RTG-02, SBX-02), `PERSIST`(SSN-01, MEM-01, TL-19), `FORK`(WRL-04), `CHECKPOINT`(WRL-05), `EVALUATE`(EVL-01), `DETECT`(EVL-08), `CORRELATE`(OBS-03), `EXPORT`(OBS-06), etc. | **CRITICAL** |
| **Banned Verbs** | *CREATE, GET, EXECUTE, RUN, INVOKE, PERFORM, TRIGGER, MANAGE.* | Scan for banned verbs. | **VIOLATION** — `API-02 CREATE Session` (banned), `API-03 GET Session` (banned), `API-09 GET Tool` (banned), `API-10 GET Config` (banned). | **HIGH** |
| **Verb Rule** | *ACTIVATE not in allowed verb lists.* | Check `docs/UC/uc_plugins.puml` for verb. | **VIOLATION** — `ACT-01 ACTIVATE Plugin` uses verb not in core or domain list. Should be registered or renamed to `LOAD`/`ENABLE`. | **MEDIUM** |
| **REST H-1** | *"HTTP method name may be used as verb prefix when mapping to endpoints; description must note CRUD equivalent."* | Check SRV group UCs. | **PASS** — Exception applied consistently. UC-03 GET Session notes CRUD equivalent READ. | — |

### 2.3 C4 Traceability (C4-UC-10 through C4-UC-12)

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **C4-UC-10** | *"Every C4 component must have at least one UC group that owns its operations."* | Cross-reference 200+ C4 components against UC traceability table (148 rows). | **PASS** — All C4 components have at least one UC owner. No orphan components found. | — |
| **C4-UC-11** | *"Every UC must trace to a C4 element."* | Verify each UC has a C4 component owner in traceability table. | **PASS** — 148-row traceability matrix with Component Owner column. | — |
| **C4-UC-12** | *"Maintain a traceability table in docs/UC/README.md."* | Check format and completeness. | **PASS** — Full 148-row matrix with UC ID, Operation, Component Owner, C4 Group, SQ Diagram. | — |
| **CMP-01** | *"Every actor from C4 Context must appear in at least one UC diagram."* | Map C4 actors to UC actors. | **PASS** — User, LLM Backend, MCP Client, Git Repository, etc. all appear in UC diagrams. | — |
| **CMP-03** | *"Every domain entity in a data store must have CRUD use cases for accessing actors."* | Check Session entity against SSN group UCs. | **PASS** — INSERT(SSN-01), READ(SSN-02), LIST(SSN-03), UPDATE(SSN-04), DELETE(SSN-09) plus specialized operations (SSN-05..08). | — |
| **CMP-04** | *"If a UC cannot be traced to a C4 interaction, remove it."* | Check for speculative UCs. | **PASS** — TL-23, TL-24, TL-25 (speculative, no SQ) were removed. MCP-05, MCP-06 deferred to Phase 2 with reserved IDs. | — |

---

## 3. State Machines — Rule-by-Rule CAR

### 3.1 State Completeness (EXT-01 through EXT-05)

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **EXT-01** | *"Any entity with a lifecycle (status field, state enum) needs an SM."* | Cross-reference data entities from ER/CT/DATA against SM diagrams. | **PASS** — Session, Plan, Plugin, Subagent all have SMs. Agent has Process FSM (documented deviation). | — |
| **EXT-04** | *"Collect every state and transition — no omissions allowed."* | Verify UC pre/post-conditions map to SM transitions. | **PASS** — AGT UCs match SM transitions (AGT-01→IDLE, AGT-02→TOOL_EXEC, AGT-06→COMPACTING, etc.). | — |
| **State Def** | *"Each state has: ALL_CAPS name, semantic description, entry condition, hex color, owning LCW UC."* | Check SM README states table for completeness. | **VIOLATION** — Agent FSM states (17 of them) lack "owning lifecycle-write UC" column (documented deviation — Process FSM). Session/Plan/Plugin states have LCW UC mapping in a separate table, not inline in the state definitions. Subagent SM has NO state definition table at all in README. | **LOW** |

### 3.2 Transition Rules

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **Trans Label** | *"Transition labels use UC-ID only. No human-readable suffixes."* | Check all 5 SM `.puml` files for transition labels. | **PASS** — `AGT-01`, `PRV-02`, `SAF-02`, `API-02`, `EDT-10`. All UC-ID only throughout. | — |
| **Trans Label** | *Subagent initial transition anomaly.* | Check `sm_subagent_lifecycle.puml:23`. | **VIOLATION** — `[*] --> IDLE` has NO UC-ID label. All other SMs have `[*] --> FIRST_STATE : UC-ID` pattern. This is the only SM missing an initial transition label. | **MEDIUM** |
| **Hex Colors** | *"Every state uses exactly one hex color from the canonical palette. No two states share a color."* | Scan all 5 SMs for hex color collisions. | **PASS** — 32+ states each have unique hex colors. No collisions across diagrams. | — |
| **Terminal States** | *"Terminal states with no outgoing transitions must be clearly marked."* | Check each SM for terminal state marking. | **VIOLATION** — `sm_agent_lifecycle.puml:103` `IDLE --> [*] : API-06` suggests termination, but IDLE is the initial state with many outgoing transitions. SM policy says "A transition into a terminal state is irreversible" — if IDLE→[*] means termination, IDLE should NOT have outgoing transitions. No state is explicitly marked with `<<terminate>>` or equivalent in any SM. `CLOSED→[*]` in Session SM is the only unambiguous terminal transition. | **MEDIUM** |
| **Auto-transition** | *"All transitions are operator-initiated. No auto-transitions, no polling triggers."* | Check for transitions without UC ID triggers. | **PASS** — Every transition has an explicit UC ID. No timer/auto triggers. | — |
| **Initial state** | *"Initial state shown as [*] --> first_real_state."* | Check all 5 SMs. | **PASS** — Except subagent (missing UC label), pattern is correct. | — |
| **Transition Matrix** | *"Require a comprehensive source→event→target matrix alongside diagrams."* | Check SM README for full transition tables. | **VIOLATION** — No full transition matrix exists for any of the 5 SMs. SM README has state definitions and LCW UC mappings but NOT a complete source→target→event table. Agent SM has 40+ transitions — only "Transitions from STAGING" (2 rows) is documented. Session/Plan/Plugin/Subagent have zero transition rows. | **CRITICAL** |

### 3.3 Lifecycle-Write Ownership

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **LCW** | *"One lifecycle-write UC per target state. Each owns ALL sources that can transition into that target state."* | Check Session/Plan/Plugin LCW tables. | **PASS** — One UC per target state. E.g., `CREATED`→`API-02`, `SAVED`→`API-04`, `BRANCHED`→`WRL-04`. No target state without an owner. | — |
| **LCW** | *"No other UC group writes lifecycle_state."* | Cross-reference state-write UC ownership. | **PASS** — Only designated LCW UCs write state. No inline state writes in non-LCW UCs. | — |

### 3.4 DRY and Reusability

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **DRY-SM-01** | *"If multiple entities share the same lifecycle pattern, extract a generic state machine via submachine states."* | Compare Session(6), Plan(7), Plugin(6), Subagent(5) topology patterns. | **VIOLATION** — Session and Plan share an "Approvable" pattern (CREATED→ACTIVE→SAVED/BRANCHED→CLOSED and EMPTY→BUILDING→QUEUED→APPROVED→EXECUTING→COMPLETED). No submachine states extracted. | **MEDIUM** |
| **DRY-SM-03** | *"Never copy-paste a full state machine diagram. Always reference the reusable one."* | Check for duplicated `skinparam state {}` blocks. | **VIOLATION** — Identical `skinparam state { BackgroundColor #FFFFFF; BorderColor #424242; FontColor #000000 }` block is duplicated verbatim in all 5 SM `.puml` files (lines 15-19 in session, 14-18 in plan, etc.). Should be extracted to a shared PlantUML include. | **MEDIUM** |

### 3.5 Traceability (TRC-SM)

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **TRC-SM-01** | *"Maintain SM registry: table listing each SM, its owning C4 element, and UCs that trigger transitions."* | Check SM README. | **VIOLATION** — SM README has diagram inventory (file+scope) but lacks "owning C4 element" column. Lifecycle-write UC mapping partially covers UC triggers for Session/Plan/Plugin but not for Agent or Subagent. | **MEDIUM** |
| **TRC-SM-02** | *"Every SM diagram includes a note listing related UC IDs."* | Check all 5 SM `.puml` files for UC ID inventory notes. | **VIOLATION** — UC IDs are embedded in transition labels but NO explicit `note` block lists all relevant UC IDs in any SM file. Header comments mention conventions but are not UC ID inventories. | **LOW** |
| **TRC-SM-04** | *"SM diagrams are versioned with the same lifecycle as the C4 model."* | Compare version strings across layers. | **VIOLATION** — Agent FSM: 8.0.0, Session: 8.0.0, Subagent: 9.1.0, C4 Agent: 9.1.0, C4 Context: 9.1.3, SQ AGT-01: 9.1.0. Minor versions inconsistent across layers. | **LOW** |

---

## 4. Sequence Diagrams — Rule-by-Rule CAR

### 4.1 Structure and C4 Level

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **C4 Level** | *"Every SQ diagram operates at exactly one C4 level. Never mix levels."* | Sample 21 SQs (one per group). | **PASS** — Container-level (AGT-01 shows User→ServerRouter→AgentOrchestrator→ToolRegistry). No class-level internals. | — |
| **Actor rule** | *"ALL diagrams must have actors and full entry chains."* | Check 21 sampled SQs for `actor` declaration and entry chain. | **PASS** — Every sampled SQ has `actor "User"` with full entry chain: `User→ServerRouter→AgentOrchestrator` (CLI/API) or `User→MCPClientRuntime` (MCP). | — |
| **Entry Chain** | *"CLI: Developer→REPLSession→AgentOrchestrator. HTTP: HTTPClient→ServerRouter."* | Check CLI and SRV SQs. | **PASS** — All SQs route through `ServerRouter`. CLI-specific SQs show `User→ServerRouter→AgentOrchestrator` (API-First convention). | — |

### 4.2 Note Sections

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **Intro note** | *"All 7 fields: Scope, Preconditions, Contexts, Excludes, Rollback, Design, Returns."* | Check `sq_agt01.puml:37-49`. | **PASS** — All 7 present plus Classification and Returns with success/failure codes. | — |
| **Summary note** | *"All 4 fields: Flow, State, Success, Failure."* | Check `sq_agt01.puml:114-120`. | **PASS** — Flow, State, Success, Failure + Invariant. | — |
| **Intro Returns** | *"Returns field lists all success/failure codes."* | Check `sq_agt01.puml:47-48`. | **PASS** — Success: `AgentEvent(Done)`. Failure: `AgentEvent(Error)`. Structured types for internal component. | — |
| **Summary Success/Failure** | *"Success/Failure include HTTP status codes or structured result types."* | Check `sq_agt01.puml:117-119`. | **PASS** — Structured result types for internal component. | — |

### 4.3 ROD/CSR Compliance

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **ROD names** | *"UC_ID METHOD ResourceName(parameters) on all manager-level calls."* | Check message labels in 21 sampled SQs. | **PASS** — `agt03 UPDATE Conversation`, `prv02 REQUEST Chat`, `hk04 DISPATCH Pre-LLM Hook`. | — |
| **Bare functions** | *"No bare function names like add_message, compact."* | Check for non-ROD labels. | **PASS** — `dispatchMessage(session_id, message)` at line 56 is an adapter-level call from router→agent (no UC prefix needed — correct per rules). All manager-level calls use ROD format. | — |
| **CSR chain** | *"Actor → Interface → Controller → Service → Repository. No layer skipped."* | Trace call chain in AGT-01 (lines 53-107). | **PASS** — `User→ServerRouter(Controller)→AgentOrchestrator(Service)→ToolRegistry(Repository)`. Complete chain. | — |
| **CSR forbidden** | *"Controller→Repository: NO. Repository→Service: NO. Actor→Service: NO."* | Check for violations. | **PASS** — Zero forbidden cross-layer calls in all sampled SQs. | — |

### 4.4 Box Color Compliance

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **Box colors** | *"Box colors project-configured. Follow those values; do not invent new colors."* | Compare SQ box hex values against canoncial CSR palette from `common/c4_styles.puml` (Controller=#E3F2FD, Service=#FFF3E0, Repository=#E8F5E9, Infrastructure=#F3E5F5). | **VIOLATION** — Sampled 3 SQs (AGT-01, SSN-01, SRV-01). 7 of 12 box declarations (58%) use wrong colors: | **CRITICAL** |
| | | AGT-01: API Group box = #E8EAF6 (should be #E3F2FD), Agent Group box = #E3F2FD (should be #FFF3E0), Tool Group box = #F3E5F5 (should be #E8F5E9), Safety = #FFF9C4 (invented, not in palette), Observability = #E0F2F1 (invented, not in palette). | |
| | | SSN-01: API Group box = #E8F5E9 (Green=Repository, should be Blue=Controller), Agent Group box = #E3F2FD (Blue=Controller, should be Orange=Service), Session Group = #F1F8E9 (drift from #E8F5E9). | |
| | | SRV-01: Service Group box = #F3E5F5 (Purple=Infrastructure, should be Orange=Service). | |

### 4.5 State Machine Integration (SM-01 through SM-05)

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **SM-01/SM-02** | *"Before any state-changing operation, service must retrieve entity. Insert `alt` fragment: valid state→proceed, invalid→error."* | Check entity SQs (SSN, PLG) for state guard `alt` fragments. | **PENDING** — Requires reading entity SSN/PLG SQ files. AGT-01 uses `alt` for text vs tool_calls branching (correct for Process FSM), not entity state guards. Not confirmed for Session/Plan/Plugin entity SQs. | **PENDING** |
| **SM-04** | *"State transitions with side effects model them after state change or in `par` fragments."* | Check entity SQs for side-effect modeling. | **PENDING** — Requires reading entity SQs. | **PENDING** |
| **State annotation** | *"note right with State: <back:#HEX>FROM</back> → <back:#HEX>TO</back>. Hex from lifecycle.md."* | Check `sq_agt01.puml:116`. | **PASS** — `IDLE→SERVING→THINKING→TOOL_EXEC→RESPONDING→IDLE`. Hex values match SM README. | — |

### 4.6 Combined Fragments and DRY

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **Combined fragments** | *"Every diagram shows ≥1 failure/alternative path. `break` for failures."* | Check `sq_agt01.puml:79-108`. | **PASS** — `alt` (text vs tools), `loop` (per tool_call), `break` (error). Coverage complete. | — |
| **Failure prop** | *"All failure paths propagate back through full entry chain to actor."* | Check `break` block (line 102-108). | **PASS** — `agent→router→user`: full chain with status codes at each layer. | — |
| **DRY-01 ref** | *"Extract common patterns (safety, logging, CRUD) into separate SQs. `ref` must specify UC ID."* | Check `ref` blocks in AGT-01. | **PASS** — OBS-03 (trace correlation), AGT-03 (conversation update), HK-04/HK-05 (hooks), PRV-02 (provider), AGT-02 (tool dispatch). Standard patterns extracted as refs. | — |
| **DRY-02** | *"ref block must specify UC ID and brief description."* | Check ref label format. | **PASS** — `AGT-03: UPDATE Conversation (append user message)`. UC ID + description in all refs. | — |

### 4.7 Agent System SQ Rules

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **Agent rules** | *"Event emission, not print(). Provider via Protocol. Safety ref before tool. ErrorBoundary."* | Check AGT-01 for event yields. | **PASS** — `AgentEvent(Done)` and `AgentEvent(Error)` emitted. Provider via ref (PRV-02). Safety via ref (AGT-02 includes safety pipeline). ErrorBoundary referenced in Design note. | — |
| **Agent rules** | *"State overlays for agent states with hex colors."* | Check for `hnote` at state transitions. | **PASS** — Summary note line 116 shows full state transition chain with hex colors: `<back:#ECEFF1>IDLE</back>` → `<back:#E0F7FA>SERVING</back>` → etc. | — |

---

## 5. Cross-Cutting Methodology Compliance

### 5.1 CSR Pattern (Controller-Service-Repository)

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **CSR-01** | *"Every backend operation has Controller entrypoint that delegates to Service."* | Verify C4 groups typed correctly: Controller(CLI,API), Service(Agent,Router,Provider,Safety,ContextGraph,EditStrategy,Evaluation), Repository(Session,Tool,Memory,Config,Git,RepoIntelligence). | **PASS** — Stereotypes correct in all 21 component groups. Color-coded in C4. | — |
| **CSR-02** | *"Services never import persistence frameworks. All data access through Repository interfaces."* | Check in SQ: AgentOrchestrator→SessionStore called through ref, not direct SQL. | **PASS** — Correct delegation pattern in SQs. Data access goes through repository layer. | — |
| **CSR-03** | *"Repositories scoped to single aggregate/entity."* | Check SessionStore (single: Session aggregate), ToolRegistry (single: Tool ABC). | **PASS** — One repo per aggregate. | — |
| **CSR-06** | *"Domain entities persistence-ignorant."* | Check CL diagram. Entities carry no persistence annotations. | **PASS** — Pure domain objects. | — |
| **CSR gap** | *"Infrastructure layer (MCP, Sandbox, Observability, Wire Log, Hooks, Plugins) — where does it fit?"* | C4 README defines Infrastructure as a 4th layer (Purple). SQ rules define 3-layer CSR. | **PASS** — Infrastructure is acknowledged as a cross-cutting layer that spans CSR boundaries. C4 README line 97: "Purple — Infrastructure: MCP, Sandbox, Observability, Wire Log, Hooks, Plugins." This is a documented 4th layer extension to the standard 3-layer CSR. | — |

### 5.2 ROD / Resource-Oriented Design

| Rule | Challenge | Action | Result | Sev |
|------|-----------|--------|--------|-----|
| **ROD-1** | *"Every endpoint hangs off a noun resource with full name."* | Check OpenAPI paths from UC README API table. | **PASS** — `/v1/sessions`, `/v1/sessions/{session}/messages`, `/v1/tools`, `/v1/config`. | — |
| **ROD-4** | *"No verbs-as-paths. No /doX, /runY."* | Check for verb path segments. | **PASS** — Custom methods use AIP-136 `:send` suffix. No verb paths. | — |
| **ROD-5** | *"Every List has page_size + page_token + next_page_token."* | Check List operations. | **PASS** — Pagination params present in API-01, API-07, API-08. | — |
| **ROD-6** | *"Every partial Update has update_mask."* | Check PATCH operations. | **PASS** — `update_mask` parameter present in API-04, API-11. | — |
| **ROD-7** | *"Lifecycle state is OUTPUT_ONLY. Changed via methods, not direct PATCH."* | Verify `state` field has `readOnly: true`. | **PASS** — Session.state is readOnly. State changed via API-02/API-04 custom methods, not direct PATCH. | — |
| **ROD-8** | *"Error body has code + message + status + details (AIP-193)."* | Check ErrorResponse schema. | **PASS** — Canonical AIP-193 format. | — |
| **ROD-10** | *"Resource hierarchy matches C4/UC design chain."* | Verify OpenAPI resources map to C4 components. | **PASS** — Sessions→SessionStore, Tools→ToolRegistry, Config→ConfigLoader. | — |

### 5.3 OOP Principles

| Principle | Evidence | Verdict |
|-----------|----------|---------|
| **Provider Protocol** | `Provider` is Protocol (not concrete). LiteLLMProxy implements it. C4 diagrams show Protocol abstraction. | **PASS** |
| **Tool ABC** | `Tool` ABC with 16 polymorphic implementations via `execute()`. ToolRegistry manages all tools through the ABC interface. | **PASS** |
| **Composition > Inheritance** | AgentOrchestrator composes 7+ services (ConversationHistory, ContextCompactor, PlanSession, SubagentCoordinator, PersonaManager, ErrorBoundary, SafetyCoordinator). No deep inheritance beyond 2 levels for any class. | **PASS** |
| **Strategy Pattern** | `EditStrategy` ABC with 8 implementations (SearchReplaceCoder, WholeFileCoder, UnifiedDiffCoder, etc.). StrategySelector applies heuristics to pick the right strategy. | **PASS** |
| **Structured Errors** | `ToolResult(success, content, error)` — structured error type with success flag. AgentEvent(Error) for agent-level failures. | **PASS** |
| **Event-Driven** | AgentEvent hierarchy: TextChunk, ToolStart, ToolResult, Error, Done. Pattern matching on event types in renderer. | **PASS** |
| **DTO Pattern** | Request/Response DTO separation in SRV group UCs. Controller→Service→Repository with type conversions at each boundary. SQ diagrams show DTO flow per CSR layering. | **PASS** |
| **Dependency Injection** | ConfigLoader provides typed Config dataclass. Provider registered via factory/registry pattern. Tool instances registered in ToolRegistry. | **PASS** |

### 5.4 DRY Assessment

| Pattern | Challenge | Evidence | Verdict |
|---------|-----------|----------|---------|
| **entities.md** | *Single source of truth for names.* | All C4→UC→SM→SQ names traceable, BUT `entities.md` does not exist (see SE-06). | **CRITICAL** |
| **ref fragments** | *Reuse common SQ patterns.* | OBS-03, AGT-03, HK-04, PRV-02, HK-05 reused across multiple SQs. DRY-04 (generic CRUD ref) partially followed. | **PASS** |
| **SM skinparam** | *Identical skinparam blocks duplicated 5x.* | All 5 SM files have identical `skinparam state {}` blocks. Should be extracted to include. | **MEDIUM** |
| **SM submachines** | *No reusable lifecycle patterns extracted.* | Session/Plan share "Approvable" pattern but no extraction. | **MEDIUM** |
| **SBX/Sandbox duplication** | *Sandbox and DiffSandboxManager in both SBX and EDT groups.* | SBX has SandboxMonitor, SandboxExecutor; EDT has DiffSandboxManager. Role separation is correct (two different concerns), but SQ diagrams may duplicate isolation patterns. | **LOW** |
| **Verb drift** | *43 non-allowed verbs across 148 UCs.* | Only 18 of ~61 distinct verbs are in allowed lists. Majority of UCs use verbs outside the authorized vocabulary. | **CRITICAL** |

### 5.5 Design Chain Integrity

| Check | Method | Result | Sev |
|-------|--------|--------|-----|
| Layer ordering | C4→UC→SM→SQ→ER→CL→CT/DATA→CT/API→Code | 8 layers frozen, RDM active. Order maintained. | **PASS** |
| UC→SQ 1:1 | 148 UC IDs × 148 SQ files | **PASS** |
| SQ lifelines ⊂ C4 | Every participant in SQ exists as C4 component via `Component_Ext()` | **PASS** |
| SM transitions → UC IDs | Every transition label is a valid UC ID from UC README | **PASS** |
| State enums through chain | SM states → CT/DATA enum → CT/API enum | **PASS** (verified Session lifecycle) |
| Orphan components | C4 components without UC owners | **PASS** (0 orphans) |
| Cross-layer version | C4:9.1.0/9.1.3, SM:8.0.0/9.1.0, SQ:9.1.0 | **DRIFT** — Minor/patch inconsistent across layers. C4 context=9.1.3, container=9.0.0, SM subagent=9.1.0 but others=8.0.0. | **LOW** |
| **entities.md missing** | Referenced in 7+ C4 Source headers but file absent | **CRITICAL** |

---

## 6. Consolidated Violation Register

| # | Rule | File(s) | Finding | Sev |
|---|------|---------|---------|-----|
| **V01** | **entities.md / SE-06** | Project root | `entities.md` referenced as source of truth in 7+ C4 headers but file does not exist. CRITICAL design dependency missing. | **CRITICAL** |
| **V02** | **UC Verb List** | `docs/UC/README.md` (all 148 UCs) | 43 of ~61 distinct verbs are outside allowed core+domain verb lists. Only 18 verbs compliant. | **CRITICAL** |
| **V03** | **SQ Box Colors** | `docs/SQ/AGT/sq_agt01.puml`, `docs/SQ/SSN/`, `docs/SQ/SRV/` | 7/12 (58%) box declarations use wrong hex colors. Agent box=#E3F2FD(should be #FFF3E0), Tool box=#F3E5F5(should be #E8F5E9), Safety/Observability use invented colors not in palette. | **CRITICAL** |
| **V04** | **SIM-05** | `c4_nasim_context.puml:40` | `LAYOUT_WITH_LEGEND()` with zero `AddElementTag()` calls = empty auto-generated legend. Styles file warns against this. | **CRITICAL** |
| **V05** | **SM Transition Matrix** | `docs/SM/README.md` | No full source→event→target transition matrix for any of the 5 SMs. Only STAGING partial (2 rows) exists. | **CRITICAL** |
| **V06** | **C4 Arch Pattern** | `c4_nasim_component_agent.puml:41,48,49` | `AgentOrchestrator→ToolRegistry`, `AgentOrchestrator→SessionStore`, `AgentOrchestrator→MemoryStore` direct without Adapter layer. No `SessionStoreAdapter` or `MemoryStoreAdapter` component exists. | **HIGH** |
| **V07** | **SIM-05** | `c4_nasim_container.puml` | Dual legends: `LAYOUT_WITH_LEGEND()` + manual `legend right`. Overlap risk in rendering. | **HIGH** |
| **V08** | **Banned Verbs** | `API-02`, `API-03`, `API-09`, `API-10` | 4 banned verb violations: `CREATE`(API-02), `GET`(API-03, API-09, API-10). | **HIGH** |
| **V09** | **SM DRY-03** | All 5 SM `.puml` files | Identical `skinparam state {}` block duplicated verbatim 5 times. Should be extracted to shared include. | **MEDIUM** |
| **V10** | **SM Trans Label** | `sm_subagent_lifecycle.puml:23` | `[*] --> IDLE` missing UC-ID label. All other SMs include UC label on initial transition. | **MEDIUM** |
| **V11** | **SM TRC-01** | `docs/SM/README.md` | Registry lacks "owning C4 element" column. No UC triggers listed for Agent or Subagent SMs. | **MEDIUM** |
| **V12** | **SM Terminal States** | `sm_agent_lifecycle.puml:103` | No state marked as terminal with zero outgoing transitions. `IDLE→[*]` suggests termination but IDLE has outgoing transitions — contradiction. | **MEDIUM** |
| **V13** | **SM DRY-01** | `sm_session_lifecycle.puml`, `sm_plan_lifecycle.puml` | No generic submachine states extracted despite shared "Approvable" pattern. | **MEDIUM** |
| **V14** | **C4 Header Position** | All 24 `.puml` files | Header comment block placed AFTER `@startuml`. Rule states "before @startuml." | **MEDIUM** |
| **V15** | **C4 Style — skinparam** | `common/c4_styles.puml` | 6 `skinparam rectangle<<...>>` blocks violate "No skinparam blocks" recommended style. Document deviation or migrate. | **MEDIUM** |
| **V16** | **SIM-01 exceptions** | `c4_nasim_component_observability.puml` (17 elements) | Not in the SIM-01 documented exceptions table in C4 README. | **MEDIUM** |
| **V17** | **UC Verb** | `docs/UC/uc_plugins.puml` (ACT-01) | `ACTIVATE Plugin` uses verb not in allowed core/domain list. | **MEDIUM** |
| **V18** | **C4 Purpose** | `c4_nasim_context.puml:8-36` | Purpose field is 28 lines with abstraction notes. Rule: "One sentence." | **LOW** |
| **V19** | **C4 Comments** | `c4_nasim_component_overview_core.puml:105,110,118,122,129,134,138` | Section divider comments (`' ── Entry points ──`) violate minimal comments rule. Model should speak. | **LOW** |
| **V20** | **SM State Def** | `docs/SM/README.md` | Agent FSM states table missing "owning LCW UC" column. Subagent SM has NO state definition table. | **LOW** |
| **V21** | **SM TRC-02** | All 5 SM `.puml` files | No explicit UC ID inventory note blocks (UC IDs embedded in transition labels instead). | **LOW** |
| **V22** | **Cross-version drift** | C4, SM, SQ headers | C4:9.1.3, Container:9.0.0, SM:8.0.0, SM Subagent:9.1.0, SQ:9.1.0. Minor/patch inconsistent. | **LOW** |
| **V23** | **SIM-01** | `c4_nasim_component_edit_strategy.puml` (13 elements) | Marginal overage, not documented as exception. | **LOW** |

### Severity Legend

| Severity | Meaning | Count |
|----------|---------|-------|
| **CRITICAL** | Renders artifact non-compliant or misleading; blocks design chain integrity | 5 |
| **HIGH** | Architectural or structural deficiency with significant impact | 3 |
| **MEDIUM** | Rule violation with moderate impact; systematic pattern | 8 |
| **LOW** | Style, minor inconsistency, or documentation gap | 7 |
| **PASS** | All checks clear | ~60 |
| **PENDING** | Requires further artifact reading | 2 |

---

## 7. Priority Recommendations

### Immediate (CRITICAL) — 5 items

1. **R1 — Create `entities.md`**: Author the canonical entity registry at `docs/entities.md`. Include all component names, domain entities, verb extension registrations, and reference from all C4 headers. This is a prerequisite for SE-06 (DRY) compliance and the sole source of truth for 200+ cross-layer names. BLOCKED by file absence.

2. **R2 — Reconcile UC verb list**: Either (a) expand the allowed core+domain verb list to include the 43 non-allowed verbs used across 148 UCs, or (b) rename all non-compliant UCs. Option (a) is recommended to preserve existing UCs. Update `entities.md` with the expanded list.

3. **R3 — Fix SQ box colors across all 148 files**: Audit every SQ box declaration against the canonical CSR palette:
   - Controller (CLI, API Group) = `#E3F2FD`
   - Service (Agent, Router, Provider, Safety, Context Graph, Edit Strategy, Evaluation) = `#FFF3E0`
   - Repository (Session, Tool, Memory, Config, Git, Repo Intelligence) = `#E8F5E9`
   - Infrastructure (MCP, Sandbox, Observability, Wire Log, Hooks, Plugins) = `#F3E5F5`

4. **R4 — Fix legend mechanism in context diagram**: Replace `LAYOUT_WITH_LEGEND()` with manual `legend right` block (consistent with `c4_styles.puml`). Container diagram: remove `LAYOUT_WITH_LEGEND()` and keep only the manual `legend right`.

5. **R5 — Author full transition matrices for all 5 SMs**: Document comprehensive source→event→target tables in `docs/SM/README.md` for Agent (40+ rows), Session (10+ rows), Plan (12+ rows), Plugin (8+ rows), and Subagent (6+ rows) state machines.

### Short-term (HIGH) — 4 items

6. **R6 — Add Adapter layer in Agent Group**: Create `SessionStoreAdapter`, `MemoryStoreAdapter` components. Update `AgentOrchestrator`→`SessionStore` direct to go through adapter. Follow the pattern from Memory Group which already has adapters (`EpisodicMemoryAdapter`, `SemanticMemoryAdapter`, `WorkingMemoryAdapter`). `AgentOrchestrator`→`ToolRegistry` is debatable since ToolRegistry IS the repository/registry pattern.

7. **R7 — Fix banned verbs in API Group**: Rename `API-02 CREATE Session` → `API-02 INSERT Session`, `API-03 GET Session` → `API-03 READ Session`, `API-09 GET Tool` → `API-09 READ Tool`, `API-10 GET Config` → `API-10 READ Config`.

8. **R8 — Document SIM-01 exceptions for observability(17) and edit_strategy(13)**: Add these 2 diagrams to the exceptions table in C4 README with rationale. Observability is tightly coupled (logger↔metrics↔correlator). Edit strategy needs all 11 implementations visible for strategy pattern comprehension.

9. **R9 — Fix subagent initial UC label**: Add `: AGT-09` to `[*] --> IDLE` in `sm_subagent_lifecycle.puml`.

### Medium-term (MEDIUM) — 5 items

10. **R10 — Extract shared `skinparam state` block**: Move the identical block from all 5 SM files to `docs/SM/common/sm_styles.puml` and reference via `!include`.

11. **R11 — Move C4 headers before `@startuml`**: Reorder all 24 `.puml` files so the structured header block precedes `@startuml` (per c4.md specification).

12. **R12 — Add SM submachine states for shared patterns**: Extract "Approvable" submachine shared by Session and Plan lifecycles. Follow DRY-SM-01..04 rules.

13. **R13 — Audit all 148 SQs for SM-01/02/04 state guard compliance**: Entity SQs (SSN, PLG) must be verified for `alt` state guard fragments and SM reference notes.

14. **R14 — Add "owning C4 element" column to SM README registry**: Complete the TRC-SM-01 requirement.

### Low priority (LOW) — 5 items

15. **R15 — Add `ACTIVATE` to domain verb list** in `entities.md` or rename to `LOAD Plugin`.

16. **R16 — Shorten C4 context diagram Purpose field** to one sentence.

17. **R17 — Remove section divider comments** from overview diagrams (minimal comments rule).

18. **R18 — Align version numbers** across C4, SM, SQ layers to a consistent scheme.

19. **R19 — Add Subagent state definition table** to SM README.

---

## 8. Previous Audit Resolution Status

Tracking findings from `2026.06.27.deepseek.v4.flash.md`:

| # | Previous Finding | Status | This Audit |
|---|------------------|--------|------------|
| V01 | Context: empty legend (LAYOUT_WITH_LEGEND + no AddElementTag) | **UNRESOLVED** | V04 (CRITICAL) |
| V02 | Container: dual legends | **UNRESOLVED** | V07 (HIGH) |
| V03 | SM: no transition matrix | **UNRESOLVED** | V05 (CRITICAL) |
| V04 | Container: 22 elements exceeds SIM-01 | **RESOLVED** — Exception documented | PASS |
| V05 | Agent: direct AgentOrchestrator→SessionStore/MemoryStore | **UNRESOLVED** | V06 (HIGH) |
| V06 | SQ box colors drift | **UNRESOLVED** | V03 (CRITICAL) |
| V07 | C4 headers after @startuml | **UNRESOLVED** | V14 (MEDIUM) |
| V08 | SM terminal states | **UNRESOLVED** | V12 (MEDIUM) |
| V09 | SM DRY: no submachine states | **UNRESOLVED** | V13 (MEDIUM) |
| V10 | SM TRC-01: registry incomplete | **UNRESOLVED** | V11 (MEDIUM) |
| V11 | C4 skinparam style | **UNRESOLVED** | V15 (MEDIUM) |
| V12 | UC verb: ACTIVATE | **UNRESOLVED** | V17 (MEDIUM) |
| V13 | C4 Purpose field too long | **UNRESOLVED** | V18 (LOW) |
| V14 | SM state def missing LCW column | **UNRESOLVED** | V20 (LOW) |
| V15 | SM TRC-02: no UC note | **UNRESOLVED** | V21 (LOW) |
| V16 | Cross-layer version drift | **UNRESOLVED** | V22 (LOW) |

**NEW findings in this audit (not in previous):**
- V01: entities.md missing (CRITICAL) — previously reported as PASS
- V02: 43 non-allowed UC verbs (CRITICAL) — not previously detected
- V08: 4 banned verb violations (HIGH) — previously missed (only ACTIVATE flagged)
- V09: SM skinparam duplication (MEDIUM) — not previously detected
- V10: Subagent initial transition missing UC label (MEDIUM) — not previously detected
- V16: Observability(17) and edit_strategy(13) not in SIM-01 exceptions (MEDIUM) — not previously detected
- V19: Section divider comments in overview (LOW) — not previously detected
- V23: Edit strategy 13 elements marginal (LOW) — not previously detected

**Resolution rate:** 1/16 previous findings resolved (6.25%). Fifteen findings carry over unresolved, with 8 new findings adding to the backlog. Expect significant remediation effort for CRITICAL items (entities.md creation, verb reconciliation, box color correction across 148 SQs).

---

*End of CAR audit v5 — 2026-06-27*
