"""Server-side bridge — Anthropic Messages API proxy to Ollama.

:mod:`nasim.bridge.server` is a thin FastAPI transport; all translation lives in
the pure, I/O-free :mod:`nasim.bridge.translator` package.
"""
