# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Test / lint caches
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtualenvs
.venv/
venv/

# Per-machine config override (never committed)
cfg/nasim.local.toml

# E2E scratch workspace
test/e2e/_workspace/

# Exported Claude Code session transcripts (working artifacts, not docs)
docs/*-local-command-caveat*.txt

# Editor / OS
*.swp
.DS_Store

# Project .claude/ (Claude brain for this project) — gitignored on BB origin per global rules.
# Enriched only to personal gh-private via post-commit hook. .grok/ is the primary committed knowledge here.
.claude/

# Per-run test artifacts, temp tunnels, litellm sessions, etc.
*.tmp
nasim-*.log
/tmp/nasim-*
/tmp/litellm-nasim-*

# CI caches if any
.github/workflows/_tmp/
