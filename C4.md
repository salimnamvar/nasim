

--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/README.md ---

# NASIM Application — C4 Diagram Inventory

## How to Read These Diagrams

The C4 model provides a hierarchical way to understand software architecture at different zoom levels. NASIM Application's diagram set follows this hierarchy:

```
Level 1: Context    →  c4_nasim_context.puml
Level 2: Container  →  c4_nasim_container.puml
Level 3: Component  →  c4_nasim_component_overview.puml (high-level)
                     →  c4_nasim_component_*.puml (per-group details)
```

### Reading Path

1. **Start with Context** (`c4_nasim_context.puml`): Understand what NASIM Application is and what external systems it interacts with.
2. **Move to Container** (`c4_nasim_container.puml`): See the 3 runtime entry points: CLI Process, HTTP API Server, and Core Engine. Note: NASIM Application runs as a single Python process — CLI and HTTP are modeled as separate Containers because they represent distinct runtime entry points and responsibilities.
3. **Explore Overview** (`c4_nasim_component_overview.puml`): See all 21 component groups inside the Core Engine, color-coded by CSR layer.
4. **Dive into Details** (`c4_nasim_component_*.puml`): Each per-group diagram shows internal components, relationships, and external dependencies.

## C4 Diagram Set

### Level 1: System Context

| Diagram | Level | Description |
|---------|-------|-------------|
| `c4_nasim_context.puml` | Context | NASIM Application as a single system with User actor and 15 external systems |

### Level 2: Containers

| Diagram | Level | Description |
|---------|-------|-------------|
| `c4_nasim_container.puml` | Container | 3 runtime entry points: CLI Process, HTTP API Server, Core Engine + 4 external interface containers |

### Level 3: Components

| Diagram | Level | Description |
|---------|-------|-------------|
| `c4_nasim_component_overview.puml` | Component (Overview) | High-level view of all 21 groups with CSR layer coloring and key cross-group relationships |

## Per-Group Component Diagrams (21 diagrams)

Each per-group diagram shows internal components within `Boundary(nasim_application, "NASIM Application")` and `Boundary(group_name)`. Cross-group references use `Component_Ext(alias, "ComponentName", "Group Name")`.

### Controller Layer (Blue)

| Diagram | Group | Key Components |
|---------|-------|----------------|
| `c4_nasim_component_cli.puml` | CLI Group | ArgParser, REPLSession, Renderer, SlashCommandHandler |
| `c4_nasim_component_server.puml` | API Group | ServerApp, ServerRouter, SSEHandler, APISchema |

### Service Layer (Orange)

| Diagram | Group | Key Components |
|---------|-------|----------------|
| `c4_nasim_component_agent.puml` | Agent Group | AgentOrchestrator, ConversationHistory, ContextCompactor, PlanSession, SubagentCoordinator, ErrorBoundary, PersonaManager |
| `c4_nasim_component_router.puml` | Router Group | ModelRouter, FallbackChain, ProviderCapabilities |
| `c4_nasim_component_provider.puml` | Provider Group | Provider (Protocol), LiteLLMProxy |
| `c4_nasim_component_safety.puml` | Safety Group | SafetyCoordinator, PermissionGate, InjectionScanner, EgressInspector |
| `c4_nasim_component_context_graph.puml` | Context Graph Group | ContextGraph, PipelineOrchestrator, ContextPrioritizer, TruncationProcessor, DistillationProcessor, InjectionProcessor, CompactionProcessor, TokenBudgetTracker |
| `c4_nasim_component_edit_strategy.puml` | Edit Strategy Group | EditStrategyManager, EditStrategy (ABC), SearchReplaceCoder, WholeFileCoder, UnifiedDiffCoder, FencedBlockCoder, FunctionLevelCoder, DiffSandboxCoder, ArchitectCoder, InlinePatchCoder, StrategySelector |
| `c4_nasim_component_evaluation.puml` | Evaluation Group | EvaluationEngine, TaskEvaluator, SuccessCheckRunner, LLMReviewer, TestRunner, RetryCoordinator, RepetitionDetector, TurnBudgetInjector, QualitySignal |

### Repository Layer (Green)

| Diagram | Group | Key Components |
|---------|-------|----------------|
| `c4_nasim_component_session.puml` | Session Group | SessionStore, SessionVersioning, SessionSearch, SessionFork |
| `c4_nasim_component_tools.puml` | Tool Group | Tool (ABC), ToolRegistry, FileTools, SearchTools, ShellTool, DirTool, WebTools, GitTool, LspTool, SubagentTool, TodoTool, MemoryTool, PlanTool, RepoMapTool, SemanticSearchTool, ReviewTool |
| `c4_nasim_component_memory.puml` | Memory Group | MemoryStore, MemoryIndex, MemoryScope, EpisodicMemoryAdapter, SemanticMemoryAdapter, WorkingMemoryAdapter, MemoryRetriever, MemoryIndexer |
| `c4_nasim_component_config.puml` | Config Group | ConfigLoader, Config (dataclass) |
| `c4_nasim_component_git.puml` | Git Group | GitIntegration, GitStatus, GitCommit |
| `c4_nasim_component_repo_intelligence.puml` | Repo Intelligence Group | RepoIntelligenceManager, ASTIndexAdapter, SymbolGraph, RankingService, EmbeddingAdapter, SemanticSearchService, RepoMapBuilder |

### Infrastructure Layer (Purple)

| Diagram | Group | Key Components |
|---------|-------|----------------|
| `c4_nasim_component_mcp.puml` | MCP Group | MCPClientRuntime, MCPServerRuntime, MCPToolAdapter, MCPDiscovery |
| `c4_nasim_component_sandbox.puml` | Sandbox Group | SandboxExecutor, SandboxPolicy, SandboxMonitor, ResourceLimiter, DiffSandboxManager, EditStagingArea, DiffComputer, DiffPresenter, StagedApplicator |
| `c4_nasim_component_observability.puml` | Observability Group | StructuredLogger, MetricsCollector, TraceCorrelator, ContextPropagator, LogRedactor, DualOutputAdapter, InstrumentationMiddleware, OTelExporter |
| `c4_nasim_component_wire_log.puml` | Wire Log Group | WireLog, WireAppender, WireReader, SessionForkManager |
| `c4_nasim_component_hooks.puml` | Hooks Group | HookManager |
| `c4_nasim_component_plugins.puml` | Plugins Group | PluginLoader |

**Total: 24 C4 diagrams (1 context + 1 container + 1 overview + 21 group components)**

## CSR Layering & Visual Coding

Each component group is color-coded by its CSR layer:

| Color | Layer | Groups |
|-------|-------|--------|
| Blue | **Controller** | CLI Group, API Group |
| Orange | **Service** | Agent Group, Router Group, Provider Group, Safety Group, Context Graph Group, Edit Strategy Group, Evaluation Group |
| Green | **Repository** | Session Group, Tool Group, Memory Group, Config Group, Git Group, Repo Intelligence Group |
| Purple | **Infrastructure** | MCP Group, Sandbox Group, Observability Group, Wire Log Group, Hooks Group, Plugins Group |

### CSR Pattern Flow

```
User → Controller (CLI/API) → Service (Agent/Router/Safety/...) → Repository (Session/Tool/Memory/...)
```

**Key delegation paths:**
- `AgentOrchestrator` → `SafetyCoordinator` (safety validation before every tool dispatch)
- `AgentOrchestrator` → `ErrorBoundary` (structured error handling with recovery)
- `AgentOrchestrator` → `ContextGraph` (context pipeline orchestration)
- `ServerRouter` → `AgentOrchestrator` (sole entry gate for all interface containers)

## Architecture Differentiators vs Reference Agents

### vs aider
- **CSR enforcement**: nasim strictly separates Controller → Service → Repository. aider mixes concerns.
- **SafetyCoordinator**: Dedicated safety pipeline with PermissionGate, InjectionScanner, EgressInspector. aider has ad-hoc safety checks.
- **ErrorBoundary**: Structured error hierarchy with recovery actions. aider uses generic try/except.

### vs Cline
- **No God Objects**: nasim decomposes functionality into 21 focused groups. Cline often has monolithic controllers.
- **ContextGraph**: Dedicated context pipeline with prioritization, truncation, distillation, injection, compaction. Cline uses simpler context management.
- **EditStrategyManager**: Polymorphic edit strategies (8 implementations) vs Cline's single approach.

### vs Claude Code
- **CSR visibility**: nasim's diagrams make the layering explicit through color coding and stereotypes.
- **SafetyCoordinator**: Proactive safety pipeline vs Claude Code's reactive approach.
- **Observability**: Dedicated StructuredLogger, MetricsCollector, TraceCorrelator with OTel support. Claude Code has minimal observability.

### vs Roo-Code
- **Container clarity**: nasim's 3-runtime-entry-point model (CLI, API Server, Core Engine) is cleaner than Roo-Code's architecture.
- **MCP integration**: nasim treats MCP as first-class with dedicated runtime, adapter, and discovery components.
- **Memory system**: nasim has 8 memory components (store, index, scope, adapters, retriever, indexer) vs Roo-Code's simpler approach.

## C4 Hierarchy

```
Context:   Person(User) → System(NASIM Application) → System_Ext(...)
Container: System_Boundary(nasim_application, "NASIM Application") {
               Container(cli_process, "CLI Process")
               Container(api_server, "HTTP API Server")
               Container(core_engine, "Core Engine")
           }
           Container_Ext(WebApp, DesktopApp, MobileApp) → Container(api_server)
Component: Boundary(nasim_application, "NASIM Application") {
               Boundary(cli_group) <<controller>> { ... }
               Boundary(api_group) <<controller>> { ... }
               Boundary(agent_group) <<service>> { ... }
               Boundary(router_group) <<service>> { ... }
               Boundary(safety_group) <<service>> { ... }
               Boundary(context_graph_group) <<service>> { ... }
               Boundary(edit_strategy_group) <<service>> { ... }
               Boundary(evaluation_group) <<service>> { ... }
               Boundary(session_group) <<repository>> { ... }
               Boundary(tool_group) <<repository>> { ... }
               Boundary(memory_group) <<repository>> { ... }
               Boundary(config_group) <<repository>> { ... }
               Boundary(git_group) <<repository>> { ... }
               Boundary(repo_intelligence_group) <<repository>> { ... }
               Boundary(mcp_group) <<infrastructure>> { ... }
               Boundary(sandbox_group) <<infrastructure>> { ... }
               Boundary(observability_group) <<infrastructure>> { ... }
               Boundary(wire_log_group) <<infrastructure>> { ... }
               Boundary(hooks_group) <<infrastructure>> { ... }
               Boundary(plugins_group) <<infrastructure>> { ... }
               Boundary(provider_group) <<service>> { ... }
           }
           Container_Ext(WebApp, DesktopApp, MobileApp) → ServerRouter
           Person(User) → REPLSession (CLI mode)
```

## Actors

| Actor | Description |
|-------|-------------|
| User | Human developer. Interacts via CLI (REPLSession) or via interface container apps (WebApp, DesktopApp, MobileApp → ServerRouter). |

## Key C4 Modeling Decisions

This section explains the C4 modeling choices that commonly confuse readers.

### Why CLI Process and HTTP API Server are modeled as separate Containers

NASIM Application runs as a **single Python process**. However, **CLI Process** and **HTTP API Server** are modeled as separate `Container` elements — not because they are separate deployments, but because they represent **distinct runtime entry points**.

In C4, a Container models either:
1. A separately deployable unit, OR
2. A distinct runtime responsibility with its own lifecycle

CLI Process and HTTP API Server fall into category 2. They share a process boundary but serve fundamentally different interaction models:
- **CLI Process**: Terminal REPL with argument parsing, rendering, and slash commands
- **HTTP API Server**: FastAPI server with HTTP endpoints and SSE streaming

Each has its own initialization sequence, request handling, and lifecycle management. Modeling them as separate Containers makes the architecture's entry points visible and allows readers to understand the two distinct ways users interact with NASIM Application.

### Why WebApp, DesktopApp, and MobileApp are modeled as Container_Ext

**WebApp** (JavaScript SPA), **DesktopApp** (Electron/Tauri), and **MobileApp** (React Native/Flutter) are modeled as `Container_Ext` because they are **genuinely separate deployable units** — each with its own process, codebase, and deployment pipeline.

These are **clients** of NASIM Application, not part of it. They communicate with NASIM Application over HTTP/SSE to the API Server. In C4:
- `Container` = inside the system boundary (NASIM Application owns it)
- `Container_Ext` = outside the system boundary but interacts with it

Since WebApp/DesktopApp/MobileApp are not developed, deployed, or versioned as part of NASIM Application, they correctly belong outside the boundary as `Container_Ext`.

### Container vs Component: When is something a Container?

A **Container** represents a unit with a distinct runtime responsibility — either a separate deployment or a separate entry point. A **Component** represents an internal module within a Container.

The **HTTP API Server** is a Container (not a Component) because:
- It is a distinct entry point with its own lifecycle
- It handles a fundamentally different interaction model (HTTP) than the CLI (terminal)
- It is not an internal module of the Core Engine — it *delegates to* the Core Engine

The **Core Engine** is a Container because it is the main application shell that contains all business logic components. Within it, components like `AgentOrchestrator`, `SafetyCoordinator`, and `ToolRegistry` are internal modules.

## Interface Containers (external, separate deployable units)

| Container | Type | Connection to NASIM Application |
|-----------|------|---------------------------------|
| WebApp | JavaScript SPA | HTTP/JSON + SSE to ServerRouter |
| DesktopApp | Electron / Tauri | HTTP/JSON + SSE to ServerRouter |
| MobileApp | React Native / Flutter | HTTP/JSON + SSE to ServerRouter |

These are separate deployable units that connect to NASIM Application's HTTP API Server. They are modeled as `Container_Ext` (outside the system boundary) because they are clients, not part of NASIM Application itself.

## External Systems

| System | Protocol | Purpose |
|--------|----------|---------|
| LLM Backend | HTTP/JSON | Multi-provider inference (Ollama, OpenAI, Anthropic, etc.) via litellm |
| Host Filesystem | path I/O | Read/write/search project files |
| Host Shell | subprocess | Execute shell commands (via sandbox) |
| Web | HTTP | Fetch documentation, search results |
| MCP Server | stdio/SSE | Extension tools via Model Context Protocol |
| MCP Client | stdio/SSE | External tools connecting to NASIM Application MCP server |
| Git Repository | git CLI | Version control for project files |
| Sandbox Runtime | OS primitives | OS-level process isolation (landlock, seccomp, bubblewrap) |
| Memory Backend | read/write | Long-term knowledge persistence |
| LSP Server | LSP protocol | Language server for code intelligence |
| Plugin Directory | filesystem | ~/.nasim/plugins/ — community extensions |
| Tree-sitter | tree-sitter CLI/lib | AST extraction for code intelligence |
| Embedding Model | HTTP/local | Vector embedding generation for semantic search |
| Vector Store | SQLite + vector | Embedding storage and similarity search |
| OTel Collector | OTLP/gRPC | OpenTelemetry trace and metric export (optional) |

## Architecture Principles

- **Single process:** NASIM Application runs as one Python process. CLI and HTTP API Server share the same process boundary but are modeled as separate Containers because they are distinct runtime entry points with different responsibilities (terminal REPL vs HTTP+SSE). This is a C4 modeling choice, not a deployment choice.
- **Container = runtime entry point OR separate deployment:** In C4, a Container does not always mean a separate process. It can also mean a distinct runtime responsibility within a shared process. CLI Process and HTTP API Server use this interpretation.
- **Container_Ext = separate deployable unit outside the system boundary:** WebApp, DesktopApp, and MobileApp are truly separate applications with their own processes. They are clients of NASIM Application, not part of it.
- **CSR Pattern:** Controller (CLI Group / API Group) → Service (Agent Group, Router Group, etc.) → Repository (Session Group, Tool Group, Memory Group, Config Group). Strict delegation.
- **One group, one Boundary:** Each component group is a `Boundary` inside `Boundary(nasim_application, "NASIM Application")`. Groups are logical, not deployable.
- **No Container_Ext for internals:** Cross-group references inside NASIM Application use `Component_Ext(alias, "ComponentName", "Group Name")`, never `Container_Ext` or `System_Ext`.
- **System_Ext for real externals only:** Filesystems, web, git repos, LLM backends, MCP servers, LSP servers — genuinely external infrastructure.
- **Version consistency:** All diagram headers use Version 9.0.0.
- **Pin C4-PlantUML:** All diagrams reference v2.10.0 — never `master`.

## Shared Styles

All diagrams include `common/c4_styles.puml` for:
- Consistent color palette per CSR layer
- Skinparam settings for readability
- Legend macro for CSR layer explanation

## Design Chain Consistency

All diagrams are traceable to the design chain:
- **C4** → **UC** (Use Case) → **SQ** (Sequence) → **SM** (State Machine)
- **Cross-layer sync verified:** All lifelines in SQ diagrams exist as C4 components.
- **100% design chain consistency** across 148 SQ diagrams and 24 C4 diagrams.



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_context.puml ---

@startuml c4_nasim_context
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Context.puml
!theme plain

' ============================================================
' Title:     NASIM Application — System Context
' Boundary:  NASIM Application
' Purpose:   Show NASIM Application as a code-agent system with one User actor
'            and all external systems it depends on or is consumed by.
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/entities.md
' Review:    —
' ============================================================

LAYOUT_LEFT_RIGHT()
LAYOUT_WITH_LEGEND()

title NASIM Application — System Context

Person(user, "User", "Developer using NASIM Application as an AI coding assistant")

System(nasim_application, "NASIM Application", "Code agent: assists developers with coding tasks via CLI or HTTP API. Integrates LLM providers, file tools, shell execution, web access, and MCP extensions.")

System_Ext(llm_backend, "LLM Backend", "Multi-provider inference: Ollama, OpenAI, Anthropic, and 100+ via litellm")
System_Ext(host_fs, "Host Filesystem", "Project files and directories read, written, and searched by NASIM Application")
System_Ext(host_shell, "Host Shell", "Shell for executing commands via sandbox isolation")
System_Ext(web, "Web", "Documentation pages, package APIs, and search engines")
System_Ext(mcp_server, "MCP Server", "Extension tools provided to NASIM Application via Model Context Protocol")
System_Ext(mcp_client, "MCP Client", "External tools that invoke NASIM Application as an MCP server")
System_Ext(git_repo, "Git Repository", "Version-controlled project files")
System_Ext(sandbox, "Sandbox Runtime", "OS-level process isolation (landlock, seccomp, bubblewrap)")
System_Ext(lsp_server, "LSP Server", "Language server providing code intelligence")
System_Ext(plugin_dir, "Plugin Directory", "Community plugins installed at ~/.nasim/plugins/")
System_Ext(tree_sitter, "Tree-sitter", "Incremental AST parser for source code indexing")
System_Ext(embedding_model, "Embedding Model", "Code embedding generation (local or API-based)")
System_Ext(vector_store, "Vector Store", "Persistent vector storage for semantic code search")
System_Ext(otel_collector, "OTel Collector", "OTLP/gRPC endpoint for optional trace and metric export")
System_Ext(obs_platform, "Observability Platform", "Log agent, Prometheus, Grafana. NASIM Application emits only — platform owns collection, storage, and visualization.")

Rel(user, nasim_application, "runs coding tasks via CLI or HTTP API", "terminal / HTTP + SSE")
Rel(nasim_application, llm_backend, "sends chat requests, receives completions", "HTTP/JSON")
Rel(nasim_application, host_fs, "reads, writes, and searches project files", "path operations")
Rel(nasim_application, host_shell, "executes shell commands via sandbox", "subprocess")
Rel(nasim_application, web, "fetches documentation and search results", "HTTP")
Rel(nasim_application, mcp_server, "discovers and invokes extension tools", "stdio / SSE")
Rel(mcp_client, nasim_application, "invokes NASIM Application tools via MCP protocol", "stdio / SSE")
Rel(nasim_application, git_repo, "reads status and creates commits", "git CLI")
Rel(nasim_application, sandbox, "executes commands in isolated environment", "OS primitives")
Rel(nasim_application, lsp_server, "queries code intelligence", "LSP protocol")
Rel(nasim_application, plugin_dir, "discovers and loads plugins at startup", "filesystem")
Rel(nasim_application, tree_sitter, "parses source code AST for indexing", "library / CLI")
Rel(nasim_application, embedding_model, "generates code embeddings", "HTTP / local")
Rel(nasim_application, vector_store, "stores and retrieves code embeddings", "SQLite + vector")
Rel(nasim_application, obs_platform, "emits structured logs to stdout; exposes /metrics for pull-scrape", "stdout + HTTP")
Rel(nasim_application, otel_collector, "exports traces and metrics (optional)", "OTLP/gRPC")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_config.puml ---

@startuml c4_nasim_component_config
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!include common/c4_styles.puml
!theme plain

' ============================================================
' Title:     NASIM Application — Config Component Diagram
' Boundary:  NASIM Application (container) — Config Group
' Purpose:   Config layer internal components
'            CSR Layer: Repository
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/prompt/p1.md — corrective architecture review
' Review:    docs/audit/audit.2026.06.20.comprehensive.reference.audit.md
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Config Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(config_group, "Config Group") <<repository>> {
        Component(config_loader, "ConfigLoader", "pydantic", "Loads global YAML, project YAML, env vars, CLI flags; validates at load")
        Component(config_schema, "Config", "dataclass", "Typed configuration: provider, model, safety_mode, context_budget, mcp_servers")
    }
}

System_Ext(global_config, "Global Config", "~/.nasim/config.yaml")
System_Ext(project_config, "Project Config", ".nasim/config.yaml")
System_Ext(env_vars, "Environment Variables", "NASIM_* env vars")

Rel(config_loader, global_config, "reads defaults")
Rel(config_loader, project_config, "reads project overrides")
Rel(config_loader, env_vars, "reads env overrides")
Rel(config_loader, config_schema, "produces validated Config")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_server.puml ---

@startuml c4_nasim_component_server
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!include common/c4_styles.puml
!theme plain

' ============================================================
' Title:     NASIM Application — API Component Diagram
' Boundary:  NASIM Application (container) — API Group
' Purpose:   API component group inside nasim. Shows how
'            interface containers connect to ServerRouter.
'            API is a component, not a container.
'            CSR Layer: Controller
' Milestone: v1.0
' Version:   9.0.0
' Source:    CAR refinement loop — C4 correction
' Review:    —
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — API Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(api_group, "API Group") <<controller>> {
        Component(server_app, "ServerApp", "FastAPI", "ASGI application factory with lifespan, middleware, route mounts")
        Component(server_router, "ServerRouter", "FastAPI Router", "ROD-compliant REST endpoints. Sole entry gate for all interface containers.")
        Component(sse_handler, "SSEHandler", "SSE", "Converts AgentEvent stream to SSE format for all interface clients")
        Component(api_schema, "APISchema", "Pydantic", "Request/response models with AIP-193 error format")
    }
}

Component_Ext(agent_orch, "AgentOrchestrator", "Agent Group")

Container_Ext(webapp, "WebApp", "JavaScript SPA", "Browser-based interface")
Container_Ext(desktop_app, "DesktopApp", "Electron / Tauri", "Desktop application")
Container_Ext(mobile_app, "MobileApp", "React Native / Flutter", "Mobile application")
System_Ext(mcp_client_ext, "MCP Client", "External tool or IDE connecting via MCP")

Rel(webapp, server_router, "HTTP requests + SSE streaming", "HTTP/JSON + SSE")
Rel(desktop_app, server_router, "HTTP requests + SSE streaming", "HTTP/JSON + SSE")
Rel(mobile_app, server_router, "HTTP requests + SSE streaming", "HTTP/JSON + SSE")
Rel(mcp_client_ext, server_router, "invokes tools via MCP protocol", "stdio / SSE")

Rel(server_app, server_router, "mounts routes")
Rel(server_router, api_schema, "validates requests/responses")
Rel(server_router, agent_orch, "delegates to agent orchestrator")
Rel(server_router, sse_handler, "streams via")
Rel(sse_handler, agent_orch, "consumes AgentEvents")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_sandbox.puml ---

@startuml c4_nasim_component_sandbox
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!include common/c4_styles.puml
!theme plain

' ============================================================
' Title:     NASIM Application — Sandbox Component Diagram
' Boundary:  NASIM Application (container) — Sandbox Group
' Purpose:   OS-level process isolation and diff staging for shell and edit operations
'            CSR Layer: Infrastructure
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/audit/audit.2026.06.20.deep.domain.audit.md
' Review:    —
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Sandbox Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(sandbox_group, "Sandbox Group") <<infrastructure>> {
        Component(sandbox_executor, "SandboxExecutor", "", "OS-level process isolation: landlock, seccomp, bubblewrap")
        Component(sandbox_policy, "SandboxPolicy", "", "Network domain allowlists, filesystem mount rules, exec restrictions")
        Component(sandbox_monitor, "SandboxMonitor", "", "Process monitoring, timeout enforcement, resource limits")
        Component(resource_limiter, "ResourceLimiter", "", "Enforces CPU, memory, and disk quotas per sandbox instance")
        Component(diff_sandbox_mgr, "DiffSandboxManager", "", "Manages sandboxed diff staging and application")
        Component(edit_staging_area, "EditStagingArea", "", "Stages file edits before sandbox-validated application")
        Component(diff_computer, "DiffComputer", "", "Computes diffs between original and staged content")
        Component(diff_presenter, "DiffPresenter", "", "Formats diffs for human review and approval")
        Component(staged_applicator, "StagedApplicator", "", "Applies approved staged diffs to filesystem")
    }
}

System_Ext(host_shell, "Host Shell", "subprocess execution")
System_Ext(host_fs, "Host Filesystem", "restricted mount points")

Rel(sandbox_executor, sandbox_policy, "applies policy before exec")
Rel(sandbox_executor, sandbox_monitor, "monitors running process")
Rel(sandbox_executor, resource_limiter, "configures limits before exec")
Rel(sandbox_policy, resource_limiter, "defines quota rules")
Rel(resource_limiter, host_fs, "enforces disk quotas")
Rel(resource_limiter, host_shell, "enforces CPU/memory via cgroups/rlimits")
Rel(sandbox_executor, host_shell, "runs isolated subprocess")
Rel(sandbox_executor, host_fs, "restricted file access")
Rel(diff_sandbox_mgr, edit_staging_area, "stages edits")
Rel(diff_sandbox_mgr, sandbox_executor, "validates in sandbox")
Rel(edit_staging_area, diff_computer, "computes diff")
Rel(edit_staging_area, diff_presenter, "presents for review")
Rel(diff_computer, host_fs, "reads original files")
Rel(staged_applicator, edit_staging_area, "reads approved edits")
Rel(staged_applicator, sandbox_executor, "applies in sandbox")
Rel(staged_applicator, host_fs, "writes final files")
Rel(diff_presenter, diff_sandbox_mgr, "notifies approval")
Rel(diff_sandbox_mgr, staged_applicator, "applies approved diffs")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_repo_intelligence.puml ---

@startuml c4_nasim_component_repo_intelligence
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!theme plain
!include common/c4_styles.puml


' ============================================================
' Title:     NASIM Application — Repo Intelligence Component Diagram
' Boundary:  NASIM Application (container) — Repo Intelligence Group
' Purpose:   Codebase intelligence: AST indexing, symbol graph, semantic search, repo mapping
'            CSR Layer: Repository
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/audit/audit.2026.06.20.deep.domain.audit.md
' Review:    —
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Repo Intelligence Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(repo_intel_group, "Repo Intelligence Group") <<repository>> {
        Component(repo_intel_mgr, "RepoIntelligenceManager", "", "Orchestrates codebase intelligence: indexing, search, ranking, repo mapping")
        Component(ast_index_adapter, "ASTIndexAdapter", "", "Tree-sitter based AST indexing for symbol extraction")
        Component(symbol_graph, "SymbolGraph", "", "Cross-file symbol reference graph: definitions, usages, imports")
        Component(ranking_service, "RankingService", "", "Ranks code search results by relevance, recency, usage frequency")
        Component(embedding_adapter, "EmbeddingAdapter", "", "Wraps embedding model API for vector generation")
        Component(semantic_search_svc, "SemanticSearchService", "", "Vector similarity search over code embeddings")
        Component(repo_map_builder, "RepoMapBuilder", "", "Generates hierarchical repo map summaries for context injection")
    }
}

System_Ext(tree_sitter, "Tree-sitter", "Incremental parser for source code AST")
System_Ext(embedding_model, "Embedding Model", "Code embedding model (local or API)")
System_Ext(vector_store, "Vector Store", "Persistent vector storage for code embeddings")
System_Ext(host_fs, "Host Filesystem", "Source code files to index")

Rel(repo_intel_mgr, ast_index_adapter, "indexes source files")
Rel(repo_intel_mgr, symbol_graph, "builds reference graph")
Rel(repo_intel_mgr, ranking_service, "ranks search results")
Rel(repo_intel_mgr, embedding_adapter, "generates embeddings")
Rel(repo_intel_mgr, semantic_search_svc, "performs vector search")
Rel(repo_intel_mgr, repo_map_builder, "builds repo map")
Rel(ast_index_adapter, tree_sitter, "parses AST")
Rel(ast_index_adapter, symbol_graph, "feeds symbol data")
Rel(ast_index_adapter, host_fs, "reads source files")
Rel(embedding_adapter, embedding_model, "API call for vectors")
Rel(embedding_adapter, vector_store, "stores vectors")
Rel(semantic_search_svc, vector_store, "query vectors")
Rel(ranking_service, symbol_graph, "reads reference data")
Rel(ranking_service, semantic_search_svc, "combines with vector scores")
Rel(repo_map_builder, symbol_graph, "reads structure")
Rel(repo_map_builder, host_fs, "traverses directories")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_tools.puml ---

@startuml c4_nasim_component_tools
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!theme plain
!include common/c4_styles.puml


' ============================================================
' Title:     NASIM Application — Tools Component Diagram
' Boundary:  NASIM Application (container) — Tool Group
' Purpose:   Tool layer: base ABC, registry, file/search/shell/web/git/lsp tools
'            CSR Layer: Repository
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/audit/audit.2026.06.20.deep.domain.audit.md
' Review:    —
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Tools Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(tool_group, "Tool Group") <<repository>> {
        Component(tool_abc, "Tool", "ABC", "Base tool: name, description, parameters, safe, execute")
        Component(tool_registry, "ToolRegistry", "", "Instance-based registry, dynamic registration for MCP")
        Component(file_tools, "FileTools", "", "ReadFileTool, WriteFileTool, EditFileTool")
        Component(search_tools, "SearchTools", "", "GrepTool, GlobTool, FindFileTool")
        Component(shell_tool, "ShellTool", "", "Shell command execution via sandbox with timeout")
        Component(dir_tool, "DirTool", "", "List directory contents")
        Component(web_tools, "WebTools", "", "WebFetchTool, WebSearchTool")
        Component(git_tool, "GitTool", "", "Git status, diff, commit operations")
        Component(lsp_tool, "LspTool", "", "LSP operations: hover, definition, references, symbols")
        Component(subagent_tool, "SubagentTool", "", "Spawns child agents via SubagentCoordinator")
        Component(todo_tool, "TodoTool", "", "Task tracking within session")
        Component(memory_tool, "MemoryTool", "", "Persist and retrieve cross-session knowledge")
        Component(plan_tool, "PlanTool", "", "Plan creation and management")
        Component(repo_map_tool, "RepoMapTool", "", "Generates and queries repository map summaries")
        Component(semantic_search_tool, "SemanticSearchTool", "", "Embedding-based semantic code search")
        Component(review_tool, "ReviewTool", "", "LLM-driven code review and quality assessment")
    }
}

Component_Ext(sandbox_executor, "SandboxExecutor", "Sandbox Group")
Component_Ext(repo_intel_mgr, "RepoIntelligenceManager", "Repo Intelligence Group")
Component_Ext(provider, "Provider", "Provider Group")
System_Ext(host_fs, "Host Filesystem")
System_Ext(web, "Web")
System_Ext(mcp_server, "MCP Server")
System_Ext(git_repo, "Git Repository")
System_Ext(lsp_server, "LSP Server")

Rel(tool_registry, tool_abc, "manages instances")
Rel(file_tools, tool_abc, "extends")
Rel(search_tools, tool_abc, "extends")
Rel(shell_tool, tool_abc, "extends")
Rel(dir_tool, tool_abc, "extends")
Rel(web_tools, tool_abc, "extends")
Rel(git_tool, tool_abc, "extends")
Rel(lsp_tool, tool_abc, "extends")
Rel(subagent_tool, tool_abc, "extends")
Rel(todo_tool, tool_abc, "extends")
Rel(memory_tool, tool_abc, "extends")
Rel(plan_tool, tool_abc, "extends")
Rel(repo_map_tool, tool_abc, "extends")
Rel(semantic_search_tool, tool_abc, "extends")
Rel(review_tool, tool_abc, "extends")
Rel(shell_tool, sandbox_executor, "executes via sandbox")
Rel(file_tools, host_fs, "path I/O")
Rel(search_tools, host_fs, "search files")
Rel(dir_tool, host_fs, "directory listing")
Rel(web_tools, web, "HTTP")
Rel(git_tool, git_repo, "git CLI")
Rel(lsp_tool, lsp_server, "LSP protocol")
Rel(repo_map_tool, repo_intel_mgr, "queries repo map")
Rel(semantic_search_tool, repo_intel_mgr, "vector search")
Rel(review_tool, provider, "LLM review call")
Rel(tool_registry, mcp_server, "discovers and registers MCP tools")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_router.puml ---

@startuml c4_nasim_component_router
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!theme plain
!include common/c4_styles.puml

' ============================================================
' Title:     NASIM Application — Router Component Diagram
' Boundary:  NASIM Application (container) — Router Group
' Purpose:   Model selection, fallback chains, capability queries
'            CSR Layer: Service
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/prompt/p1.md — corrective architecture review
' Review:    —
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Router Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(router_group, "Router Group") <<service>> {
        Component(model_router, "ModelRouter", "", "Model selection, fallback chains, task classification, routing strategies")
        Component(fallback_chain, "FallbackChain", "", "Ordered provider failover chain with retry and exponential backoff")
    }
}

Component_Ext(agent_orch, "AgentOrchestrator", "Agent Group")
Component_Ext(provider, "Provider", "Provider Group")

Rel(agent_orch, model_router, "route model selection")
Rel(model_router, provider, "route to provider")
Rel(model_router, fallback_chain, "trigger failover")
Rel(fallback_chain, provider, "failover to next provider")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_plugins.puml ---

@startuml c4_nasim_component_plugins
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!theme plain
!include common/c4_styles.puml

' ============================================================
' Title:     NASIM Application — Plugins Component Diagram
' Boundary:  NASIM Application (container) — Plugins Group
' Purpose:   Plugin discovery, loading, dynamic tool/hook registration
'            CSR Layer: Infrastructure
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/prompt/p1.md — corrective architecture review
' Review:    docs/audit/audit.2026.06.20.comprehensive.reference.audit.md
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Plugins Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(plugins_group, "Plugins Group") <<infrastructure>> {
        Component(plugin_loader, "PluginLoader", "nasim/plugins/loader.py", "Discovers and loads plugins from ~/.nasim/plugins/")
    }
}

System_Ext(plugin_dir, "Plugin Directory", "~/.nasim/plugins/")
Component_Ext(tool_registry, "ToolRegistry", "Tool Group")
Component_Ext(hook_manager, "HookManager", "Hooks Group")

Rel(plugin_loader, plugin_dir, "discovers plugin manifests")
Rel(plugin_loader, tool_registry, "registers plugin tools")
Rel(plugin_loader, hook_manager, "registers plugin hooks")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_evaluation.puml ---

@startuml c4_nasim_component_evaluation
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!theme plain
!include common/c4_styles.puml


' ============================================================
' Title:     NASIM Application — Evaluation Component Diagram
' Boundary:  NASIM Application (container) — Evaluation Group
' Purpose:   Task evaluation: success checks, LLM review, testing, retry coordination
'            CSR Layer: Service
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/audit/audit.2026.06.20.deep.domain.audit.md
' Review:    —
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Evaluation Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(evaluation_group, "Evaluation Group") <<service>> {
        Component(eval_engine, "EvaluationEngine", "", "Orchestrates task evaluation: runs checks, scores, retries")
        Component(task_evaluator, "TaskEvaluator", "", "Evaluates task completion against success criteria")
        Component(success_check_runner, "SuccessCheckRunner", "", "Runs user-defined success checks and assertions")
        Component(llm_reviewer, "LLMReviewer", "", "LLM-based code review and quality assessment")
        Component(test_runner, "TestRunner", "", "Runs project test suites to validate changes")
        Component(retry_coordinator, "RetryCoordinator", "", "Coordinates retry attempts with backoff and escalation")
        Component(repetition_detector, "RepetitionDetector", "", "Detects repeated failures or loops, triggers escalation")
        Component(turn_budget_injector, "TurnBudgetInjector", "", "Injects turn budget limits and progress signals into context")
        Component(quality_signal, "QualitySignal", "", "Records quality metrics: task success, retry counts, token usage, latency")
    }
}

Component_Ext(provider, "Provider", "Provider Group")
Component_Ext(tool_registry, "ToolRegistry", "Tool Group")

Rel(eval_engine, task_evaluator, "evaluate completion")
Rel(eval_engine, success_check_runner, "run success checks")
Rel(eval_engine, llm_reviewer, "LLM review call")
Rel(eval_engine, test_runner, "run test suites")
Rel(eval_engine, retry_coordinator, "coordinate retry")
Rel(eval_engine, repetition_detector, "detect loops")
Rel(eval_engine, turn_budget_injector, "inject budget limits")
Rel(eval_engine, quality_signal, "records quality metrics")
Rel(llm_reviewer, provider, "LLM review call")
Rel(test_runner, tool_registry, "executes test commands")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_observability.puml ---

@startuml c4_nasim_component_observability
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!theme plain
!include common/c4_styles.puml

' ============================================================
' Title:     NASIM Application — Observability Component Diagram
' Boundary:  NASIM Application (container) — Observability Group
' Purpose:   Application-side logging and metrics emission (emit only)
'            CSR Layer: Infrastructure
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/audit/audit.2026.06.20.logging.observability.car.md
' Review:    —
' ============================================================

LAYOUT_LEFT_RIGHT()
LAYOUT_WITH_LEGEND()

title NASIM Application — Observability Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(observability_group, "Observability Group") <<infrastructure>> {
        Component(structured_logger, "StructuredLogger", "structlog + JSON formatter", "Writes structured JSON records to stdout only. Includes trace_id, span_id, session_id, turn_number. Never ships to Loki or log agent.")
        Component(metrics_collector, "MetricsCollector", "prometheus-client", "Owns application metrics: token usage, latency histograms, tool call counters, error rates. Exposes /metrics for pull scrape only.")
        Component(trace_correlator, "TraceCorrelator", "contextvars", "Generates root trace/span per top-level task. Binds trace_id, span_id, parent_span_id to context.")
        Component(context_propagator, "ContextPropagator", "contextvars + threading", "Propagates trace context across Provider calls, tool dispatch, hook execution, subagent spawn, MCP calls.")
        Component(log_redactor, "LogRedactor", "regex patterns", "Strips secrets before any emission to structured logs or wire events. Always on. Configurable redaction rules.")
        Component(dual_output_adapter, "DualOutputAdapter", "tty detection", "CLI adapter: writes JSON to stdout + delegates to Renderer when isatty. HTTP/MCP: direct structured emission.")
        Component(instrumentation_mw, "InstrumentationMiddleware", "ASGI middleware", "Outermost HTTP request handler. Extracts/generates request_id; binds to contextvars.")
        Component(otel_exporter, "OTelExporter", "opentelemetry-sdk (optional)", "Behind otel feature flag. Bridges TraceCorrelator spans to OTel SDK. Zero dep when disabled.")
    }
}

Component_Ext(agent_orch, "AgentOrchestrator", "Agent Group")
Component_Ext(provider, "Provider", "Provider Group")
Component_Ext(tool_registry, "ToolRegistry", "Tool Group")
Component_Ext(wire_log, "WireLog", "Wire Log Group")
Component_Ext(config_loader, "ConfigLoader", "Config Group")
Component_Ext(renderer, "Renderer", "CLI Group")

Rel(agent_orch, structured_logger, "logs agent lifecycle events")
Rel(agent_orch, metrics_collector, "records token usage, latency, tool call counts")
Rel(agent_orch, context_propagator, "obtains trace context for events")
Rel(agent_orch, wire_log, "appends domain events")

Rel(provider, structured_logger, "logs LLM request/response metadata")
Rel(provider, metrics_collector, "records LLM latency and token consumption")
Rel(provider, context_propagator, "propagates trace across LLM calls")

Rel(tool_registry, structured_logger, "logs tool execution events")
Rel(tool_registry, metrics_collector, "records tool call counts and duration")
Rel(tool_registry, context_propagator, "propagates trace across tool dispatch")

Rel(trace_correlator, context_propagator, "provides root trace/span context")
Rel(context_propagator, structured_logger, "injects trace_id, span_id into log records")
Rel(context_propagator, wire_log, "injects correlation ids into wire events")
Rel(log_redactor, structured_logger, "sanitizes log records before emission")
Rel(log_redactor, wire_log, "sanitizes wire events before append")
Rel(dual_output_adapter, structured_logger, "delegates structured JSON emission")
System_Ext(stdout, "stdout", "Standard output stream")
Rel(dual_output_adapter, stdout, "writes JSON to stdout", "stdout only — emit path ends here")
Rel(dual_output_adapter, renderer, "delegates human-readable output when isatty")
Rel(instrumentation_mw, context_propagator, "generates and binds request_id")
Rel(instrumentation_mw, metrics_collector, "records HTTP request duration and status")
Rel(instrumentation_mw, structured_logger, "logs HTTP request lifecycle")
Rel(config_loader, structured_logger, "configures log level and format")
Rel(config_loader, metrics_collector, "enables/disables metrics emission")
Rel(config_loader, log_redactor, "configures redaction rules")
Rel(config_loader, otel_exporter, "enables/disables OTel feature")

System_Ext(otel_collector, "OTel Collector", "OTLP/gRPC endpoint for optional trace and metric export")
Rel(structured_logger, stdout, "writes structured JSON logs to stdout", "stdout — emit path ends here")
System_Ext(prometheus, "Prometheus", "Metrics collection and storage")
Rel(metrics_collector, prometheus, "exposes /metrics for pull scrape", "HTTP GET — pull model only")
Rel(otel_exporter, otel_collector, "exports traces/metrics", "OTLP/gRPC — only when otel enabled")
Rel(trace_correlator, otel_exporter, "bridges spans to OTel SDK (optional)")
Rel(metrics_collector, otel_exporter, "bridges metrics to OTel SDK (optional)")
Rel(wire_log, structured_logger, "optional: project domain events as structured (configurable)")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_mcp.puml ---

@startuml c4_nasim_component_mcp
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!theme plain
!include common/c4_styles.puml


' ============================================================
' Title:     NASIM Application — MCP Component Diagram
' Boundary:  NASIM Application (container) — MCP Group
' Purpose:   Model Context Protocol as first-class subsystem
'            CSR Layer: Infrastructure
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/prompt/p1.md — corrective architecture review
' Review:    docs/audit/audit.2026.06.20.comprehensive.reference.audit.md
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — MCP Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(mcp_group, "MCP Group") <<infrastructure>> {
        Component(mcp_client_rt, "MCPClientRuntime", "nasim/mcp/client.py", "Connects to external MCP servers via stdio/SSE transport")
        Component(mcp_server_rt, "MCPServerRuntime", "nasim/mcp/server.py", "Exposes NASIM Application tools to external MCP clients via stdio/SSE")
        Component(mcp_tool_adapter, "MCPToolAdapter", "nasim/mcp/adapter.py", "Wraps MCP server tools into NASIM Application Tool ABC format")
        Component(mcp_discovery, "MCPDiscovery", "nasim/mcp/discovery.py", "Discovers and registers MCP server tools at startup from config")
    }
}

System_Ext(mcp_server, "MCP Server", "External extension tools")
System_Ext(mcp_client, "MCP Client", "External tool consumers")
Component_Ext(tool_registry, "ToolRegistry", "Tool Group")

Rel(mcp_client_rt, mcp_server, "connects via stdio/SSE")
Rel(mcp_client_rt, mcp_discovery, "discovers tools")
Rel(mcp_discovery, mcp_tool_adapter, "registers discovered tools")
Rel(mcp_tool_adapter, tool_registry, "registers as NASIM Application tools")
Rel(mcp_server_rt, tool_registry, "exposes nasim tools to external clients")
Rel(mcp_server_rt, mcp_client, "serves MCP protocol via stdio/SSE")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_edit_strategy.puml ---

@startuml c4_nasim_component_edit_strategy
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!theme plain
!include common/c4_styles.puml


' ============================================================
' Title:     NASIM Application — Edit Strategy Component Diagram
' Boundary:  NASIM Application (container) — Edit Strategy Group
' Purpose:   Pluggable edit strategies: search-replace, whole-file, diff, sandbox, architect
'            CSR Layer: Service
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/audit/audit.2026.06.20.deep.domain.audit.md
' Review:    —
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Edit Strategy Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(edit_strategy_group, "Edit Strategy Group") <<service>> {
        Component(edit_strategy_mgr, "EditStrategyManager", "", "Manages and selects edit strategy based on edit type and context")
        Component(edit_strategy, "EditStrategy", "ABC", "Base strategy interface: plan_edit, apply_edit, validate_edit")
        Component(search_replace_coder, "SearchReplaceCoder", "", "Search-and-replace based edit with fuzzy matching")
        Component(whole_file_coder, "WholeFileCoder", "", "Whole file rewrite strategy")
        Component(unified_diff_coder, "UnifiedDiffCoder", "", "Unified diff format edit application")
        Component(diff_sandbox_coder, "DiffSandboxCoder", "", "Sandboxed diff-based edit with validation")
        Component(architect_coder, "ArchitectCoder", "", "Multi-file architectural edit strategy with planning")
        Component(fenced_block_coder, "FencedBlockCoder", "", "Fenced code block format edit")
        Component(function_level_coder, "FunctionLevelCoder", "", "AST-targeted function replacement")
        Component(inline_patch_coder, "InlinePatchCoder", "", "apply-patch format edit")
        Component(strategy_selector, "StrategySelector", "", "Selects optimal strategy: edit size, risk, file type heuristics. Applies heuristics: edit_size, risk_level, file_type, complexity")
    }
}

Component_Ext(diff_sandbox_mgr, "DiffSandboxManager", "Sandbox Group")
System_Ext(host_fs, "Host Filesystem", "Target files for editing")

Rel(edit_strategy_mgr, strategy_selector, "selects strategy")
Rel(edit_strategy_mgr, edit_strategy, "delegates to")
Rel(search_replace_coder, edit_strategy, "extends")
Rel(whole_file_coder, edit_strategy, "extends")
Rel(unified_diff_coder, edit_strategy, "extends")
Rel(diff_sandbox_coder, edit_strategy, "extends")
Rel(architect_coder, edit_strategy, "extends")
Rel(fenced_block_coder, edit_strategy, "extends")
Rel(function_level_coder, edit_strategy, "extends")
Rel(inline_patch_coder, edit_strategy, "extends")
Rel(strategy_selector, edit_strategy_mgr, "returns strategy choice")
Rel(search_replace_coder, host_fs, "applies edits")
Rel(whole_file_coder, host_fs, "rewrites files")
Rel(unified_diff_coder, host_fs, "applies diffs")
Rel(diff_sandbox_coder, diff_sandbox_mgr, "stages diffs for sandbox-validated apply")
Rel(architect_coder, host_fs, "multi-file edits")
Rel(fenced_block_coder, host_fs, "applies edits")
Rel(function_level_coder, host_fs, "AST-aware edits")
Rel(inline_patch_coder, host_fs, "applies patch")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_hooks.puml ---

@startuml c4_nasim_component_hooks
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!theme plain
!include common/c4_styles.puml


' ============================================================
' Title:     NASIM Application — Hooks Component Diagram
' Boundary:  NASIM Application (container) — Hooks Group
' Purpose:   Pre/post hooks for tool use and LLM calls
'            CSR Layer: Infrastructure
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/prompt/p1.md — corrective architecture review
' Review:    docs/audit/audit.2026.06.20.comprehensive.reference.audit.md
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Hooks Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(hooks_group, "Hooks Group") <<infrastructure>> {
        Component(hook_manager, "HookManager", "nasim/hooks/manager.py", "Registers, discovers, and executes hooks in priority order")
    }
}

Component_Ext(agent_orch, "AgentOrchestrator", "Agent Group")
Component_Ext(tool_registry, "ToolRegistry", "Tool Group")
Component_Ext(plugin_loader, "PluginLoader", "Plugins Group")

Rel(agent_orch, hook_manager, "triggers PreToolUse, PostToolUse, PreLLMCall, PostLLMCall")
Rel(plugin_loader, hook_manager, "registers plugin hooks")
Rel(hook_manager, tool_registry, "queries tool metadata for hook dispatch")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_provider.puml ---

@startuml c4_nasim_component_provider
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!theme plain
!include common/c4_styles.puml

' ============================================================
' Title:     NASIM Application — Provider Component Diagram
' Boundary:  NASIM Application (container) — Provider Group
' Purpose:   Provider Group: universal LLM abstraction via litellm.
'            Provider is a Protocol — all LLM calls route through
'            LiteLLM Proxy to 100+ backends without per-provider code.
'            CSR Layer: Service
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/entities.md
' Review:    —
' ============================================================

LAYOUT_LEFT_RIGHT()
LAYOUT_WITH_LEGEND()

title NASIM Application — Provider Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(provider_group, "Provider Group") <<service>> {
        Component(provider, "Provider", "Protocol", "Unified interface: chat(), chat_stream(), model_name, capabilities. All providers route through litellm.")
        Component(litellm_proxy, "LiteLLM Proxy", "litellm library", "Universal LLM proxy: 100+ providers via model string prefix (anthropic/, openai/, gemini/, deepseek/, ollama/, etc.)")
        Component(provider_caps, "ProviderCapabilities", "", "Per-model capability metadata: supports_tools, supports_vision, supports_streaming, max_context, supports_prompt_caching.")
    }
}

Component_Ext(agent_orch, "AgentOrchestrator", "Agent Group")
Component_Ext(model_router, "ModelRouter", "Router Group")
System_Ext(all_llm_backends, "LLM Backends", "Unified via litellm: OpenAI, Anthropic, Google, DeepSeek, Ollama, vLLM, and 100+ providers via model string prefix")

Rel(agent_orch, provider, "chat, chat_stream")
Rel(model_router, provider_caps, "queries capabilities")
Rel(provider, litellm_proxy, "delegates all LLM calls")
Rel(provider, provider_caps, "exposes capabilities")
Rel(litellm_proxy, all_llm_backends, "routes via model string prefix", "HTTP/JSON")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_memory.puml ---

@startuml c4_nasim_component_memory
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!theme plain
!include common/c4_styles.puml


' ============================================================
' Title:     NASIM Application — Memory Component Diagram
' Boundary:  NASIM Application (container) — Memory Group
' Purpose:   Cross-session knowledge persistence, retrieval, and RAG adapters
'            CSR Layer: Repository
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/audit/audit.2026.06.20.deep.domain.audit.md
' Review:    —
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Memory Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(memory_group, "Memory Group") <<repository>> {
        Component(memory_store, "MemoryStore", "", "Persists knowledge entries with scope, key, content, timestamps")
        Component(memory_index, "MemoryIndex", "", "FTS5 index for semantic search across stored knowledge")
        Component(memory_scope, "MemoryScope", "", "Scope isolation: global, project, session-level knowledge")
        Component(episodic_mem_adapter, "EpisodicMemoryAdapter", "", "Stores and retrieves episodic memory: past actions, outcomes, lessons")
        Component(semantic_mem_adapter, "SemanticMemoryAdapter", "", "Stores and retrieves semantic memory: facts, rules, preferences")
        Component(working_mem_adapter, "WorkingMemoryAdapter", "", "Short-term working memory for current session context")
        Component(memory_retriever, "MemoryRetriever", "", "RAG retrieval: query memory stores, rank by relevance")
        Component(memory_indexer, "MemoryIndexer", "", "Indexes new memory entries into FTS5 and embedding stores")
    }
}

System_Ext(memory_backend, "Memory Backend", "Persistent storage for knowledge entries")

Rel(memory_store, memory_index, "indexes new entries")
Rel(memory_store, memory_backend, "read/write persistence")
Rel(memory_scope, memory_store, "filters by scope")
Rel(episodic_mem_adapter, memory_store, "persist episodic entries")
Rel(semantic_mem_adapter, memory_store, "persist semantic entries")
Rel(working_mem_adapter, memory_store, "persist working entries")
Rel(memory_retriever, memory_index, "query index")
Rel(memory_retriever, memory_store, "fetch entries")
Rel(memory_indexer, memory_index, "index entries")
Rel(memory_indexer, memory_store, "read entries for indexing")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_container.puml ---

@startuml c4_nasim_container
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Container.puml
!theme plain

' ============================================================
' Title:     NASIM Application — Container Diagram
' Boundary:  NASIM Application
' Purpose:   Show the 3 runtime entry points within NASIM Application:
'            1. CLI Process (terminal interface)
'            2. HTTP API Server (FastAPI + SSE)
'            3. Core Engine (agent loop, tools, business logic)
'
'            WHY CLI AND HTTP ARE SEPARATE CONTAINERS:
'            NASIM Application runs as a single Python process. However,
'            CLI Process and HTTP API Server are modeled as separate
'            Containers because they are distinct RUNTIME ENTRY POINTS —
'            each with its own initialization, argument parsing, request
'            handling, and lifecycle. In C4, a Container represents a
'            separately runnable/deployable unit OR a distinct runtime
'            responsibility. Here, the "responsibility" interpretation
'            applies: they share a process boundary but serve different
'            interaction models (terminal REPL vs HTTP+SSE).
'
'            WHY WEBAPP/DESKTOP/MOBILE ARE Container_Ext:
'            WebApp, DesktopApp, and MobileApp are genuinely separate
'            deployable units (browser SPA, Electron app, mobile app)
'            with their own processes, codebases, and deployment pipelines.
'            They are NOT part of NASIM Application — they are clients
'            that communicate with it over HTTP. In C4, a Container_Ext
'            represents a container outside the system boundary that
'            interacts with it. This is the correct model for external
'            client applications.
'
'            CONTAINER vs COMPONENT distinction:
'            - Container: represents a separately runnable unit OR a
'              distinct runtime entry point (CLI Process, HTTP API Server)
'              OR the main application shell (Core Engine).
'            - Component: represents an internal module/class within a
'              Container (e.g., AgentOrchestrator, SafetyCoordinator).
'            The HTTP API Server is NOT a Component because it is a
'            distinct entry point with its own lifecycle, not an internal
'            module of the Core Engine.
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/entities.md
' Review:    —
' ============================================================

LAYOUT_LEFT_RIGHT()
LAYOUT_WITH_LEGEND()

title NASIM Application — Container Diagram

Person(user, "User", "Developer using NASIM Application as an AI coding assistant")

System_Boundary(nasim_application, "NASIM Application") {
    Container(cli_process, "CLI Process", "Python 3.10+", "Terminal interface: REPL, arg parsing, rendering. Delegates to Core Engine.")
    Container(api_server, "HTTP API Server", "FastAPI + SSE", "REST API with Server-Sent Events streaming. Entry gate for all interface containers.")
    Container(core_engine, "Core Engine", "Python 3.10+", "Unified agent engine: agentic loop, tool orchestration, safety, context management, business logic")
}

Container_Ext(webapp, "WebApp", "JavaScript SPA", "Browser-based interface connecting to API Server via HTTP")
Container_Ext(desktop_app, "DesktopApp", "Electron / Tauri", "Desktop application connecting to API Server via HTTP")
Container_Ext(mobile_app, "MobileApp", "React Native / Flutter", "Mobile application connecting to API Server via HTTP")

System_Ext(mcp_client, "MCP Client", "External tool or IDE that invokes NASIM Application via MCP protocol")
System_Ext(llm_backend, "LLM Backend", "Multi-provider inference: Ollama, OpenAI, Anthropic, and 100+ via litellm")
System_Ext(host_fs, "Host Filesystem", "Project code files and directories")
System_Ext(host_shell, "Host Shell", "Shell execution via OS subprocess")
System_Ext(web_ext, "Web", "Documentation pages and search engines")
System_Ext(mcp_server, "MCP Server", "Extension tools provided to NASIM Application via Model Context Protocol")
System_Ext(git_repo, "Git Repository", "Version-controlled project files")
System_Ext(sandbox_rt, "Sandbox Runtime", "OS-level process isolation (landlock, seccomp, bubblewrap)")
System_Ext(lsp_server, "LSP Server", "Language server providing code intelligence")
System_Ext(plugin_dir, "Plugin Directory", "Community plugins at ~/.nasim/plugins/")
System_Ext(tree_sitter, "Tree-sitter", "Incremental AST parser for source code indexing")
System_Ext(embedding_model, "Embedding Model", "Code embedding generation (local or API-based)")
System_Ext(vector_store, "Vector Store", "Persistent vector storage for semantic code search")
System_Ext(otel_collector, "OTel Collector", "OTLP/gRPC endpoint for optional trace and metric export")
System_Ext(obs_platform, "Observability Platform", "Log agent, Prometheus, Grafana. NASIM Application emits only — platform owns collection, storage, and visualization.")

Rel(user, cli_process, "runs coding tasks (CLI mode)", "terminal REPL")
Rel(user, webapp, "interacts via browser", "HTTPS")
Rel(user, desktop_app, "interacts via desktop app", "local")
Rel(user, mobile_app, "interacts via mobile app", "HTTPS")

Rel(cli_process, core_engine, "delegates tasks", "Internal Call")
Rel(webapp, api_server, "HTTP requests + SSE streaming", "HTTP/JSON + SSE")
Rel(desktop_app, api_server, "HTTP requests + SSE streaming", "HTTP/JSON + SSE")
Rel(mobile_app, api_server, "HTTP requests + SSE streaming", "HTTP/JSON + SSE")
Rel(mcp_client, api_server, "invokes tools via MCP protocol", "stdio / SSE")
Rel(api_server, core_engine, "delegates to core agent engine", "Internal Call")

Rel(core_engine, llm_backend, "sends chat requests, receives completions", "HTTP/JSON streaming")
Rel(core_engine, host_fs, "reads, writes, and searches project files")
Rel(core_engine, host_shell, "executes shell commands via sandbox")
Rel(core_engine, web_ext, "fetches documentation and search results", "HTTP")
Rel(core_engine, mcp_server, "discovers and invokes extension tools", "stdio / SSE")
Rel(core_engine, git_repo, "reads status and creates commits", "git CLI")
Rel(core_engine, sandbox_rt, "executes commands in isolated environment")
Rel(core_engine, lsp_server, "queries code intelligence", "LSP protocol")
Rel(core_engine, plugin_dir, "discovers and loads plugins at startup")
Rel(core_engine, tree_sitter, "parses source code AST for indexing")
Rel(core_engine, embedding_model, "generates code embeddings")
Rel(core_engine, vector_store, "stores and retrieves code embeddings")
Rel(core_engine, otel_collector, "exports traces and metrics (optional)", "OTLP/gRPC")
Rel(core_engine, obs_platform, "emits structured logs to stdout; exposes /metrics for pull-scrape", "stdout + HTTP")

' ============================================================
' Note for Readers:
' ============================================================
' NASIM Application runs as a SINGLE Python process.
' CLI Process and HTTP API Server are NOT separate deployments —
' they are separate RUNTIME ENTRY POINTS within the same process.
'
' In C4, a Container models a unit with a distinct runtime
' responsibility. CLI Process handles terminal interaction (REPL,
' arg parsing, rendering). HTTP API Server handles HTTP requests
' and SSE streaming. Both delegate to the shared Core Engine.
'
' WebApp, DesktopApp, and MobileApp are modeled as Container_Ext
' because they are truly separate deployable units (browser SPA,
' Electron app, mobile app) with their own processes. They are
' clients of NASIM Application, not part of it.
' ============================================================

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_agent.puml ---

@startuml c4_nasim_component_agent
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!include common/c4_styles.puml
!theme plain

' ============================================================
' Title:     NASIM Application — Agent Component Diagram
' Boundary:  NASIM Application (container) — Agent Group
' Purpose:   Agent Group: core agentic loop (Service layer).
'            AgentOrchestrator is the single orchestrating
'            component for the nasim Service layer.
'            CSR Layer: Service
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/entities.md
' Review:    —
' ============================================================

LAYOUT_WITH_LEGEND()

title NASIM Application — Agent Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(agent_group, "Agent Group") <<service>> {
        Component(agent_orch, "AgentOrchestrator", "", "Core agentic loop: Provider call, tool dispatch, repeat. Yields structured events")
        Component(conv_history, "ConversationHistory", "", "Owns messages list and token count. Triggers compaction when budget exceeded")
        Component(compactor, "ContextCompactor", "", "Summarizes old exchanges via secondary LLM call when budget exceeded")
        Component(plan_session, "PlanSession", "", "Holds queued tool calls in plan mode. /approve drains the queue")
        Component(subagent_coord, "SubagentCoordinator", "", "Parent-child orchestration, nesting limit 5, result aggregation")
        Component(error_boundary, "ErrorBoundary", "", "Structured error handling with recovery actions and error hierarchy")
        Component(persona_mgr, "PersonaManager", "", "Runtime persona switching for specialized agent roles")
    }
}

Component_Ext(provider, "Provider", "Provider Group")
Component_Ext(tool_registry, "ToolRegistry", "Tool Group")
Component_Ext(session_store, "SessionStore", "Session Group")
Component_Ext(memory_store, "MemoryStore", "Memory Group")
Component_Ext(structured_logger, "StructuredLogger", "Observability Group")
Component_Ext(safety_coord, "SafetyCoordinator", "Safety Group")

Rel(agent_orch, provider, "chat, chat_stream")
Rel(agent_orch, tool_registry, "execute tool calls")
Rel(agent_orch, conv_history, "reads/writes messages")
Rel(agent_orch, safety_coord, "safety validation before tool exec")
Rel(agent_orch, subagent_coord, "spawn, collect child agents")
Rel(agent_orch, error_boundary, "handle structured errors")
Rel(agent_orch, persona_mgr, "load, switch persona")
Rel(agent_orch, memory_store, "persist, retrieve knowledge")
Rel(agent_orch, session_store, "save, load session")
Rel(agent_orch, structured_logger, "emit structured logs, metrics")
Rel(agent_orch, plan_session, "queue, approve plan")
Rel(conv_history, compactor, "triggers compaction")
Rel(compactor, provider, "secondary LLM call for summarization")
Rel(subagent_coord, agent_orch, "child agent inherits core")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_overview.puml ---

@startuml c4_nasim_component_overview
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!theme plain
!include common/c4_styles.puml

' ============================================================
' Title:     NASIM Application — Component Overview
' Boundary:  NASIM Application (container)
' Purpose:   High-level view of all 21 component groups inside
'            the Core Engine. Shows key cross-group
'            relationships and CSR layering. Internal details
'            are in per-group diagrams.
'            CSR Pattern:
'            - Controller: CLI Group, API Group
'            - Service: Agent Group, Router Group, Safety Group,
'                       Context Graph Group, Edit Strategy Group,
'                       Evaluation Group
'            - Repository: Session Group, Tool Group, Memory Group,
'                          Config Group, Git Group, Repo Intelligence Group
'            - Infrastructure: MCP Group, Sandbox Group, Observability Group,
'                              Wire Log Group, Hooks Group, Plugins Group
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/entities.md
' Review:    —
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Component Overview

Boundary(nasim_application, "NASIM Application") {

    Boundary(cli_group, "CLI Group") <<controller>> {
        Component(repl_session, "REPLSession", "", "Terminal REPL entry point")
    }

    Boundary(api_group, "API Group") <<controller>> {
        Component(server_router, "ServerRouter", "FastAPI", "HTTP entry gate")
    }

    Boundary(agent_group, "Agent Group") <<service>> {
        Component(agent_orch, "AgentOrchestrator", "", "Core agentic loop")
    }

    Boundary(router_group, "Router Group") <<service>> {
        Component(model_router, "ModelRouter", "", "Model selection, fallback")
    }

    Boundary(safety_group, "Safety Group") <<service>> {
        Component(safety_coord, "SafetyCoordinator", "", "Permission, injection, egress")
    }

    Boundary(context_graph_group, "Context Graph Group") <<service>> {
        Component(context_graph, "ContextGraph", "", "Context pipeline orchestrator")
    }

    Boundary(edit_strategy_group, "Edit Strategy Group") <<service>> {
        Component(edit_strategy_mgr, "EditStrategyManager", "", "Polymorphic edit strategies")
    }

    Boundary(evaluation_group, "Evaluation Group") <<service>> {
        Component(eval_engine, "EvaluationEngine", "", "Task evaluation and retry")
    }

    Boundary(session_group, "Session Group") <<repository>> {
        Component(session_store, "SessionStore", "", "Message persistence")
    }

    Boundary(tool_group, "Tool Group") <<repository>> {
        Component(tool_registry, "ToolRegistry", "", "Tool instance management")
    }

    Boundary(memory_group, "Memory Group") <<repository>> {
        Component(memory_store, "MemoryStore", "", "Cross-session knowledge")
    }

    Boundary(config_group, "Config Group") <<repository>> {
        Component(config_loader, "ConfigLoader", "", "Typed configuration")
    }

    Boundary(git_group, "Git Group") <<repository>> {
        Component(git_integration, "GitIntegration", "", "Git operations")
    }

    Boundary(repo_intelligence_group, "Repo Intelligence Group") <<repository>> {
        Component(repo_intel_mgr, "RepoIntelligenceManager", "", "Codebase intelligence")
    }

    Boundary(mcp_group, "MCP Group") <<infrastructure>> {
        Component(mcp_client_rt, "MCPClientRuntime", "", "External MCP connections")
    }

    Boundary(sandbox_group, "Sandbox Group") <<infrastructure>> {
        Component(sandbox_executor, "SandboxExecutor", "", "OS-level isolation")
    }

    Boundary(observability_group, "Observability Group") <<infrastructure>> {
        Component(structured_logger, "StructuredLogger", "", "Structured logging")
    }

    Boundary(wire_log_group, "Wire Log Group") <<infrastructure>> {
        Component(wire_log, "WireLog", "", "Event recording")
    }

    Boundary(hooks_group, "Hooks Group") <<infrastructure>> {
        Component(hook_manager, "HookManager", "", "Hook lifecycle")
    }

    Boundary(plugins_group, "Plugins Group") <<infrastructure>> {
        Component(plugin_loader, "PluginLoader", "", "Plugin discovery")
    }

    Boundary(provider_group, "Provider Group") <<service>> {
        Component(provider, "Provider", "Protocol", "LLM abstraction")
    }
}

Person(user, "User", "Developer using NASIM Application")
Container_Ext(webapp, "WebApp", "JavaScript SPA", "Browser interface")
Container_Ext(desktop_app, "DesktopApp", "Electron / Tauri", "Desktop interface")
Container_Ext(mobile_app, "MobileApp", "React Native / Flutter", "Mobile interface")

System_Ext(llm_backend, "LLM Backend")
System_Ext(host_fs, "Host Filesystem")
System_Ext(host_shell, "Host Shell")
System_Ext(web_ext, "Web")
System_Ext(mcp_server, "MCP Server")
System_Ext(mcp_client, "MCP Client")
System_Ext(git_repo, "Git Repository")
System_Ext(sandbox_rt, "Sandbox Runtime")
System_Ext(lsp_server, "LSP Server")
System_Ext(plugin_dir, "Plugin Directory")
System_Ext(tree_sitter, "Tree-sitter")
System_Ext(embedding_model, "Embedding Model")
System_Ext(vector_store, "Vector Store")
System_Ext(otel_collector, "OTel Collector")
System_Ext(obs_platform, "Observability Platform")

' Controller → Service
Rel(user, repl_session, "types commands", "terminal REPL")
Rel(webapp, server_router, "HTTP + SSE", "HTTP/JSON + SSE")
Rel(desktop_app, server_router, "HTTP + SSE", "HTTP/JSON + SSE")
Rel(mobile_app, server_router, "HTTP + SSE", "HTTP/JSON + SSE")
Rel(mcp_client, mcp_client_rt, "invokes tools via MCP", "stdio / SSE")
Rel(repl_session, agent_orch, "delegates tasks")
Rel(server_router, agent_orch, "delegates tasks")

' Service → Service
Rel(agent_orch, safety_coord, "safety validation")
Rel(agent_orch, model_router, "route model selection")
Rel(agent_orch, context_graph, "process context")
Rel(agent_orch, provider, "chat, stream")

' Service → Repository
Rel(agent_orch, tool_registry, "execute tools")
Rel(agent_orch, session_store, "save, load")
Rel(agent_orch, memory_store, "persist, retrieve")
Rel(agent_orch, hook_manager, "triggers hooks")
Rel(model_router, provider, "delegates LLM calls")

' Repository → External
Rel(tool_registry, sandbox_executor, "sandboxed execution")
Rel(git_integration, git_repo, "git CLI")
Rel(sandbox_executor, host_shell, "executes commands")
Rel(sandbox_executor, host_fs, "file access")
Rel(provider, llm_backend, "LLM API")
Rel(repo_intel_mgr, tree_sitter, "AST parsing")
Rel(repo_intel_mgr, embedding_model, "vector generation")
Rel(repo_intel_mgr, vector_store, "similarity search")

' Infrastructure
Rel(structured_logger, obs_platform, "emits logs")
Rel(mcp_server, mcp_client_rt, "provides extension tools via MCP", "stdio / SSE")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_context_graph.puml ---

@startuml c4_nasim_component_context_graph
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!include common/c4_styles.puml
!theme plain

' ============================================================
' Title:     NASIM Application — Context Graph Component Diagram
' Boundary:  NASIM Application (container) — Context Graph Group
' Purpose:   Context pipeline: graph construction, truncation, distillation, injection, compaction
'            CSR Layer: Service
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/audit/audit.2026.06.20.deep.domain.audit.md
' Review:    —
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Context Graph Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(context_graph_group, "Context Graph Group") <<service>> {
        Component(context_graph, "ContextGraph", "", "Directed graph of context nodes with edges: dependencies, references, flow")
        Component(pipeline_orchestrator, "PipelineOrchestrator", "", "Orchestrates context processing pipeline stages in order")
        Component(context_prioritizer, "ContextPrioritizer", "", "Assigns importance score to each ContextNode: recency_weight * (1 - age/max_age) + frequency_weight * edit_count + type_weight. Enables priority-based truncation (lowest importance dropped first).")
        Component(truncation_processor, "TruncationProcessor", "", "Truncates context nodes to fit within token budget. Drops nodes in ascending importance order (via ContextPrioritizer).")
        Component(distillation_processor, "DistillationProcessor", "", "Summarizes and distills long context nodes")
        Component(injection_processor, "InjectionProcessor", "", "Injects retrieved context (memory, repo map) into context graph")
        Component(compaction_processor, "CompactionProcessor", "", "Merges redundant or overlapping context nodes")
        Component(token_budget_tracker, "TokenBudgetTracker", "", "Tracks cumulative token usage across context pipeline stages")
    }
}

Component_Ext(memory_store, "MemoryStore", "Memory Group")
Component_Ext(repo_intel_mgr, "RepoIntelligenceManager", "Repo Intelligence Group")
System_Ext(llm_backend, "LLM Backend", "Used for distillation and summarization calls")

Rel(pipeline_orchestrator, context_prioritizer, "priority scoring stage")
Rel(pipeline_orchestrator, truncation_processor, "truncation stage")
Rel(pipeline_orchestrator, distillation_processor, "distillation stage")
Rel(pipeline_orchestrator, injection_processor, "injection stage")
Rel(pipeline_orchestrator, compaction_processor, "compaction stage")
Rel(pipeline_orchestrator, token_budget_tracker, "track budget")
Rel(pipeline_orchestrator, context_graph, "operates on graph")
Rel(context_prioritizer, context_graph, "scores nodes by importance")
Rel(distillation_processor, llm_backend, "summarization call")
Rel(injection_processor, memory_store, "injects memory")
Rel(injection_processor, repo_intel_mgr, "injects repo map")
Rel(truncation_processor, context_prioritizer, "queries node priorities")
Rel(truncation_processor, token_budget_tracker, "queries remaining budget")
Rel(compaction_processor, context_graph, "merges nodes")
Rel(token_budget_tracker, context_graph, "reads node costs")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_safety.puml ---

@startuml c4_nasim_component_safety
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!include common/c4_styles.puml
!theme plain

' ============================================================
' Title:     NASIM Application — Safety Component Diagram
' Boundary:  NASIM Application (container) — Safety Group
' Purpose:   Permission gating, injection scanning, egress inspection
'            CSR Layer: Service
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/prompt/p1.md — corrective architecture review
' Review:    —
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Safety Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(safety_group, "Safety Group") <<service>> {
        Component(safety_coord, "SafetyCoordinator", "", "Composes PermissionGate, injection scanner, egress inspector into pipeline")
        Component(permission_gate, "PermissionGate", "", "Evaluates tool safety flags against session safety mode")
        Component(injection_scanner, "InjectionScanner", "", "Detects prompt injection patterns in tool outputs")
        Component(egress_inspector, "EgressInspector", "", "Inspects outbound data for policy violations")
    }
}

Component_Ext(agent_orch, "AgentOrchestrator", "Agent Group")
Component_Ext(tool_registry, "ToolRegistry", "Tool Group")

Rel(agent_orch, safety_coord, "validates before tool dispatch")
Rel(safety_coord, permission_gate, "evaluates permissions")
Rel(safety_coord, injection_scanner, "scans for injection")
Rel(safety_coord, egress_inspector, "inspects outbound data")
Rel(tool_registry, safety_coord, "triggers safety checks")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_session.puml ---

@startuml c4_nasim_component_session
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!theme plain
!include common/c4_styles.puml


' ============================================================
' Title:     NASIM Application — Session Component Diagram
' Boundary:  NASIM Application (container) — Session Group
' Purpose:   Session layer internal components
'            CSR Layer: Repository
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/prompt/p1.md — corrective architecture review
' Review:    docs/audit/audit.2026.06.20.comprehensive.reference.audit.md
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Session Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(session_group, "Session Group") <<repository>> {
        Component(session_store, "SessionStore", "", "Persists/loads message history to ~/.nasim/sessions/ as JSON Lines")
        Component(session_versioning, "SessionVersioning", "", "Snapshots and undo for session state")
        Component(session_search, "SessionSearch", "", "Cross-session search via FTS5 index")
        Component(session_fork, "SessionFork", "", "Branch conversations from any point")
    }
}

System_Ext(session_dir, "Session Directory", "~/.nasim/sessions/<session-id>/")

Rel(session_store, session_dir, "reads/writes JSON Lines files")
Rel(session_versioning, session_store, "creates snapshots, restores state")
Rel(session_search, session_store, "indexes and queries session content")
Rel(session_fork, session_store, "copies session state for branching")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_wire_log.puml ---

@startuml c4_nasim_component_wire_log
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!theme plain
!include common/c4_styles.puml


' ============================================================
' Title:     NASIM Application — Wire Log Component Diagram
' Boundary:  NASIM Application (container) — Wire Log Group
' Purpose:   Agent-provider-tool interaction logging, replay, and session forking
'            CSR Layer: Infrastructure
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/audit/audit.2026.06.20.deep.domain.audit.md
' Review:    —
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Wire Log Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(wire_log_group, "Wire Log Group") <<infrastructure>> {
        Component(wire_log, "WireLog", "", "Records all agent-provider-tool interactions as structured log")
        Component(wire_appender, "WireAppender", "", "Appends wire log entries with buffering and flush control")
        Component(wire_reader, "WireReader", "ABC", "Reads and parses wire log entries for replay and analysis")
        Component(session_fork_mgr, "SessionForkManager", "", "Forks sessions from wire log replay at any turn")
    }
}

Component_Ext(session_store, "SessionStore", "Session Group")
System_Ext(wire_log_dir, "Wire Log Storage", "~/.nasim/wire-logs/<session-id>/")

Rel(wire_log, wire_appender, "appends entries")
Rel(wire_log, wire_reader, "reads entries")
Rel(wire_log, session_fork_mgr, "fork from turn")
Rel(wire_appender, wire_log_dir, "writes log files")
Rel(wire_reader, wire_log_dir, "reads log files")
Rel(session_fork_mgr, session_store, "creates forked session")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_cli.puml ---

@startuml c4_nasim_component_cli
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!include common/c4_styles.puml
!theme plain

' ============================================================
' Title:     NASIM Application — CLI Component Diagram
' Boundary:  NASIM Application (container) — CLI Group
' Purpose:   CLI Group: Controller layer for terminal users.
'            REPLSession is the entry point; it delegates to
'            AgentOrchestrator (Service layer) and renders
'            AgentEvents to terminal via Renderer.
'            CSR Layer: Controller
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/entities.md
' Review:    —
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — CLI Component Diagram

Person(user, "User", "Developer using NASIM Application")

Boundary(nasim_application, "NASIM Application") {
    Boundary(cli_group, "CLI Group") <<controller>> {
        Component(arg_parser, "ArgParser", "", "CLI argument parsing with layered config overrides")
        Component(repl_session, "REPLSession", "", "Interactive REPL loop, input handling, slash command dispatch")
        Component(renderer, "Renderer", "", "Terminal output: colors, diffs, streaming tokens, tool display")
        Component(slash_handler, "SlashCommandHandler", "", "Maps /cmd strings to agent actions")
    }
}

Component_Ext(agent_orch, "AgentOrchestrator", "Agent Group")
Component_Ext(config_loader, "ConfigLoader", "Config Group")

Rel(user, repl_session, "types commands", "terminal REPL")
Rel(repl_session, agent_orch, "delegates agent tasks")
Rel(repl_session, renderer, "AgentEvents to terminal output")
Rel(repl_session, slash_handler, "/cmd dispatch")
Rel(slash_handler, agent_orch, "agent action")
Rel(arg_parser, config_loader, "CLI flags override config")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/c4_nasim_component_git.puml ---

@startuml c4_nasim_component_git
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/v2.10.0/C4_Component.puml
!theme plain
!include common/c4_styles.puml


' ============================================================
' Title:     NASIM Application — Git Component Diagram
' Boundary:  NASIM Application (container) — Git Group
' Purpose:   Version control awareness: status, diff, commit
'            CSR Layer: Repository
' Milestone: v1.0
' Version:   9.0.0
' Source:    docs/prompt/p1.md — corrective architecture review
' Review:    docs/audit/audit.2026.06.20.comprehensive.reference.audit.md
' ============================================================

LAYOUT_WITH_LEGEND()
title NASIM Application — Git Component Diagram

Boundary(nasim_application, "NASIM Application") {
    Boundary(git_group, "Git Group") <<repository>> {
        Component(git_integration, "GitIntegration", "", "Auto-commit after file edits, branch awareness, diff tracking")
        Component(git_status, "GitStatus", "", "Read working tree status, staged changes, branch info")
        Component(git_commit, "GitCommit", "", "Create commits with conventional commit messages")
    }
}

System_Ext(git_repo, "Git Repository", "Version control for project files")

Rel(git_integration, git_status, "reads current state")
Rel(git_integration, git_commit, "creates commits")
Rel(git_status, git_repo, "git status, git diff")
Rel(git_commit, git_repo, "git commit, git add")

@enduml



--- SOURCE: /home/salim/prj/salim/nasim/code/nasim/docs/C4/common/c4_styles.puml ---

@startuml c4_styles
' ============================================================
' Title:     NASIM C4 — Shared Styles
' Purpose:   Common color palette, skinparam, and macros
'            for all nasim C4 diagrams.
' Version:   9.0.0
' Source:    docs/C4/common/c4_styles.puml
' ============================================================

' --- Color Palette (CSR Layers) ---
' Controller Layer (CLI, API)
skinparam rectangle<<controller>> {
    BackgroundColor<<controller>> #E3F2FD
    BorderColor<<controller>> #1565C0
    FontColor<<controller>> #0D47A1
}

' Service Layer (Agent, Router, Safety, Context Graph, Edit Strategy, Evaluation)
skinparam rectangle<<service>> {
    BackgroundColor<<service>> #FFF3E0
    BorderColor<<service>> #E65100
    FontColor<<service>> #BF360C
}

' Repository Layer (Session, Tool, Memory, Config, Git, Repo Intelligence)
skinparam rectangle<<repository>> {
    BackgroundColor<<repository>> #E8F5E9
    BorderColor<<repository>> #2E7D32
    FontColor<<repository>> #1B5E20
}

' Infrastructure Layer (MCP, Sandbox, Observability, Wire Log, Hooks, Plugins)
skinparam rectangle<<infrastructure>> {
    BackgroundColor<<infrastructure>> #F3E5F5
    BorderColor<<infrastructure>> #7B1FA2
    FontColor<<infrastructure>> #4A148C
}

' External Container (WebApp, DesktopApp, MobileApp)
skinparam rectangle<<external_container>> {
    BackgroundColor<<external_container>> #FAFAFA
    BorderColor<<external_container>> #616161
    FontColor<<external_container>> #212121
}

' Deployable Container (CLI Process, API Server Process, Core Engine)
skinparam rectangle<<deployable>> {
    BackgroundColor<<deployable>> #E8EAF6
    BorderColor<<deployable>> #283593
    FontColor<<deployable>> #1A237E
}

' --- Legend Macro ---
' Usage: !include common/c4_styles.puml
' Then: Legend("CSR Layer", "Controller", "#E3F2FD", "Service", "#FFF3E0", "Repository", "#E8F5E9", "Infrastructure", "#F3E5F5")

@enduml

